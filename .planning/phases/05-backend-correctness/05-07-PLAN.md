---
phase: 05-backend-correctness
plan: 07
type: execute
wave: 3
depends_on: ['05-01', '05-02', '05-03', '05-04', '05-05', '05-06', '05-08']
files_modified:
  - .planning/codebase/CONCERNS.md
  - .planning/phases/05-backend-correctness/05-CONTEXT.md
autonomous: true
requirements: ['BE-01', 'BE-02', 'BE-03', 'BE-04', 'BE-05', 'BE-06']
tags: ['docs', 'concerns-sweep', 'closing-slice']

must_haves:
  truths:
    - 'All six Phase 5 CONCERNS.md entries carry resolution markers in a SINGLE diff (one commit)'
    - 'Entry "Migrations run unconditionally on every process boot" → Resolved in Phase 5 (BE-01) with both NODE_ENV gate AND VACUUM INTO trap rollback cited'
    - 'Entry "Row-builder for games/platforms duplicated 3×" → Partially resolved in Phase 5 (BE-02) — INSERT zdedplikowany; UPDATE świadomie pozostawiony per D-10; re-open trigger (new column) + promotion trigger (3rd UPDATE call-site → separate helpers)'
    - 'Entry "Import-merge is N+1 reads inside a transaction" → Resolved in Phase 5 (BE-03) with grep guard reference'
    - 'Entry "Missing indices for some sort fields" → Resolved in Phase 5 (BE-04, accepted cost) — block comment in schema.ts'
    - 'Entry "Singleton wiring graph" → Test gap closed in Phase 5 (BE-06) via architectural grep pin + 503 state machine'
    - 'Entry "Hono route ordering is registration-sensitive" → Resolved in Phase 5 (BE-05) — body-shape pin + counter-weight test'
    - 'NEW Entry 7 (BE-02c, OPEN — blocked on ExportSnapshotV5): "Production import path silently drops coverImage + metadataRef" — `ImportData.execute` does not plumb these fields to `NewGame.create` because v4 snapshot schema does not carry them; repo-layer Q8 fix is dead code on this path. Documented via FIXME(BE-02c) markers in `import-data.ts` + `export-snapshot.ts` (planted by 05-08 Task 4).'
    - 'Top-of-file `*Last updated:*` line set to 2026-05-15'
    - 'Prior plan acceptance gates re-verified at sweep commit time: all BE-XX test files green AND architectural greps (BE-02 dedup, BE-03 lookup pattern, BE-06 singleton rg) return expected counts. Sweep gates the markers — it does not just write them.'
    - 'Phase 5 decisions D-30, D-31, D-32, D-33, D-34 transcribed into `.planning/phases/05-backend-correctness/05-CONTEXT.md` decisions section (persistent context — plan files get archived).'
  artifacts:
    - path: '.planning/codebase/CONCERNS.md'
      provides: 'Single closing-sweep commit landing all six Phase 5 resolution markers'
      contains: 'Resolved in Phase 5'
    - path: '.planning/phases/05-backend-correctness/05-CONTEXT.md'
      provides: 'D-30..D-33 transcribed into decisions section (Phase 5 milestone-locked decisions)'
      contains: 'D-33'
  key_links:
    - from: 'BE-02 partial resolution wording'
      to: 'D-10 carve-out + Q5 promotion trigger'
      via: 'inline re-open / promotion triggers documented in the entry body'
      pattern: 'Promotion trigger|Re-open trigger'
---

<objective>
Centralized closing-sweep slice for Phase 5: write all six CONCERNS.md resolution markers in a SINGLE diff (one commit). This replaces the per-plan CONCERNS edits originally scattered across 05-01..05-06.

**Why this slice exists (Q1 restructuring decision):** The original plan serialized six waves linearly (`05-02 → 05-03 → 05-04 → 05-01 → 05-05 → 05-06`) where only `05-02 → 05-03` was a real code dependency — the other five edges existed purely to serialize edits to this shared markdown file. By collapsing the doc edits into a single closing sweep, we honor only code dependencies and unlock parallel waves:

  - Wave 1 (parallel): 05-02, 05-04, 05-05
  - Wave 2 (parallel): 05-03 (needs 05-02 helper), 05-01, 05-06
  - Wave 3 (closing): 05-07 — this slice — single CONCERNS.md sweep

The trade-off: doc resolutions land in one commit (cleaner history) instead of being interleaved across six commits (cleaner per-author traceability). For a single-developer project the cleaner-history side wins.

**Why partial resolutions, not just "Resolved":** BE-02 is *partially* resolved (D-10 deliberately leaves update()/saveMetadata() row-builders inline). The sweep documents this honestly with re-open + promotion triggers per Q5, so a future contributor reading CONCERNS.md can tell what's actually closed vs. what's a deferred carve-out.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-correctness/05-CONTEXT.md
@.planning/phases/05-backend-correctness/05-RESEARCH.md
@.planning/phases/05-backend-correctness/05-PATTERNS.md
@.planning/phases/05-backend-correctness/05-VALIDATION.md
@.planning/phases/05-backend-correctness/05-01-SUMMARY.md
@.planning/phases/05-backend-correctness/05-02-SUMMARY.md
@.planning/phases/05-backend-correctness/05-03-SUMMARY.md
@.planning/phases/05-backend-correctness/05-04-SUMMARY.md
@.planning/phases/05-backend-correctness/05-05-SUMMARY.md
@.planning/phases/05-backend-correctness/05-06-SUMMARY.md
@CLAUDE.md
@.planning/codebase/CONCERNS.md

<interfaces>
CONCERNS.md current sections (line numbers approximate, may shift):
  Line 12: "Migrations run unconditionally on every process boot"  → BE-01
  Line 17: "Row-builder for games/platforms duplicated 3×"          → BE-02 (partial)
  Line 77: "Import-merge is N+1 reads inside a transaction"          → BE-03
  Line 82: "Missing indices for some sort fields"                    → BE-04 (accepted cost)
  Line 99: "Singleton wiring graph in apps/api/src/wiring.ts"        → BE-06 (test gap closed)
  Line 105: "Hono route ordering is registration-sensitive"          → BE-05
Top-of-file: "*Last updated: <date>*" line — update to 2026-05-15.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Single CONCERNS.md sweep — six resolution markers + Last updated line</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (full file)
    - All six 05-XX-SUMMARY.md artifacts (cross-reference what each plan actually delivered — exact paths, test names, etc.)
    - .planning/phases/05-backend-correctness/05-CONTEXT.md (D-10 carve-out wording for BE-02 partial resolution)
    - .planning/phases/05-backend-correctness/05-PATTERNS.md §10 (CONCERNS.md target table)
  </read_first>
  <behavior>
    - Single commit modifying `.planning/codebase/CONCERNS.md` ONLY.
    - All six listed entries carry resolution markers per the action template below.
    - BE-02 entry is special — **partial resolution** with explicit re-open + promotion triggers per Q5.
    - BE-01 entry covers BOTH the NODE_ENV gate AND the Q2 VACUUM INTO trap rollback in one resolution bullet.
    - BE-06 entry mentions the architectural grep pin (Q3) — NOT the old `await import().toBe()` tautology.
    - BE-05 entry mentions body-shape pin (Q6) — NOT the manual swap procedure.
    - BE-03 entry references the grep guard (Q4) alongside the inArray() refactor.
    - Top `*Last updated:*` set to `2026-05-15`.
    - No other sections of CONCERNS.md touched (no scope creep).
  </behavior>
  <action>
    **ORDERING (F-07-1 — strict, enforced in prose, not just verify-block):**

    Step 0 (BEFORE editing CONCERNS.md): run the F-07-1 re-verification gate. Execute:

    ```bash
    bun test apps/api/src/infrastructure/db/__tests__/to-game-insert-row.test.ts \
             apps/api/src/infrastructure/import/__tests__/apply-merge.test.ts \
             apps/api/src/__tests__/wiring.test.ts \
             apps/api/src/routes/games.test.ts
    test "$(grep -c 'toGameInsertRow(' apps/api/src/infrastructure/import/drizzle-import-repository.ts)" -ge 2
    test "$(grep -cE '\.where\(eq\(.*externalId.*\)\)' apps/api/src/infrastructure/import/drizzle-import-repository.ts)" -eq 0
    rg -c 'new (DrizzleGameRepository|DrizzleTransactionRunner|IgdbChainHolder)\(' apps/api/src -g '!apps/api/src/wiring.ts' -g '!**/__tests__/**'  # expect rc=1, no matches
    ```

    **If ANY of these fail, STOP.** Do not write any CONCERNS.md marker. A regressed prior plan must block the sweep, not get a "Resolved in Phase 5" marker that lies. Surface the failure to the executor; fix the regression in the relevant BE-XX plan; rerun this gate; then proceed to Step 1.

    Only AFTER all four `bun test` files exit 0 and all three architectural greps return expected counts, proceed:

    Step 1 — Edit `.planning/codebase/CONCERNS.md`. For each of the six target entries, REPLACE the existing `- Fix:` (or equivalent proposed-solution) line with the literal markdown below. Keep all surrounding lines (file/line citations, description) intact.

    **Entry 1 — "Migrations run unconditionally on every process boot" (BE-01):**
    ```
    - **Resolved in Phase 5 (BE-01):** auto-migrate gated by `process.env.NODE_ENV !== 'production'` in `apps/api/src/infrastructure/db/client.ts`. Production migrations run via versioned `scripts/deploy.sh` ahead of `sudo systemctl restart apex-api`. Deploy script takes a `VACUUM INTO` snapshot (`apps/api/scripts/backup.ts`) before each migration and uses `trap restore_and_exit ERR` to roll the DB back to the snapshot on failure — so a half-migrated state cannot persist. Retention: last 10 snapshots in `apps/api/data/backups/`.
    ```

    **Entry 2 — "Row-builder for games/platforms duplicated 3×" (BE-02, PARTIAL):**
    ```
    - **Partially resolved in Phase 5 (BE-02):** INSERT row-builder zdedplikowany do `toGameInsertRow(userId, input)` w `apps/api/src/infrastructure/db/schema.ts`; trzy INSERT call-sites (`DrizzleGameRepository.create`, `applyMerge` INSERT branch, `applyReplace` INSERT loop) używają helpera. **Świadomie pozostawione jako duplikat (D-10):** `DrizzleGameRepository.update()` i `saveMetadata()` — to dwa różne use-case'y (user PATCH vs system enrichment z IGDB) niewspółmierne z INSERT shape. Pin: `apps/api/src/infrastructure/db/__tests__/to-game-insert-row.test.ts` (6 it blocks: 4 functional + dedup grep gate + VO-unwrap snapshot counter).
    - **Re-open trigger:** gdy dodajemy nową kolumnę do `games` table — zweryfikuj wszystkie trzy miejsca z VO-unwrap (helper INSERT + update() set + saveMetadata() set). Snapshot counter test wybuchnie jeśli przegapisz.
    - **Promotion trigger:** jeśli zostanie dodany 3. UPDATE call-site z VO-unwrap pattern, wprowadzić `toGameUpdateRow` / `toGameMetadataRow` jako **osobne** helpery (NIE jeden wspólny — `update` to user PATCH, `saveMetadata` to system event, premature unification = pierwsza zmiana scope'u rozwala wspólny helper).
    - **Future work (v2 aggregate redesign):** `saveMetadata` to mały DDD smell — powinno być osobną metodą aggregate'a emitującą `GameMetadataEnriched` event, repo eksponuje pojedyncze `save(game)` z diffem. Out of scope dla Phase 5; zostawione na radarze.
    ```

    **Entry 3 — "Import-merge is N+1 reads inside a transaction" (BE-03):**
    ```
    - **Resolved in Phase 5 (BE-03):** `applyMerge` reads all matching rows via `inArray(externalId, [...])` — 2 SELECTs total (jeden dla `platforms`, jeden dla `games`) + in-memory `Map<externalId, row>` lookup. Per-row UPDATEs retained intentionally (D-13). Per-user scoping preserved via `and(eq(table.userId, userId), inArray(...))`. Empty-array guard sidesteps SQLite `IN ()` syntax error. Semantic regression pin: `apps/api/src/infrastructure/import/__tests__/apply-merge.test.ts` (100 games + 5 platforms + per-user isolation + empty plan edge case). **Q4 architectural guard:** `grep -E '\.where\(eq\(.*externalId.*\)\)' apps/api/src/infrastructure/import/drizzle-import-repository.ts` returns 0 — pinuje że per-row `externalId` lookup pattern nie wraca do tego pliku.
    ```

    **Entry 4 — "Missing indices for some sort fields" (BE-04, ACCEPTED COST):**
    ```
    - **Resolved in Phase 5 (BE-04, accepted cost):** block comment over `games` table in `apps/api/src/infrastructure/db/schema.ts` documents ~10ms full-scan sort cost on `hoursPlayed`/`genre`/`status` for single-user ≤5k rows. Already-indexed sort fields: `title`, `platform`, `format`, `releaseYear` (each scoped by `(user_id, kind, ...)`). Revisit when schema stabilises. See `feedback_no_premature_indices`.
    ```

    **Entry 5 — "Singleton wiring graph in `apps/api/src/wiring.ts`" (BE-06):**
    ```
    - **Test gap closed in Phase 5 (BE-06):** `apps/api/src/__tests__/wiring.test.ts` pins (a) `igdbConfigured === false` → 503 z `body.type='/errors/feature-disabled'` na `GET /api/games/metadata/candidates`, (b) 503 na `PATCH /api/games/:externalId/metadata`, (c) **architectural singleton pin** via `Bun.spawnSync(rg)` — 0 trafień dla `new (DrizzleGameRepository|DrizzleTransactionRunner|IgdbChainHolder)\(` poza `wiring.ts`. To pinuje anti-pattern z CLAUDE.md ("Skipping wiring.ts and new-ing dependencies in routes") wykonywalnym testem, NIE tautologicznym `await import().toBe()` (które przeszłoby przez ESM module cache niezależnie od singleton intent). Clean swap fixture (`beforeEach: swap(null)` + `afterEach: swap(snapshot)` bez throw) działa na dev maszynie z IGDB creds seeded.
    ```

    **Entry 6 — "Hono route ordering is registration-sensitive" (BE-05):**
    ```
    - **Resolved in Phase 5 (BE-05):** `describe('route ordering pin')` w `apps/api/src/routes/games.test.ts` zawiera 2 it bloki: (1) **body-shape pin** — `GET /api/games/metadata/candidates` zwraca status ≠ 404, a gdy 503 to `body.type === '/errors/feature-disabled'` (stabilny discriminator generowany WYŁĄCZNIE przez `games-metadata` sub-router, nie przez `:externalId` handler); (2) **counter-weight** — `GET /api/games/:externalId` dla non-reserved slug nadal trafia w single-game handler. RED jest dowiedziony konstrukcyjnie przez kształt asercji (body.type), nie przez manual swap routes — test failuje w momencie regresji bez ingerencji w `apps/api/src/routes/games.ts`.
    ```

    **Entry 7 — NEW — "Production import path silently drops coverImage + metadataRef" (BE-02c, OPEN — blocked on ExportSnapshotV5):**

    INSERT (do NOT replace anything — this is a new entry appended to the CONCERNS list after BE-06). Use a unique heading so subsequent greps locate it:

    ```
    ### Production import path silently drops `coverImage` + `metadataRef` — BE-02c
    - **File:** `apps/api/src/application/import/import-data.ts:101` (NewGame.create call) + `apps/api/src/application/export/export-snapshot.ts` (ExportedGame shape)
    - **Description:** `ImportData.execute` builds `NewGame.create(...)` from a v4 snapshot that does NOT carry `coverImage` or `metadataRef` (verified — `packages/shared/src/import-schema-v4.ts` does not declare them; `export-snapshot.ts` does not emit them). `DrizzleImportRepository.applyMerge` / `applyReplace` were extended in Phase 5 (BE-02b, plan 05-08) to persist these fields when supplied — but `g.coverImage` and `g.metadataRef` arrive as `undefined` at the call site. The repo-layer fix is dead code on this production path.
    - **Documented in Phase 5 (BE-02c, OPEN):** plan 05-08 Task 4 plants three signposts so the gap survives discovery: (a) TSDoc block above the `for` loop in `import-data.ts` listing the v5 unblocking work step-by-step; (b) inline `FIXME(BE-02c, F-08-1)` markers at both the `import-data.ts` call site AND the `export-snapshot.ts` ExportedGame shape; (c) round-trip test `apps/api/src/infrastructure/import/__tests__/round-trip.test.ts` Test 1 asserts `not.toHaveProperty('coverImage')` etc. — which will fail when v5 adds them, forcing a conscious decision to flip those to positive preservation assertions and update this entry to "Resolved".
    - **Unblocking work (out of Phase 5):** (1) bump snapshot schema to `ExportSnapshotV5` with `coverImage`, `metadataProvider`, `metadataProviderId`, `metadataMatchedAt` (additive — keep v4 readable); (2) extend `exportSnapshot` to emit them; (3) add `coverImage: g.coverImage ?? null` + `metadataRef: g.metadataRef ?? null` to `NewGame.create` call; (4) flip `not.toHaveProperty` → positive preservation in `round-trip.test.ts` Test 1; (5) update 05-CONTEXT.md D-33 to mark BE-02c resolved.
    - **Discovery search:** `rg 'FIXME\(BE-02c' apps/api/src` returns 3 hits (block + inline in `import-data.ts`, block in `export-snapshot.ts`).
    ```

    Update the top-of-file `*Last updated:*` line to `*Last updated: 2026-05-15*`.

    Do NOT touch any other sections of CONCERNS.md. Do NOT delete the original entries — only REPLACE the proposed-solution line. The original file/line citations + description prose remain.
  </action>
  <verify>
    <automated>
      # 1. Marker text present (six CONCERNS entries written)
      grep -c 'Resolved in Phase 5 (BE-01)\|Partially resolved in Phase 5 (BE-02)\|Resolved in Phase 5 (BE-03)\|Resolved in Phase 5 (BE-04, accepted cost)\|Test gap closed in Phase 5 (BE-06)\|Resolved in Phase 5 (BE-05)' .planning/codebase/CONCERNS.md

      # 2. Prior-plan test files still green at sweep commit time (F-07-1 gate — sweep verifies, doesn't just assert)
      bun test apps/api/src/infrastructure/db/__tests__/to-game-insert-row.test.ts \
               apps/api/src/infrastructure/import/__tests__/apply-merge.test.ts \
               apps/api/src/__tests__/wiring.test.ts \
               apps/api/src/routes/games.test.ts

      # 3. Architectural grep gates (BE-02 INSERT dedup, BE-03 per-row externalId anti-pattern, BE-06 singleton instantiation outside wiring)
      test "$(grep -c 'toGameInsertRow(' apps/api/src/infrastructure/import/drizzle-import-repository.ts)" -ge 2
      test "$(grep -cE '\.where\(eq\(.*externalId.*\)\)' apps/api/src/infrastructure/import/drizzle-import-repository.ts)" -eq 0
      Bun.spawnSync(["rg", "-c", "new (DrizzleGameRepository|DrizzleTransactionRunner|IgdbChainHolder)\\(", "apps/api/src", "-g", "!apps/api/src/wiring.ts", "-g", "!**/__tests__/**"]) # expect rc=1 (no matches)
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Resolved in Phase 5 (BE-01)' .planning/codebase/CONCERNS.md` = 1
    - `grep -c 'Partially resolved in Phase 5 (BE-02)' .planning/codebase/CONCERNS.md` = 1
    - `grep -c 'Resolved in Phase 5 (BE-03)' .planning/codebase/CONCERNS.md` = 1
    - `grep -c 'Resolved in Phase 5 (BE-04, accepted cost)' .planning/codebase/CONCERNS.md` = 1
    - `grep -c 'Resolved in Phase 5 (BE-05)' .planning/codebase/CONCERNS.md` = 1
    - `grep -c 'Test gap closed in Phase 5 (BE-06)' .planning/codebase/CONCERNS.md` = 1
    - `grep -c 'Re-open trigger' .planning/codebase/CONCERNS.md` ≥ 1 (BE-02 partial resolution wording)
    - `grep -c 'Promotion trigger' .planning/codebase/CONCERNS.md` ≥ 1 (BE-02 partial resolution wording)
    - `grep -c 'VACUUM INTO\|trap restore_and_exit' .planning/codebase/CONCERNS.md` ≥ 1 (BE-01 Q2 addition mentioned)
    - `grep -c 'body-shape pin\|body.type' .planning/codebase/CONCERNS.md` ≥ 1 (BE-05 Q6 addition mentioned)
    - `grep -c 'architectural singleton pin\|Bun.spawnSync.*rg' .planning/codebase/CONCERNS.md` ≥ 1 (BE-06 Q3 addition mentioned)
    - `grep -c 'Last updated: 2026-05-15' .planning/codebase/CONCERNS.md` ≥ 1
    - All six original entry headings still present (`grep -c '"Migrations run\|Row-builder\|Import-merge\|Missing indices\|Singleton wiring\|Hono route ordering"'` — entries preserved, not deleted)
    - **BE-02c new OPEN entry inserted:** `grep -c 'BE-02c' .planning/codebase/CONCERNS.md` ≥ 1; `grep -c 'OPEN — blocked on ExportSnapshotV5\|ExportSnapshotV5' .planning/codebase/CONCERNS.md` ≥ 1; `grep -c 'FIXME(BE-02c' .planning/codebase/CONCERNS.md` ≥ 1 (entry references the discoverability search)
    - **BE-02c FIXME markers physically present in source:** `rg -c 'FIXME\(BE-02c' apps/api/src/application/import/import-data.ts` = 2; `rg -c 'FIXME\(BE-02c' apps/api/src/application/export/export-snapshot.ts` = 2 (block label + cross-ref to import-data.ts companion inside the planted TSDoc body; planted by 05-08 Task 4 — verifies the cross-plan dependency landed)
    - **F-07-1 re-verification gate:** `bun test apps/api/src/infrastructure/db/__tests__/to-game-insert-row.test.ts apps/api/src/infrastructure/import/__tests__/apply-merge.test.ts apps/api/src/__tests__/wiring.test.ts apps/api/src/routes/games.test.ts` exits 0 — the four BE-XX test files that anchor the resolution markers are still green at sweep commit time. A regressed prior plan blocks the sweep instead of getting a "Resolved" marker that lies.
    - **Architectural grep gates re-verified:** `grep -c 'toGameInsertRow(' apps/api/src/infrastructure/import/drizzle-import-repository.ts` ≥ 2 (BE-02 helper still used); `grep -cE '\.where\(eq\(.*externalId.*\)\)' apps/api/src/infrastructure/import/drizzle-import-repository.ts` = 0 (BE-03 per-row anti-pattern not regressed); `rg -c 'new (DrizzleGameRepository|DrizzleTransactionRunner|IgdbChainHolder)\(' apps/api/src -g '!apps/api/src/wiring.ts' -g '!**/__tests__/**'` returns 0 (BE-06 singleton invariant).
    - **Pre-commit drift check:** if more than 7 days have passed since 2026-05-15 at sweep commit time, refresh the `Last updated:` stamp to commit date before commit.
  </acceptance_criteria>
  <done>Single CONCERNS.md commit landing six resolution markers (BE-01..BE-06) + one OPEN entry (BE-02c); existing entries preserved; partial-resolution wording for BE-02 with both triggers; Last updated stamp refreshed; BE-02c FIXME markers in source verified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Transcribe Phase 5 decisions D-30..D-34 into 05-CONTEXT.md</name>
  <files>.planning/phases/05-backend-correctness/05-CONTEXT.md</files>
  <read_first>
    - .planning/phases/05-backend-correctness/05-CONTEXT.md (full file — find the existing decisions section; D-09, D-10, D-29 already live there from prior planning)
    - .planning/phases/05-backend-correctness/05-08-PLAN.md (Q8 context for D-33 wording)
    - .planning/phases/05-backend-correctness/05-03-PLAN.md (Q7 DI context for D-30..D-32)
  </read_first>
  <behavior>
    - 05-CONTEXT.md gains five new decision entries (D-30, D-31, D-32, D-33, D-34) in the existing decisions section.
    - Entries are LOCKED markers (same status as D-09, D-10, D-29 — milestone-locked, not re-openable inside Phase 5).
    - D-33 wording reflects scope narrowing from final-pass review: round-trip invariant is pinned **only for the fields v4 ExportSnapshot carries** (price/purchasedAt/notes). coverImage + metadata cols remain repo-boundary-only until ExportSnapshot v5 lands.
    - D-34 documents the kind-strip in applyMerge UPDATE destructure (F-1 from backend-review-v3): `Game.moveToCollection()` is the only blessed kind-transition path; import cannot flip kind via snapshot-replay UPDATE. Pinned by `apply-merge.test.ts` Test 8.
    - No edits to any other CONCERNS.md entry, no edits to plan files, no edits outside the decisions section of 05-CONTEXT.md.
  </behavior>
  <action>
    Open `.planning/phases/05-backend-correctness/05-CONTEXT.md`. Locate the existing decisions section (where D-29 lives — likely near `## Decisions` or `## Locked decisions`). Append four entries:

    ```
    - **D-30 (Q7, LOCKED):** `DrizzleImportRepository` constructor accepts an optional `db` parameter defaulting to the module-level singleton: `constructor(private readonly db: BunSQLiteDatabase<typeof schema> = defaultDb) {}`. Production wiring in `apps/api/src/wiring.ts` calls `new DrizzleImportRepository()` (no-arg) — unchanged. Tests pass an in-memory DB via `new DrizzleImportRepository(db)`. Rationale: enables in-memory integration tests without disturbing production composition.
    - **D-31 (Q7, LOCKED):** In-memory SQLite via `new Database(':memory:')` + DI via D-30 is the STANDARD harness for new integration tests in Phase 5 (`apply-merge.test.ts`, `round-trip.test.ts`). Anti-patterns banned in these tests: `crypto.randomUUID()` for user IDs, `import { db } from '.../db/client'`, `new DrizzleImportRepository()` no-arg.
    - **D-32 (Q7, LOCKED):** Test users use STATIC string IDs (`TEST_USER_A = 'user-a'`, `TEST_USER_B = 'user-b'`, `TEST_USER_C = 'user-c'`). Random IDs are NOT needed because each test owns an isolated in-memory DB. Static IDs make assertions readable and per-user-isolation tests obvious.
    - **D-33 (Q8 / BE-02b, LOCKED):** Plan 05-08 SUPERSEDES D-09 for INSERT call-sites in `apps/api/src/infrastructure/import/drizzle-import-repository.ts`. Both `applyMerge` (INSERT branch AND UPDATE branch — see F-08-Q-DDD-1) and `applyReplace` INSERT loop pass the full 18-field `NewGame` shape to `toGameInsertRow`. Helper signature UNCHANGED. **Round-trip invariant scope:** pinned only for the fields v4 ExportSnapshot actually carries (`price`, `purchasedAt`, `notes`); `coverImage` and the three `metadata*` columns are exercised by `apply-merge.test.ts` Test 5/6/7 at the repo boundary but are NOT round-trippable through v4 snapshots (export-side drop documented via `not.toHaveProperty` assertions in `round-trip.test.ts`). Promotion to full round-trip coverage requires `ExportSnapshotV5` — out of Phase 5 scope; flagged in CONCERNS.md Future Work.
    - **D-34 (Q-DDD-1 / F-1, LOCKED):** `applyMerge` UPDATE branch destructure list strips `id`, `userId`, `externalId`, AND `kind` from the helper output before `.set(updateSet)`. The `kind` strip is DELIBERATE — `Game.moveToCollection()` (`apps/api/src/domain/games/game.ts`) is the only domain-blessed kind-transition path (wishlist → owned) with guards (existence check + field-reset semantics on `status`/`hoursPlayed`/`purchasedAt`). There is no `moveToWishlist()` — owned → wishlist is intentionally unsupported. A snapshot-replay UPDATE that allowed kind mutation would bypass BOTH the existence guard AND the reset semantics. The destructure-strip enforces: import can mutate scalar attributes of an existing game row but cannot flip its kind. Pinned by `apps/api/src/infrastructure/import/__tests__/apply-merge.test.ts` Test 8. If a future contributor "fixes" the strip by removing `kind: _k` from the destructure list, Test 8 fails RED.
    ```

    Do NOT touch any other section of 05-CONTEXT.md. Do NOT edit existing D-XX entries (in particular D-09 stays as-is — D-33 explicitly references D-09's INSERT-side supersession but does not modify the D-09 entry text).
  </action>
  <verify>
    <automated>
      grep -c '\*\*D-30 (Q7, LOCKED)\*\*' .planning/phases/05-backend-correctness/05-CONTEXT.md
      grep -c '\*\*D-31 (Q7, LOCKED)\*\*' .planning/phases/05-backend-correctness/05-CONTEXT.md
      grep -c '\*\*D-32 (Q7, LOCKED)\*\*' .planning/phases/05-backend-correctness/05-CONTEXT.md
      grep -c '\*\*D-33 (Q8 / BE-02b, LOCKED)\*\*' .planning/phases/05-backend-correctness/05-CONTEXT.md
      grep -c '\*\*D-34 (Q-DDD-1 / F-1, LOCKED)\*\*' .planning/phases/05-backend-correctness/05-CONTEXT.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\*\*D-30 ' .planning/phases/05-backend-correctness/05-CONTEXT.md` = 1
    - `grep -c '\*\*D-31 ' .planning/phases/05-backend-correctness/05-CONTEXT.md` = 1
    - `grep -c '\*\*D-32 ' .planning/phases/05-backend-correctness/05-CONTEXT.md` = 1
    - `grep -c '\*\*D-33 ' .planning/phases/05-backend-correctness/05-CONTEXT.md` = 1
    - `grep -c '\*\*D-34 ' .planning/phases/05-backend-correctness/05-CONTEXT.md` = 1
    - `grep -c 'D-09' .planning/phases/05-backend-correctness/05-CONTEXT.md` ≥ 2 (original D-09 entry preserved + referenced by D-33)
    - `git diff .planning/phases/05-backend-correctness/05-CONTEXT.md` shows ONLY appends to the decisions section (no other section touched)
  </acceptance_criteria>
  <done>D-30..D-34 transcribed; existing decisions preserved; D-33 honestly scopes round-trip to v4-carried fields; D-34 documents kind-strip rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure docs change) | This plan modifies only `.planning/codebase/CONCERNS.md`. No runtime, no API surface, no data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-DOCS-SWEEP | (none — meta-threat: stale CONCERNS) | .planning/codebase/CONCERNS.md | mitigate | The sweep IS the mitigation. Without it, six entries reference "to be done" while the code actually shipped resolutions — operationally confusing. LOW. |
</threat_model>

<verification>
- Task 1: `git diff --stat .planning/codebase/CONCERNS.md` shows that file modified; all six Phase 5 resolution markers present (grep checks above); all six original entry headings preserved (no deletions); BE-02 partial-resolution wording includes both Re-open trigger AND Promotion trigger lines; BE-01 wording cites VACUUM INTO + trap (Q2 addition); BE-06 wording cites architectural grep pin (Q3 addition); BE-05 wording cites body-shape pin (Q6 addition); BE-03 wording cites grep guard (Q4 addition); `*Last updated: 2026-05-15*` stamp present; new BE-02c OPEN entry inserted with FIXME(BE-02c) cross-reference
- Task 1 F-07-1 re-verification gate: `bun test` on the four BE-XX test files exits 0 BEFORE the sweep marker text is written
- Task 2: `git diff --stat .planning/phases/05-backend-correctness/05-CONTEXT.md` shows ONLY decisions-section appends; D-30/D-31/D-32/D-33 each appear exactly once; pre-existing D-09/D-10/D-29 entries unchanged
- Combined: the two-task sweep modifies exactly two files (`CONCERNS.md` + `05-CONTEXT.md`) — no other files touched
</verification>

<success_criteria>
- All eleven `must_haves.truths` observably true
- Closing-sweep commits (Task 1 + Task 2) consolidate what was previously six interleaved per-plan CONCERNS edits — honors the Q1 restructuring decision
- Partial-resolution honesty for BE-02 (D-10 carve-out documented with both triggers)
- Future-work radar for BE-02 (v2 aggregate redesign smell noted)
- F-07-1 closed: sweep gates its own markers via re-running BE-XX test files; a regressed prior plan blocks the sweep instead of silently producing a "Resolved" marker that lies
- D-30..D-33 land in persistent context (05-CONTEXT.md), not just in plan files which get archived
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-correctness/05-07-SUMMARY.md` with: the CONCERNS.md diff snippet (all six entry replacements), the Last updated stamp change, the 05-CONTEXT.md D-30..D-33 diff snippet, and a sanity-check `git log --oneline -1 -- .planning/codebase/CONCERNS.md` + `git log --oneline -1 -- .planning/phases/05-backend-correctness/05-CONTEXT.md` showing the Phase 5 sweep commits.
</output>
