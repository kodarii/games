---
phase: 05-backend-correctness
plan: 05
type: tdd
wave: 5
depends_on: ['05-01']
files_modified:
  - apps/api/src/routes/games.test.ts
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: ['BE-05']
tags: ['test', 'regression-pin', 'route-ordering', 'tdd']

must_haves:
  truths:
    - 'apps/api/src/routes/games.test.ts contains a describe("route ordering pin") with an it asserting `GET /api/games/metadata/candidates` returns status ≠ 404'
    - 'The test fails (RED) if a future contributor swaps the registration order so that /:externalId catches /metadata/*'
    - 'Acceptable statuses are documented in the test comment: 200, 400, 401, 503 (NOT 404)'
    - 'The test reuses the existing `app` harness from beforeAll (no new harness boilerplate)'
  artifacts:
    - path: 'apps/api/src/routes/games.test.ts'
      provides: 'New describe block "route ordering pin" with single regression test'
      contains: 'route ordering pin'
  key_links:
    - from: 'route ordering pin test'
      to: 'apps/api/src/routes/games.ts route registration order'
      via: 'GET /api/games/metadata/candidates resolves correctly only if `/metadata` registered BEFORE `/:externalId`'
      pattern: 'route ordering pin'
---

<objective>
Zapinać Hono route ordering regression testem w `apps/api/src/routes/games.test.ts`. ROADMAP SC-5 wprost mówi "Test `apps/api/src/routes/games.test.ts` weryfikuje że `GET /api/games/metadata/candidates` zwraca status ≠ 404".

Purpose: Hono route matching jest **registration-order sensitive**. Jeśli przyszły contributor zarejestruje `/:externalId` PRZED `/metadata/*`, wtedy `/api/games/metadata/candidates` zostanie dopasowane do `:externalId === 'metadata'` → handler dla single-game-lookup zwróci 404 (lub problem+json) zamiast routować do metadata sub-routera. Ten test failuje w momencie regresji.

Output: Nowy `describe('route ordering pin')` w istniejącym `games.test.ts`; jeden `it(...)` z assertion `expect(res.status).not.toBe(404)`; update w CONCERNS.md.

**Type: tdd.** Sekwencja: (1) RED — napisz test, uruchom, oczekuj GREEN natychmiast (bo route ordering jest już poprawne w HEAD). Potem zmień plik tymczasowo aby pokazać że test ŁAPIE regresję — RED. Potem cofnij zmianę — GREEN. Commit dwa razy: pierwszy z testem, drugi nigdy nie powstaje (regression check jest gedanken-experiment udokumentowany w SUMMARY).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-correctness/05-CONTEXT.md
@.planning/phases/05-backend-correctness/05-RESEARCH.md
@.planning/phases/05-backend-correctness/05-PATTERNS.md
@CLAUDE.md
@apps/api/src/routes/games.test.ts
@apps/api/src/routes/games.ts
@apps/api/src/routes/games-metadata.ts

<interfaces>
Existing test harness (apps/api/src/routes/games.test.ts:13-23, 142-157):
  function makeTestApp() {
    const app = new Hono<{ Variables: AuthVariables }>();
    attachProblemJsonErrorHandler(app);
    app.use('*', requestContext());
    app.use('/api/games/*', async (c, next) => {
      c.set('user', { id: TEST_USER_ID } as AuthVariables['user']);
      await next();
    });
    app.route('/api/games', games);
    return app;
  }
  // `app` is built in beforeAll(line ~54).

Existing route registration order (apps/api/src/routes/games.ts:153-156):
  games.route('/metadata', gamesMetadata);
  games.get('/:externalId', ...);
  // If a future PR swaps these, the regression test fires.

Metadata endpoint disabled (games-metadata.ts:18-26):
  When igdbChainHolder.get() === null, returns 503 with body.type='/errors/feature-disabled'.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED): Write failing test that asserts GET /api/games/metadata/candidates ≠ 404</name>
  <files>apps/api/src/routes/games.test.ts</files>
  <read_first>
    - apps/api/src/routes/games.test.ts (full file — confirm the existing `app` from beforeAll at line ~54 is in scope for new describe blocks; analog block at lines 142-157)
    - .planning/phases/05-backend-correctness/05-PATTERNS.md §6 (exact target shape for the new describe block)
    - .planning/phases/05-backend-correctness/05-CONTEXT.md §"BE-05" D-19..D-21 (Locked: file location, describe name, acceptable statuses)
    - apps/api/src/routes/games.ts (confirm current line 153-156 has `/metadata` registered BEFORE `/:externalId`)
    - apps/api/src/routes/games-metadata.ts (confirm 503 path when igdbChainHolder disabled — lines 18-26)
  </read_first>
  <behavior>
    - RED phase: First write the test as if the regression already exists. To prove the test ACTUALLY catches a regression, temporarily swap line 153 (`games.route('/metadata', ...)`) to come AFTER line 154 (`games.get('/:externalId', ...)`) in a scratch checkout. Run the test — expect status === 404 → expect().not.toBe(404) FAILS → RED confirmed. Then immediately revert games.ts to its current ordering — test GREENs. Document the RED screenshot/log in the SUMMARY.
    - GREEN phase: With games.ts at the canonical ordering, `GET /api/games/metadata/candidates?title=foo&platform=PC` returns one of {200, 400, 401, 503} — NOT 404.
    - The new describe MUST live inside games.test.ts (D-19 forced literally by ROADMAP SC-5).
  </behavior>
  <action>
    Step 1 (write the test): Append a new `describe('route ordering pin', () => { ... })` block at the bottom of `apps/api/src/routes/games.test.ts`. The block contains exactly one `it(...)`:

      `it('GET /api/games/metadata/candidates does not resolve to :externalId', async () => { const res = await app.request('/api/games/metadata/candidates?title=foo&platform=PC'); expect(res.status).not.toBe(404); });`

    Use the existing `app` from `beforeAll` (line ~54 in the file) — do NOT create a new test app. Per D-21, the seeded user middleware (line 17-22) bypasses auth so the test asserts route precedence, not auth precedence.

    Step 2 (document acceptable statuses): inside the describe block, add a leading code comment listing the acceptable statuses with rationale. Verbatim per 05-PATTERNS.md §6:

      `// BE-05: /metadata/* MUST be registered BEFORE /:externalId in routes/games.ts.`
      `// If a future maintainer reorders the registrations, GET /api/games/metadata/candidates`
      `// resolves to :externalId === 'metadata' → 404. This test fails the moment that regression lands.`
      `// Acceptable statuses: 200 (IGDB primed + match), 503 (chain null), 400 (validation), 401 (auth strips). NOT 404.`

    Step 3 (RED proof): Locally (not committed), temporarily swap lines 153-154 in `apps/api/src/routes/games.ts` to register `/:externalId` BEFORE `/metadata`. Run `bun test apps/api/src/routes/games.test.ts -t "route ordering pin"`. Expected: test FAILS with `expect(404).not.toBe(404)` (RED). Capture stdout. Revert games.ts to canonical ordering.

    Step 4 (GREEN): Run the test again — expect PASS. Log stdout. The status returned in current dev env (no IGDB seeded) should be 503 with body.type='/errors/feature-disabled' (per games-metadata.ts:18-26). The test only asserts `not.toBe(404)`, loose enough to survive env changes.

    DO NOT add new imports (`app`, `describe`, `it`, `expect` already in file scope).
  </action>
  <verify>
    <automated>bun test apps/api/src/routes/games.test.ts -t "route ordering pin"</automated>
  </verify>
  <acceptance_criteria>
    - `bun test apps/api/src/routes/games.test.ts -t "route ordering pin"` exits 0 (GREEN at canonical ordering)
    - `grep -c "route ordering pin" apps/api/src/routes/games.test.ts` = 1
    - `grep -c 'expect(res.status).not.toBe(404)' apps/api/src/routes/games.test.ts` ≥ 1
    - Comment block in the new describe contains the four acceptable statuses (`200`, `400`, `401`, `503`) AND the explicit `NOT 404` note
    - `git diff apps/api/src/routes/games.ts` shows NO changes (RED proof was reverted)
    - Manual RED log captured: `bun test apps/api/src/routes/games.test.ts -t "route ordering pin"` produced an `expect(404).not.toBe(404)` failure when games.ts route ordering was swapped (this log goes in SUMMARY, not committed)
  </acceptance_criteria>
  <done>Test exists in games.test.ts, GREEN at HEAD, manually verified RED when route order swapped.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update CONCERNS.md "Hono route ordering" section with Phase 5 resolution marker</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (find "Hono route ordering is registration-sensitive" entry, around line 105-108)
    - .planning/phases/05-backend-correctness/05-PATTERNS.md §10 (row for "Hono route ordering")
    - .planning/phases/05-backend-correctness/05-CONTEXT.md (D-19..D-21 context for resolution wording)
  </read_first>
  <behavior>
    - The entry "Hono route ordering is registration-sensitive" remains in CONCERNS.md (rewrite, do not delete).
    - Its `- Fix:` (or equivalent proposed-solution) line is replaced with: `- **Resolved in Phase 5 (BE-05):** describe('route ordering pin') in apps/api/src/routes/games.test.ts asserts GET /api/games/metadata/candidates ≠ 404. Test fails the moment a future PR swaps registration order.`
    - No other sections touched in this task (Wave 4 + 5 ordering means plans 05-01..05-04 already touched their entries).
  </behavior>
  <action>
    Edit `.planning/codebase/CONCERNS.md`. Find "Hono route ordering is registration-sensitive" entry (around lines 105-108). Replace its `- Fix:` (or equivalent) line literally with:

    `- **Resolved in Phase 5 (BE-05):** \`describe('route ordering pin')\` in \`apps/api/src/routes/games.test.ts\` asserts \`GET /api/games/metadata/candidates\` returns status ≠ 404. Test fails the moment a future PR swaps registration order in \`apps/api/src/routes/games.ts\`.`

    Keep all other lines in that entry intact. Update `*Last updated:*` to `2026-05-15` if such a line exists at top of file. Do NOT modify other sections.
  </action>
  <verify>
    <automated>grep -q 'Resolved in Phase 5 (BE-05)' .planning/codebase/CONCERNS.md && grep -q 'Hono route ordering' .planning/codebase/CONCERNS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Resolved in Phase 5 (BE-05)' .planning/codebase/CONCERNS.md` = 1
    - `grep -c 'Hono route ordering' .planning/codebase/CONCERNS.md` = 1 (entry preserved)
    - Resolution line cross-references `routes/games.test.ts` AND `routes/games.ts`
    - `git diff .planning/codebase/CONCERNS.md` shows changes ONLY in this entry (no edits in unrelated sections)
  </acceptance_criteria>
  <done>CONCERNS entry marked resolved; cross-references both test + source files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — test-only change) | This plan adds a test and updates docs. No runtime, no API surface change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-TEST | (none — meta-threat: future regression) | apps/api/src/routes/games.test.ts | mitigate | The test IS the mitigation. It pins route ordering so a careless reorder fails CI. LOW. |
</threat_model>

<verification>
- `bun test apps/api/src/routes/games.test.ts -t "route ordering pin"` exits 0
- `grep -c 'route ordering pin' apps/api/src/routes/games.test.ts` = 1
- Manual RED proof captured in SUMMARY (test failed when games.ts route order temporarily swapped)
- `git diff apps/api/src/routes/games.ts` is empty (RED proof was reverted before commit)
- `bun run --filter=@apex/api typecheck` green
- `bun run lint apps/api/src/routes/games.test.ts` clean
- `.planning/codebase/CONCERNS.md` "Hono route ordering" section marked **Resolved in Phase 5 (BE-05)**
</verification>

<success_criteria>
- All four `must_haves.truths` observably true
- ROADMAP §"Phase 5" SC-5 satisfied: test in `apps/api/src/routes/games.test.ts` verifying `GET /api/games/metadata/candidates` ≠ 404
- TDD cycle documented: RED proof captured (test caught the regression), GREEN confirmed at HEAD
- No production code changed (this is a test-only + docs plan)
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-correctness/05-05-SUMMARY.md` with: the new describe block source, the RED-log capture (manual swap stdout), GREEN run stdout, CONCERNS.md diff snippet.
</output>
