---
phase: 05-backend-correctness
plan: 06
type: tdd
wave: 6
depends_on: ['05-05']
files_modified:
  - apps/api/src/__tests__/wiring.test.ts
  - .planning/codebase/CONCERNS.md
autonomous: true
requirements: ['BE-06']
tags: ['test', 'smoke', 'composition', 'singleton', 'tdd']

must_haves:
  truths:
    - 'apps/api/src/__tests__/wiring.test.ts exists with a describe("wiring smoke (BE-06)") block'
    - 'Test 1 (503 when disabled): calling igdbChainHolder.swap(null) then GET /api/games/metadata/candidates returns 503 with body.type=/errors/feature-disabled'
    - 'Test 2 (503 on PATCH metadata): with chain null, PATCH /api/games/:externalId/metadata returns 503'
    - 'Test 3 (singleton identity): two sequential `await import("../wiring")` calls return the same igdbChainHolder, db, gameRepository, transactionRunner references'
    - 'beforeEach captures the chain state and afterEach restores it (per-test isolation)'
    - 'Test imports igdbChainHolder from ../wiring (no source-file change to wiring.ts required)'
  artifacts:
    - path: 'apps/api/src/__tests__/wiring.test.ts'
      provides: 'BE-06 composition smoke test: 503-when-disabled + singleton identity pins'
      contains: 'describe('
  key_links:
    - from: 'wiring.test.ts'
      to: 'apps/api/src/wiring.ts exports'
      via: 'static import (igdbChainHolder) + dynamic import (singleton identity)'
      pattern: 'import.*wiring'
    - from: 'igdbChainHolder.swap(null)'
      to: 'GET /api/games/metadata/candidates → 503'
      via: 'feature-disabled gate in games-metadata.ts:18-26'
      pattern: 'igdbChainHolder.swap\\(null\\)'
---

<objective>
Zapinać composition wiring (BE-06) smoke testem w nowym pliku `apps/api/src/__tests__/wiring.test.ts`. Test ma dwie nogi: (1) state machine — gdy `igdbChainHolder.swap(null)`, endpointy IGDB zwracają 503; (2) singleton identity — dwa `await import('../wiring')` zwracają te same instancje (`igdbChainHolder`, `db`, `gameRepository`, `transactionRunner`).

Purpose: ROADMAP SC-6: "`wiring.ts` ma test (smoke) że `igdbConfigured === false` skutkuje 503 na endpointach IGDB, oraz że singleton identity zachowana między requestami". Bez tego testu, ktoś może świadomie/nieświadomie rozbić composition root (np. new'ować repo w route, mock'ować igdbChainHolder lokalnie, refaktoryzować na non-singleton DI) i nie zauważymy.

Output: Nowy plik testu z 3+ `it(...)` blokami, oddzielony od `games.test.ts` (D-22 — żeby manipulacje stanem `igdbChainHolder` nie zatruwały innych testów). Reuse istniejącej `app.request()` harness z `routes/games.test.ts`. CONCERNS update.

**Type: tdd.** Sekwencja RED → GREEN per `it`. State-machine testy łatwo zacząć od RED (np. asser ` `).toBe(200)` zamiast 503 — wymusić efektywne pokrycie kontraktu — potem przepisać na docelowy 503. Singleton identity zacząć od assertion przez referencyjną nierówność, potem odwrócić.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
@$HOME/.claude/get-shit-done/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-correctness/05-CONTEXT.md
@.planning/phases/05-backend-correctness/05-RESEARCH.md
@.planning/phases/05-backend-correctness/05-PATTERNS.md
@CLAUDE.md
@apps/api/src/wiring.ts
@apps/api/src/infrastructure/igdb/igdb-chain-holder.ts
@apps/api/src/routes/games.ts
@apps/api/src/routes/games-metadata.ts
@apps/api/src/routes/games.test.ts

<interfaces>
IgdbChainHolder API (apps/api/src/infrastructure/igdb/igdb-chain-holder.ts:57-83):
  class IgdbChainHolder {
    get(): IgdbChain | null
    isConfigured(): boolean
    swap(creds: { clientId: string; clientSecret: string } | null): void
    // swap(null) resets breaker + clears chain + logs 'igdb.chain.cleared'
  }

wiring.ts exports (apps/api/src/wiring.ts):
  export const db = ...
  export const igdbChainHolder = new IgdbChainHolder({ logger })
  export const gameRepository = ...
  export const transactionRunner = ...
  // wiring.ts side-effect: at import time, primes chain from DB if creds present.

Existing app.request() harness pattern (apps/api/src/routes/games.test.ts:13-23):
  function makeTestApp() {
    const app = new Hono<{ Variables: AuthVariables }>();
    attachProblemJsonErrorHandler(app);
    app.use('*', requestContext());
    app.use('/api/games/*', async (c, next) => {
      c.set('user', { id: TEST_USER_ID } as AuthVariables['user']);
      await next();
    });
    app.route('/api/games', games);
    return app;
  }

games-metadata.ts (lines 18-26):
  When igdbChainHolder.get() === null → return c.json({ type: '/errors/feature-disabled', ... }, 503);
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1 (RED → GREEN): Write wiring.test.ts with 503-when-disabled + singleton identity tests</name>
  <files>apps/api/src/__tests__/wiring.test.ts</files>
  <read_first>
    - apps/api/src/wiring.ts (full file — confirm exports: db, igdbChainHolder, gameRepository, transactionRunner; confirm prime side-effect)
    - apps/api/src/infrastructure/igdb/igdb-chain-holder.ts (lines 57-83 — confirm `swap(null)` semantics)
    - apps/api/src/routes/games.ts (confirm GET /metadata/candidates exists, PATCH /:externalId/metadata exists)
    - apps/api/src/routes/games-metadata.ts (lines 18-26 — confirm 503 + body.type='/errors/feature-disabled' when chain null)
    - apps/api/src/routes/games.test.ts (lines 13-23 + 142-157 — reuse makeTestApp pattern)
    - .planning/phases/05-backend-correctness/05-PATTERNS.md §7 (full file target — paste-ready)
    - .planning/phases/05-backend-correctness/05-CONTEXT.md §"BE-06" D-22..D-25 (Locked: new file, isolated, swap(null) state machine, singleton via two await import)
    - .planning/phases/05-backend-correctness/05-VALIDATION.md (BE-06 verification rows)
  </read_first>
  <behavior>
    - Test 1 ("chain=null → 503 on GET /api/games/metadata/candidates"): after `igdbChainHolder.swap(null)` and `expect(igdbChainHolder.isConfigured()).toBe(false)`, `app.request('/api/games/metadata/candidates?title=foo&platform=PC')` returns status 503 AND body.type === '/errors/feature-disabled'.
    - Test 2 ("chain=null → 503 on PATCH /api/games/:externalId/metadata"): after swap(null), `app.request('/api/games/ext-1/metadata', { method: 'PATCH', headers: {'Content-Type':'application/json'}, body: JSON.stringify({ providerId: 'x' }) })` returns status 503.
    - Test 3 ("singleton identity"): `const a = await import('../wiring'); const b = await import('../wiring'); expect(a.igdbChainHolder).toBe(b.igdbChainHolder); expect(a.db).toBe(b.db); expect(a.gameRepository).toBe(b.gameRepository); expect(a.transactionRunner).toBe(b.transactionRunner);` — all four `.toBe()` checks pass via ESM module-cache identity.
    - beforeEach snapshots `igdbChainHolder.get()`. afterEach restores: if snapshot was null, call `igdbChainHolder.swap(null)` (no-op safe); if snapshot was NOT null, throw a helpful error message instructing the developer to clean their dev creds or run tests against a clean DB. Per D-23 + RESEARCH Pattern 5.
    - Test file lives in `apps/api/src/__tests__/wiring.test.ts` (NEW sibling __tests__ dir, NOT co-located with wiring.ts) — per D-22 isolated location prevents state pollution of other tests.
  </behavior>
  <action>
    Create `apps/api/src/__tests__/wiring.test.ts`. Use 05-PATTERNS.md §7 "Target shape" as the paste-ready skeleton.

    Step 1 (imports): per 05-PATTERNS.md §7 imports list:
      `import { afterEach, beforeEach, describe, expect, it } from 'bun:test';`
      `import { Hono } from 'hono';`
      `import { requestContext } from '../infrastructure/logging/request-context-middleware';`
      `import { attachProblemJsonErrorHandler } from '../routes/_problem-json';`
      `import { games as gamesRouter } from '../routes/games';`
      `import type { AuthVariables } from '../routes/middleware/require-auth';`
      `import { igdbChainHolder } from '../wiring';`

    Step 2 (test user + makeApp): replicate `makeTestApp` body inline as a top-level `makeApp()` helper. Use `const TEST_USER_ID = \`test-wiring-${crypto.randomUUID()}\`;`.

    Step 3 (describe + RED setup): write the describe block with three `it(...)`. For the RED phase per `it`:
      - Test 1 RED: start by asserting `expect(res.status).toBe(200)` — this WILL fail because chain is null → server returns 503. Capture the failure log → that's RED. Then flip to `expect(res.status).toBe(503)` → GREEN.
      - Test 2 RED: same pattern with the PATCH path.
      - Test 3 RED: start by asserting `expect(a.igdbChainHolder).not.toBe(b.igdbChainHolder)` — this WILL fail because ESM caches the module → singleton identity holds. Capture RED. Flip to `.toBe()` → GREEN.

    Step 4 (final GREEN form): per 05-PATTERNS.md §7 — the canonical form is `expect(res.status).toBe(503)` + body.type assertion for Test 1; status 503 for Test 2; four `.toBe()` identity checks for Test 3.

    Step 5 (beforeEach / afterEach): per 05-PATTERNS.md §7:
      `let savedChain: ReturnType<typeof igdbChainHolder.get>;`
      `beforeEach(() => { savedChain = igdbChainHolder.get(); });`
      `afterEach(() => { if (savedChain === null) { igdbChainHolder.swap(null); } else { throw new Error('wiring.test.ts assumed igdbChainHolder started disabled; if you changed test bootstrap to seed IGDB creds, extend afterEach to restore.'); } });`

    Step 6 (run): `bun test apps/api/src/__tests__/wiring.test.ts`. Expected: 3 it blocks GREEN.

    Step 7 (audit): capture the RED logs per `it` (from Step 3) into the SUMMARY (they are not committed — only the final GREEN form is committed).

    DO NOT touch `apps/api/src/wiring.ts` (per D-27: "Bez touch'owania `wiring.ts` poza dodaniem (jeśli niezbędne) eksportu `app`" — and 05-PATTERNS.md §7 confirms no export needed; existing exports suffice).
    DO NOT use `:memory:` SQLite — wiring.ts opens apps/api/data/apex.db at import time; test rides that side-effect (Pitfall 3 in RESEARCH).
    DO NOT seed games in this test — only chain state and identity matter.
  </action>
  <verify>
    <automated>bun test apps/api/src/__tests__/wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `bun test apps/api/src/__tests__/wiring.test.ts` exits 0 (all 3 it blocks GREEN)
    - File contains `grep -c 'igdbChainHolder.swap(null)' apps/api/src/__tests__/wiring.test.ts` ≥ 2 (one per state-machine test)
    - `grep -c 'expect(.*\.toBe(503))' apps/api/src/__tests__/wiring.test.ts` ≥ 2 (both state-machine tests assert 503)
    - `grep -c "type.*'/errors/feature-disabled'" apps/api/src/__tests__/wiring.test.ts` ≥ 1 (body.type assertion in Test 1)
    - `grep -c "await import('../wiring')" apps/api/src/__tests__/wiring.test.ts` ≥ 2 (singleton identity test does two dynamic imports)
    - `grep -c '\.toBe(' apps/api/src/__tests__/wiring.test.ts` ≥ 4 (singleton test has 4 identity assertions: igdbChainHolder, db, gameRepository, transactionRunner)
    - `grep -c 'afterEach' apps/api/src/__tests__/wiring.test.ts` ≥ 1 (state-restoration guard present)
    - `git diff apps/api/src/wiring.ts` empty (no source-file change to wiring)
    - `bun test apps/api/src/routes/games.test.ts` still green (proves the isolated location avoided cross-test pollution per D-22)
    - `bun run lint apps/api/src/__tests__/wiring.test.ts` clean
    - Manual RED proof captured in SUMMARY: per-it failures logged before the final GREEN form was written
  </acceptance_criteria>
  <done>Three `it(...)` GREEN; RED log captured per test; isolated __tests__ dir; wiring.ts untouched; existing tests still green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update CONCERNS.md "Singleton wiring graph" section with Phase 5 test-gap-closed marker</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <read_first>
    - .planning/codebase/CONCERNS.md (find "Singleton wiring graph in apps/api/src/wiring.ts" entry, around lines 99-103)
    - .planning/phases/05-backend-correctness/05-PATTERNS.md §10 (CONCERNS row for "Singleton wiring graph")
    - .planning/phases/05-backend-correctness/05-CONTEXT.md §"BE-06" (Locked decisions context for resolution wording)
  </read_first>
  <behavior>
    - The entry "Singleton wiring graph in apps/api/src/wiring.ts" remains in CONCERNS.md (rewrite, do not delete).
    - Beneath the line "Test gap: no test for composition." (or equivalent), add: `- **Test gap closed in Phase 5 (BE-06):** apps/api/src/__tests__/wiring.test.ts pins (a) igdbConfigured=false → 503 on GET /api/games/metadata/candidates and PATCH /api/games/:externalId/metadata; (b) singleton identity (igdbChainHolder, db, gameRepository, transactionRunner) across re-imports.`
    - No other sections touched in this task.
  </behavior>
  <action>
    Edit `.planning/codebase/CONCERNS.md`. Find "Singleton wiring graph in `apps/api/src/wiring.ts`" entry (around lines 99-103). Append a new bullet directly below the existing "Test gap: ..." line (or equivalent) with the literal markdown:

    `- **Test gap closed in Phase 5 (BE-06):** \`apps/api/src/__tests__/wiring.test.ts\` pins (a) \`igdbConfigured=false\` → 503 on \`GET /api/games/metadata/candidates\` and \`PATCH /api/games/:externalId/metadata\`; (b) singleton identity (\`igdbChainHolder\`, \`db\`, \`gameRepository\`, \`transactionRunner\`) across re-imports.`

    Keep all other lines in the entry intact. Update `*Last updated:*` line to `2026-05-15` if present. Do NOT modify other sections.
  </action>
  <verify>
    <automated>grep -q 'Test gap closed in Phase 5 (BE-06)' .planning/codebase/CONCERNS.md && grep -q 'Singleton wiring graph' .planning/codebase/CONCERNS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Test gap closed in Phase 5 (BE-06)' .planning/codebase/CONCERNS.md` = 1
    - `grep -c 'Singleton wiring graph' .planning/codebase/CONCERNS.md` = 1 (entry preserved)
    - Resolution line cross-references `apps/api/src/__tests__/wiring.test.ts`
    - Resolution line names all four singleton symbols: `igdbChainHolder`, `db`, `gameRepository`, `transactionRunner`
    - `git diff .planning/codebase/CONCERNS.md` shows changes ONLY in this entry
  </acceptance_criteria>
  <done>CONCERNS entry marked test-gap-closed; cross-references test file and four singleton symbols.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test ↔ module-level mutable state (igdbChainHolder) | swap(null) is observable globally inside the bun:test runtime — must be restored after each test (T-5-02 mitigation) |
| Dynamic import → ESM module cache | singleton identity is enforced by Bun's ESM loader; test asserts the contract holds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-02 | Tampering (singleton state pollution across tests) | beforeEach/afterEach guard in wiring.test.ts | mitigate | afterEach hook restores `igdbChainHolder` to snapshot or throws if the snapshot was non-null (forces developer to handle seeded creds). D-22's isolated __tests__ dir prevents this file's mutations from leaking into routes/games.test.ts. |
| T-5-TEST | Information Disclosure (test exposes wiring internals) | wiring.test.ts | accept | Test imports public exports only (`igdbChainHolder`, `db`, `gameRepository`, `transactionRunner`) — all already exported for production callers. No new surface area. LOW. |
</threat_model>

<verification>
- `bun test apps/api/src/__tests__/wiring.test.ts` exits 0 (3 it blocks GREEN)
- `bun test apps/api/src/routes/games.test.ts` still exits 0 (isolation per D-22)
- `bun test apps/api/src/__tests__/wiring.test.ts -t "503 when disabled"` exits 0 (per VALIDATION map BE-06-01)
- `bun test apps/api/src/__tests__/wiring.test.ts -t "singleton identity"` exits 0 (per VALIDATION map BE-06-02)
- `git diff apps/api/src/wiring.ts` empty (no source change required — D-27)
- `bun run --filter=@apex/api typecheck` green
- `bun run lint` clean
- `.planning/codebase/CONCERNS.md` "Singleton wiring graph" section carries **Test gap closed in Phase 5 (BE-06)** marker with all four singleton symbols named
- RED proof captured in SUMMARY: per-it failure logs before final GREEN form was committed
</verification>

<success_criteria>
- All six `must_haves.truths` observably true
- ROADMAP §"Phase 5" SC-6 satisfied: smoke test verifies `igdbConfigured === false` → 503 AND singleton identity preserved (tokenStore/circuitBreaker/rateLimiter implicitly preserved via igdbChainHolder identity, per D-24 "proxy is sufficient")
- TDD cycle: RED → GREEN per `it`, logs captured in SUMMARY
- Zero production-code changes (D-27)
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-correctness/05-06-SUMMARY.md` with: full wiring.test.ts source, RED-log snapshots per `it`, GREEN run stdout (showing 3 passes), `bun test apps/api/src/routes/games.test.ts` still-green proof, CONCERNS.md diff snippet.
</output>
