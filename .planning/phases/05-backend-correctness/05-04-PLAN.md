---
phase: 05-backend-correctness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/infrastructure/db/schema.ts
autonomous: true
requirements: ['BE-04']
tags: ['docs', 'sort-cost', 'accepted-debt']

must_haves:
  truths:
    - 'A TSDoc block comment exists ABOVE `export const games = sqliteTable(` in apps/api/src/infrastructure/db/schema.ts documenting the sort-cost decision for hoursPlayed/genre/status'
    - 'The comment names the three un-indexed sort fields explicitly (hoursPlayed, genre, status) and the four already-indexed fields (title, platform, format, releaseYear)'
    - 'The comment cites concrete numbers: single-user, ≤5k rows, ~10ms full-scan sort'
    - 'The comment references feedback_no_premature_indices'
    - 'No new migration file is added under apps/api/drizzle/ (D-16: NO indices added)'
  artifacts:
    - path: 'apps/api/src/infrastructure/db/schema.ts'
      provides: 'Block-comment documenting sort-cost acceptance above games table'
      contains: 'Sort-cost note'
  key_links:
    - from: 'apps/api/src/infrastructure/db/schema.ts block comment'
      to: 'feedback_no_premature_indices (user MEMORY)'
      via: 'in-text reference'
      pattern: 'feedback_no_premature_indices'

# NOTE: CONCERNS.md update for "Missing indices for some sort fields" section is
# handled centrally in plan 05-07 (Wave 3 CONCERNS sweep) — see 05-07-PLAN.md.
---

<objective>
Świadomie udokumentować brak indeksów dla pól sortujących `hoursPlayed`, `genre`, `status`. Per locked decision D-16: NIE dodajemy indeksów (premature optimization w ewoluującym modelu). Zamiast tego: block-comment nad `games` table w `schema.ts` + zaktualizowany wpis w `CONCERNS.md`.

Purpose: ROADMAP SC-4 daje dwie ścieżki ("dodać indeksy LUB świadomie udokumentować koszt"). User MEMORY (feedback_no_premature_indices) ustawia jasną preferencję — nie indeksuj pól, które mogą zostać przeprojektowane. Dokumentujemy konkretny koszt (single-user, ≤5k rows, ~10ms) i odsuwamy decyzję do momentu stabilizacji modelu.

Output: TSDoc block-comment w `schema.ts` (jedna lokacja). Zero zmian w kodzie, zero nowych migracji, zero zmian w testach. CONCERNS.md update centralizowany w Wave 3 (plan 05-07).

**Pure docs change.** Wave 1 (parallel z 05-02, 05-05) — plan 05-02 appenduje BELOW `NewGameRow` (line ~54), plan 05-04 inserts ABOVE `export const games = sqliteTable(` (line ~11). Linie się nie pokrywają, ale plik ten sam — executor musi merge'ować ostrożnie (możliwy syntactic conflict na dolnym pliku schema.ts gdy oba edytują top blok). Zalecane: 05-04 jako pierwszy w Wave 1 (mniejszy diff), potem 05-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-backend-correctness/05-CONTEXT.md
@.planning/phases/05-backend-correctness/05-RESEARCH.md
@.planning/phases/05-backend-correctness/05-PATTERNS.md
@CLAUDE.md
@apps/api/src/infrastructure/db/schema.ts

<interfaces>
Existing games table indices (apps/api/src/infrastructure/db/schema.ts:42-50):
  index('games_user_id_idx').on(table.userId)
  uniqueIndex('games_user_id_external_id_unq').on(table.userId, table.externalId)
  index('games_user_kind_idx').on(table.userId, table.kind)
  index('games_user_kind_platform_idx').on(table.userId, table.kind, table.platform)
  index('games_user_kind_format_idx').on(table.userId, table.kind, table.format)
  index('games_user_kind_releaseyear_idx').on(table.userId, table.kind, table.releaseYear)
  index('games_user_kind_title_idx').on(table.userId, table.kind, table.title)

NOT indexed (sort-by-only at full scan): hoursPlayed, genre, status.

Idempotency TSDoc precedent (apps/api/src/infrastructure/db/schema.ts:159-170) — table-level prose block-comment style to follow.

User MEMORY reference (~/.claude/projects/-Users-kodari-projects-games/memory/feedback_no_premature_indices.md):
  Title: "No premature DB indices"
  Body: don't index fields still being shaped; document full-scan cost instead while the schema is in flux.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add sort-cost TSDoc block comment above games table in schema.ts</name>
  <files>apps/api/src/infrastructure/db/schema.ts</files>
  <read_first>
    - apps/api/src/infrastructure/db/schema.ts (full file — games table at lines 11-54; idempotency TSDoc style precedent at lines 159-170)
    - .planning/phases/05-backend-correctness/05-PATTERNS.md §3 "Target additions" (the exact block-comment prose to land above `export const games = sqliteTable(...)`)
    - .planning/phases/05-backend-correctness/05-CONTEXT.md §"BE-04" D-16, D-17, D-18 (Locked: NO indices, doc the cost; format-already-indexed in D-18)
    - ~/.claude/projects/-Users-kodari-projects-games/memory/feedback_no_premature_indices.md (user preference)
  </read_first>
  <behavior>
    - After the change, line containing `export const games = sqliteTable(` is directly preceded (within 1 blank line gap) by a `/** ... */` TSDoc block whose body contains:
      - The literal phrase `Sort-cost note`
      - The three unindexed field names: `hoursPlayed`, `genre`, `status` (each appearing at least once)
      - The four already-indexed sort field names: `title`, `platform`, `format`, `releaseYear` (each appearing at least once)
      - The literal number `5k` (or `5000`)
      - The literal number `10ms` (or `10 ms`)
      - The literal phrase `feedback_no_premature_indices`
    - No edits to the `games` table definition itself, no new index, no migration file added.
  </behavior>
  <action>
    Edit `apps/api/src/infrastructure/db/schema.ts`. Locate `export const games = sqliteTable(` at line 11. Insert ABOVE this line (with one blank line separation from any prior import block) a `/** ... */` TSDoc block. Use the exact prose from 05-PATTERNS.md §3 "Target additions" → "Sort-cost note" comment block. Required content:

    1. Heading line: `Sort-cost note (BE-04, Phase 5).` (within the TSDoc block).
    2. Sentence stating: single-user, expected ≤5k rows per user.
    3. Sentence listing the three unindexed sort columns by name: `hoursPlayed`, `genre`, `status`.
    4. Sentence with concrete measurement: ~10ms on local WAL DB with 5k rows.
    5. Sentence stating the deferral rationale: schema continues to evolve; premature indices on fields likely to be reshaped cost a migrate-add + migrate-drop round trip with no user benefit.
    6. Reference to `feedback_no_premature_indices`.
    7. Closing sentence listing already-indexed fields: `title`, `platform`, `format`, `releaseYear` (each scoped by `(user_id, kind, ...)` per D-18 / observed in lines 42-50).

    Match the TSDoc indentation style of the idempotencyKeys block at lines 159-170 (2-space indent inside `/** * */`, single space after `*`).

    Do NOT change any other line of schema.ts. Do NOT add new imports. Do NOT add a migration. Do NOT modify the (table) => [...] indices array.

    Coordinate with plan 05-02 (also Wave 1, also touches schema.ts at a different line range): plan 05-02 appends BELOW line 54 (helper additions); plan 05-04 inserts ABOVE line 11 (comment). The two edits do not conflict structurally. Suggested ordering inside Wave 1: 05-04 first (smaller diff, ~10 lines), then 05-02 (~80-line append) — minimizes merge friction. If parallel execution lands plan 05-02 first, line numbers shift but the insertion point stays "directly above `export const games = sqliteTable(`".

    Run `bun run format apps/api/src/infrastructure/db/schema.ts` after editing.
  </action>
  <verify>
    <automated>grep -B0 -A0 'Sort-cost note' apps/api/src/infrastructure/db/schema.ts && grep -q 'hoursPlayed' apps/api/src/infrastructure/db/schema.ts && grep -q '5k\|5000' apps/api/src/infrastructure/db/schema.ts && grep -q '10[ ]*ms' apps/api/src/infrastructure/db/schema.ts && grep -q 'feedback_no_premature_indices' apps/api/src/infrastructure/db/schema.ts && bun run --filter=@apex/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Sort-cost note' apps/api/src/infrastructure/db/schema.ts` = 1
    - `grep -E 'hoursPlayed.*genre.*status|hoursPlayed,\s*genre,\s*status|hoursPlayed.*\n.*genre.*\n.*status' apps/api/src/infrastructure/db/schema.ts` matches (all three columns appear in the block, in any order, possibly across lines)
    - `grep -c 'feedback_no_premature_indices' apps/api/src/infrastructure/db/schema.ts` ≥ 1
    - `grep -E '(5k|5000)' apps/api/src/infrastructure/db/schema.ts | grep -v '^[[:space:]]*//\|^[[:space:]]*\*' | head -1` confirms the number is in the comment (`*` filter is okay — the comment WILL match the `*` filter, that's intentional)
    - Awk check: the line `export const games = sqliteTable(` is preceded by a line matching `*/` (block comment closer): `awk '/export const games = sqliteTable\(/{prev=last} {last=$0} END{exit !(prev ~ /\*\//)}' apps/api/src/infrastructure/db/schema.ts` exits 0
    - `find apps/api/drizzle -newer apps/api/src/infrastructure/db/schema.ts -name '*.sql' 2>/dev/null | wc -l` returns 0 (no new migration created)
    - `bun run --filter=@apex/api typecheck` exits 0
    - `bun run lint apps/api/src/infrastructure/db/schema.ts` clean
  </acceptance_criteria>
  <done>Block comment present above games table, all required keywords present, no new migration, typecheck + lint clean.</done>
</task>

</tasks>

<!-- NOTE: Task 2 (CONCERNS.md update for "Missing indices") was moved to plan 05-07
     (Wave 3 CONCERNS sweep). Per restructuring decision Q1, all six CONCERNS edits
     for Phase 5 are consolidated into a single closing slice to avoid serialising
     the wave chain around doc-merge convenience. -->


<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — pure docs change) | This plan changes only comments and markdown. No runtime, no API surface, no data flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-DOCS | Information Disclosure (none — comment contents are non-sensitive) | schema.ts block comment | accept | Comment documents performance characteristics already inferable from public schema. No PII, no secret. LOW. |
</threat_model>

<verification>
- `grep -c 'Sort-cost note' apps/api/src/infrastructure/db/schema.ts` = 1
- Block comment immediately precedes `export const games = sqliteTable(` (awk check)
- All required keywords present in comment: hoursPlayed, genre, status, title, platform, format, releaseYear, 5k (or 5000), 10ms, feedback_no_premature_indices
- No new file under `apps/api/drizzle/` (BE-04 D-16: zero indices added)
- `bun run --filter=@apex/api typecheck` green
- `bun run lint` clean
- CONCERNS.md update happens in 05-07 (Wave 3 sweep) — out of scope here
</verification>

<success_criteria>
- All five `must_haves.truths` observably true
- ROADMAP §"Phase 5" SC-4 satisfied: świadomie udokumentowany koszt w `schema.ts` (the "OR" branch chosen per D-17; CONCERNS.md side handled by 05-07)
- Zero source-code changes outside the new comment block (proves docs-only)
- Zero new migration files (proves D-16 honored)
</success_criteria>

<output>
After completion, create `.planning/phases/05-backend-correctness/05-04-SUMMARY.md` with: the full block comment prose and a `find apps/api/drizzle -name '*.sql' | wc -l` snapshot showing no new migration. NOTE: CONCERNS.md diff lands in plan 05-07 — do NOT include it here.
</output>
