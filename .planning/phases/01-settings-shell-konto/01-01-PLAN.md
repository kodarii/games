---
phase: 01-settings-shell-konto
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/src/components/ui/alert-dialog.tsx
  - apps/client/src/components/ui/card.tsx
  - apps/client/src/components/ui/label.tsx
  - apps/client/src/components/ui/button.tsx
  - apps/client/src/components/icons.tsx
  - apps/client/src/pages/settings/settings-layout.tsx
  - apps/client/src/pages/settings/settings-nav.tsx
  - apps/client/src/pages/settings/account.tsx
  - apps/client/src/main.tsx
autonomous: true
requirements: [SET-01, FE-07]
requirements_addressed: [SET-01, FE-07]
tags: [react, react-router-dom, shadcn, tailwind, settings, navigation]

must_haves:
  truths:
    - "D-01, D-02: Zalogowany user wpisujący `/settings` w pasek adresu (lub klikający `Settings` w głównym sidebarze) ląduje na `/settings/account` przez `<Navigate to=\"account\" replace />` index route, montowany jako nested route pod `ProtectedRoute → AppLayout` — nie na `Placeholder`, nie na 404."
    - "D-03: Wszystkie nowe pliki tej fazy żyją w `apps/client/src/pages/settings/` jako kebab-case modules (`settings-layout.tsx`, `settings-nav.tsx`, `account.tsx`, później `account-password-form.tsx` i `account-sessions-card.tsx`)."
    - "D-04: `apps/client/src/components/layout/sidebar.tsx` bottomNav pozostaje nietknięty — `Settings` link nadal kieruje na `/settings`, a redirect na `/settings/account` dziedziczony jest z index route z D-02."
    - "D-05, D-06: Side-nav to lekki własny komponent (`settings-nav.tsx`) — żadnego zagnieżdżonego `SidebarProvider`, żadnego shadcn `Sidebar` primitive — z dwiema sekcjami `KONTO` (z aktywnym `Konto`) i `POZOSTAŁE` (trzy disabled itemy: `Integracje`, `Dane`, `Wygląd`), każdy z shadcn Tooltip `Wkrótce`. Layout: `w-[220px]` aside po lewej."
    - "D-07: Aktywny link `Konto` ma classy active verbatim z `NavRow` w `sidebar.tsx` (`bg-[oklch(95%_0.02_220)] font-semibold text-apex-accent`) — wizualnie spójne z głównym sidebarem (FE-07)."
    - "D-06: Disabled item hover'owany pokazuje shadcn Tooltip z polskim tekstem `Wkrótce`; nie jest klikalny (no NavLink, `aria-disabled='true'`, `cursor-not-allowed`)."
    - "D-15: Trzy nowe shadcn primitives (`alert-dialog`, `card`, `label`) zainstalowane przez `bunx shadcn@latest add` — ich generowane pliki są ignorowane przez Biome (`**/components/ui/**`). Stanowią zależność dla Plan 02 (Card, Label) i Plan 03 (AlertDialog)."
    - "D-22: Plan 01 nie modyfikuje żadnego pliku w `apps/api/**` — better-auth ma `changePassword`/`revokeSessions` aktywne by-default w `emailAndPassword` config; nie ma nowych endpointów ani migracji DB."
    - "Komponenty respektują 4-rozmiarowy kontrakt typografii UI-SPEC (10/13/14/24 px) i paletę `apex-*` / `apex-accent` reserved."
  artifacts:
    - path: "apps/client/src/components/ui/alert-dialog.tsx"
      provides: "shadcn AlertDialog primitive (generated)"
      contains: "AlertDialogContent"
    - path: "apps/client/src/components/ui/card.tsx"
      provides: "shadcn Card primitive (generated)"
      contains: "CardContent"
    - path: "apps/client/src/components/ui/label.tsx"
      provides: "shadcn Label primitive (generated)"
      contains: "LabelPrimitive"
    - path: "apps/client/src/components/ui/button.tsx"
      provides: "Button with `destructive` variant added"
      contains: "destructive:"
    - path: "apps/client/src/components/icons.tsx"
      provides: "Icon set extended with `user`, `plug`, `database`, `palette` keys"
      contains: "user: svg("
    - path: "apps/client/src/pages/settings/settings-layout.tsx"
      provides: "SettingsLayout (Outlet host + side-nav shell)"
      exports: ["SettingsLayout"]
    - path: "apps/client/src/pages/settings/settings-nav.tsx"
      provides: "Secondary side-nav with section labels + disabled items"
      exports: ["SettingsNav"]
    - path: "apps/client/src/pages/settings/account.tsx"
      provides: "AccountPage stub (replaced in Plan 02)"
      exports: ["AccountPage"]
    - path: "apps/client/src/main.tsx"
      provides: "Router tree: nested `/settings` route with index Navigate"
      contains: "<SettingsLayout />"
  key_links:
    - from: "apps/client/src/main.tsx"
      to: "apps/client/src/pages/settings/settings-layout.tsx"
      via: "import + route element"
      pattern: "import \\{ SettingsLayout \\} from '@/pages/settings/settings-layout'"
    - from: "apps/client/src/pages/settings/settings-layout.tsx"
      to: "apps/client/src/pages/settings/settings-nav.tsx + <Outlet />"
      via: "composition"
      pattern: "<SettingsNav"
    - from: "apps/client/src/main.tsx (index route)"
      to: "/settings/account"
      via: "<Navigate to='account' replace />"
      pattern: "Navigate to=\"account\""

---

<objective>
Postaw shell strony `/settings`: zainstaluj 3 shadcn primitives (`alert-dialog`, `card`, `label`), dodaj brakujące klucze ikon i `destructive` variant do `Button`, stwórz `SettingsLayout` + `SettingsNav` + stub `AccountPage`, i podepnij nested route w `main.tsx`. Po tej fazie wertykalna kromka: zalogowany user idący na `/settings` widzi spójną z resztą apki powłokę z side-navem.

Purpose: Otworzyć przestrzeń pod sekcję Konto (Plan 02) i kolejne fazy (Integracje w Phase 2). Adresuje SET-01 (struktura side-nav + content area, rozszerzalna) oraz FE-07 (wizualna spójność z istniejącym AppLayout / sidebar).

Output: 3 nowe shadcn primitives + 4 nowe pliki w `pages/settings/` + edycje w `button.tsx`, `icons.tsx`, `main.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-settings-shell-konto/01-CONTEXT.md
@.planning/phases/01-settings-shell-konto/01-PATTERNS.md
@.planning/phases/01-settings-shell-konto/01-UI-SPEC.md

@apps/client/src/main.tsx
@apps/client/src/components/layout/app-layout.tsx
@apps/client/src/components/layout/sidebar.tsx
@apps/client/src/components/ui/button.tsx
@apps/client/src/components/icons.tsx
@apps/client/components.json

<interfaces>
<!-- Key shapes the executor needs. Do not re-explore the codebase. -->

From apps/client/src/main.tsx (current state — line ~43):
```tsx
{ path: 'settings', element: <Placeholder title="Settings" /> },
```
`Navigate` is already imported on line 16. `Placeholder` (lines 59-65) MUST stay — `/support` still uses it.

From apps/client/src/components/ui/button.tsx — `buttonVariants` exposes these variants TODAY:
`default | primary | outline | ghost | subtle`
No `destructive` variant yet. CSS tokens `bg-destructive` and `text-destructive-foreground` already exist in `tailwind.config.js` + `index.css` — no token work needed.

From apps/client/src/components/icons.tsx:
- Factory is `const svg = (children: ReactNode, vb = '0 0 16 16'): Svg`
- Existing keys (confirmed via grep): rows, grid, cal, coffee, folder, users, zap, gift, file, support, bell, chev*, sort, search, gear, more, plus, pg*, trash, arrowRight, logoMark, settings, plus likely others.
- Missing: `user`, `plug`, `database`, `palette`. MUST be added using the `svg(...)` factory (preferred) — do NOT substitute fallback keys (clean naming long-term).
- `Icon` type is exported alongside `IconName` (re-used by `sidebar.tsx:1`).

From apps/client/src/components/layout/sidebar.tsx — verbatim active-state classes (FE-07 parity):
- Active: `bg-[oklch(95%_0.02_220)] font-semibold text-apex-accent`
- Inactive: `text-apex-ink-3 hover:bg-apex-surface-hover hover:text-apex-ink`
- Icon wrapper: `flex h-[17px] w-[17px] shrink-0 items-center justify-center` + `active ? 'text-apex-accent' : 'opacity-55'`
- Section label: `text-[10px] font-semibold uppercase tracking-[0.08em] text-apex-hint`
- Match detection: `pathname.startsWith(entry.to)` (loose match)

UI-SPEC consolidations (do NOT copy sidebar's exact pixel-tweaks):
- Nav-link label: `text-[13px]` (NOT `text-[13.5px]`)
- Nav-link vertical padding: `py-2` (NOT `py-[10px]`)
- Section-label bottom tracking: `pb-1 pt-4` (NOT `pb-[6px]`)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn primitives + add `destructive` Button variant + extend icons</name>
  <files>apps/client/src/components/ui/alert-dialog.tsx, apps/client/src/components/ui/card.tsx, apps/client/src/components/ui/label.tsx, apps/client/src/components/ui/button.tsx, apps/client/src/components/icons.tsx</files>
  <read_first>
    - apps/client/src/components/ui/button.tsx (current `cva` config — line 6-25)
    - apps/client/src/components/icons.tsx (lines 1-30 — the `svg(...)` factory; then grep `^  [a-z]*: svg(` to enumerate existing keys before adding new ones)
    - apps/client/components.json (verify `style: new-york`, `baseColor: neutral`, `cssVariables: true`)
    - .planning/phases/01-settings-shell-konto/01-PATTERNS.md §6 (primitives install) and §"Note on Button variants" (lines ~432)
    - .planning/phases/01-settings-shell-konto/01-UI-SPEC.md §Color (Destructive row + reserved-destructive rule) and §Design System "Required new shadcn primitives"
  </read_first>
  <action>
    Three independent edits, applied in order.

    (a) **Install shadcn primitives (D-15).** Run `bunx shadcn@latest add alert-dialog card label` from `apps/client/` (single command, all three at once — uses the existing `components.json` preset). Confirm three new files appear: `alert-dialog.tsx`, `card.tsx`, `label.tsx` under `apps/client/src/components/ui/`. Do not hand-edit the generated files. The CLI may also touch `package.json` (adding `@radix-ui/react-alert-dialog`, `@radix-ui/react-label`) and update lockfile — let it. If the CLI offers `--overwrite` prompts for existing files, decline; only the three new files should land.

    (b) **Add `destructive` variant to `Button`** in `apps/client/src/components/ui/button.tsx`. Add one entry inside the `variants.variant` map (between `default` and `primary` is fine — Biome will not reorder object keys). Exact class set per UI-SPEC §Color → Destructive row: `bg-destructive text-destructive-foreground shadow hover:bg-destructive/90`. Preserve all existing variants, the `size` map, `defaultVariants`, the `ButtonProps` interface, the `forwardRef`, and `export { buttonVariants }`. Do NOT touch `Button.displayName`.

    (c) **Extend `apps/client/src/components/icons.tsx`** with four new SVG keys: `user`, `plug`, `database`, `palette`. Add them inside the `Icon` object using the existing `svg(...)` factory. Keep the established 16x16 viewBox (`vb = '0 0 16 16'` is default) and the same `stroke="currentColor" strokeWidth="1.4" strokeLinecap="round"` aesthetic the existing keys use (look at `rows` or `gear` for the visual style). Suggested glyphs:
       - `user`: head circle (cx=8 cy=6 r=2.5) + shoulders arc/ellipse below
       - `plug`: two prongs above a small rounded rect body + cable
       - `database`: stacked ellipses (top + middle + bottom) suggesting cylinder
       - `palette`: irregular blob with 3 small dots
       Keep glyphs simple — match the line-art density of existing icons. The `IconName` type is derived via `keyof typeof Icon`, so adding keys automatically extends the type — no other change needed.

    Do NOT introduce default exports. Do NOT touch any file in `apps/client/src/components/ui/` other than `button.tsx` (the three generated files are written by the CLI). Do NOT bump unrelated dependency versions.
  </action>
  <verify>
    <automated>
      cd /Users/kodari/projects/games &amp;&amp;
      test -f apps/client/src/components/ui/alert-dialog.tsx &amp;&amp;
      test -f apps/client/src/components/ui/card.tsx &amp;&amp;
      test -f apps/client/src/components/ui/label.tsx &amp;&amp;
      grep -q "destructive:.*bg-destructive" apps/client/src/components/ui/button.tsx &amp;&amp;
      grep -E "^[[:space:]]+(user|plug|database|palette):[[:space:]]*svg" apps/client/src/components/icons.tsx | wc -l | grep -q '^[[:space:]]*4$' &amp;&amp;
      cd apps/client &amp;&amp; bunx tsc -b --noEmit
    </automated>
  </verify>
  <done>
    Three new files exist in `components/ui/`. `button.tsx` `buttonVariants` config contains a `destructive` entry. `icons.tsx` exports four new keys: `user`, `plug`, `database`, `palette`. `tsc -b --noEmit` passes for `@apex/client`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create `SettingsLayout`, `SettingsNav`, and stub `AccountPage`</name>
  <files>apps/client/src/pages/settings/settings-layout.tsx, apps/client/src/pages/settings/settings-nav.tsx, apps/client/src/pages/settings/account.tsx</files>
  <read_first>
    - apps/client/src/components/layout/app-layout.tsx (lines 1-22 — `<Outlet />` host pattern, NO new `SidebarProvider`)
    - apps/client/src/components/layout/sidebar.tsx lines 36-102 (`NavRow` active-state classes + `SectionLabel` styling — TO COPY VERBATIM where indicated)
    - apps/client/src/pages/wishlist-columns.tsx (canonical Tooltip primitive composition — lines 1-31)
    - apps/client/src/components/icons.tsx (newly added `user`, `plug`, `database`, `palette` keys from Task 1)
    - .planning/phases/01-settings-shell-konto/01-PATTERNS.md §1, §2, §3 (per-file analog excerpts)
    - .planning/phases/01-settings-shell-konto/01-UI-SPEC.md §Spacing (`w-[220px]`, `max-w-[720px]`, `space-y-6`, `py-8 px-8`), §Typography (page h2 = `text-2xl font-semibold`), §Interaction Contracts (Side-nav active-state contract), §Copywriting Contract (side-nav copy + page title `Konto`)
  </read_first>
  <action>
    Three new files, kebab-case (D-03), named exports only, no barrel.

    (a) **`apps/client/src/pages/settings/settings-layout.tsx`** — exports `function SettingsLayout()`. Renders a two-column flex shell: left `<aside>` with `w-[220px] shrink-0 border-r border-apex-line-4 py-4` hosting `<SettingsNav />`, right `<div>` with `flex-1 min-w-0 overflow-y-auto` hosting `<Outlet />` from `react-router-dom`. The whole shell is `flex h-full` (it sits inside `AppLayout`'s `<main>` which is already `flex-1 flex-col` per `app-layout.tsx:11`). Do NOT instantiate a new `SidebarProvider` (per D-05 — settings layout lives inside AppLayout's provider). Imports: `Outlet` from `react-router-dom`, `SettingsNav` from `./settings-nav`.

    (b) **`apps/client/src/pages/settings/settings-nav.tsx`** — exports `function SettingsNav()`. Renders a vertical list with two grouped sections (D-06, UI-SPEC Copywriting Contract):
       - Section `KONTO`: one active `<NavLink to="/settings/account">` showing `Icon.user` (size=14) + label `Konto`.
       - Section `POZOSTAŁE`: three disabled items (`Integracje` → `Icon.plug`, `Dane` → `Icon.database`, `Wygląd` → `Icon.palette`).

       Active-link class set (D-07, verbatim from UI-SPEC Interaction Contracts, derived from `sidebar.tsx:54`):
       `cn('flex items-center gap-2 rounded-[7px] px-4 py-2 text-[13px] select-none', active ? 'bg-[oklch(95%_0.02_220)] font-semibold text-apex-accent' : 'text-apex-ink-3 hover:bg-apex-surface-hover hover:text-apex-ink')`

       Use `NavLink`'s render-prop signature: `<NavLink to=... className={({ isActive }) => cn(...)}>`. Within children, the icon wrapper class is `flex h-[17px] w-[17px] shrink-0 items-center justify-center` + conditional `isActive ? 'text-apex-accent' : 'opacity-55'`. Active label is rendered inside `<span className="flex-1">Konto</span>`.

       Section labels render through a small module-private `function SectionLabel({ children }: { children: React.ReactNode })` mirroring `sidebar.tsx:85-102` but with UI-SPEC spacing: classes `px-4 pb-1 pt-4 text-[10px] font-semibold uppercase tracking-[0.08em] text-apex-hint` (NOT `pb-[6px]`).

       Disabled items render through a module-private `function DisabledNavItem({ icon, label }: { icon: keyof typeof Icon; label: string })` that returns a `<Tooltip><TooltipTrigger asChild><span aria-disabled="true" className="flex items-center gap-2 rounded-[7px] px-4 py-2 text-[13px] text-apex-muted cursor-not-allowed select-none">...icon...<span>{label}</span></span></TooltipTrigger><TooltipContent>Wkrótce</TooltipContent></Tooltip>`. Wrap the whole nav in a single `<TooltipProvider>` at the top of the nav tree (shadcn `Tooltip` requires a Provider — verify by reading `wishlist-columns.tsx:1-31`). The disabled icon uses `<Icon[icon] size={14} className="opacity-55" />`.

       Top-level structure: `<nav className="flex h-full flex-col gap-1"><SectionLabel>KONTO</SectionLabel><ul className="list-none">{...active item...}</ul><SectionLabel>POZOSTAŁE</SectionLabel><ul className="list-none">{...three disabled items...}</ul></nav>` (use `<li>` wrappers for semantics — NavLink/span goes inside).

       Imports: `NavLink` from `react-router-dom`, `Icon` from `@/components/icons`, `cn` from `@/lib/utils`, `Tooltip`, `TooltipContent`, `TooltipProvider`, `TooltipTrigger` from `@/components/ui/tooltip`.

    (c) **`apps/client/src/pages/settings/account.tsx`** (stub for Plan 01, expanded in Plan 02) — exports `function AccountPage()`. For now renders (D-19, D-20 — page layout shell):
       ```tsx
       <div className="mx-auto w-full max-w-[720px] space-y-6 px-8 py-8">
         <h2 className="text-2xl font-semibold text-apex-ink">Konto</h2>
       </div>
       ```
       No `useSession` yet, no cards — Plan 02 will fill them. Just the page shell with the canonical heading and container so Plan 01's wave produces a routable, visually correct page.

    All three files: named export only (no `default`), no JSX comments referencing implementation status (no `// TODO`).
  </action>
  <verify>
    <automated>
      cd /Users/kodari/projects/games &amp;&amp;
      grep -q "export function SettingsLayout" apps/client/src/pages/settings/settings-layout.tsx &amp;&amp;
      grep -q "export function SettingsNav" apps/client/src/pages/settings/settings-nav.tsx &amp;&amp;
      grep -q "export function AccountPage" apps/client/src/pages/settings/account.tsx &amp;&amp;
      grep -q "export default" apps/client/src/pages/settings/settings-layout.tsx apps/client/src/pages/settings/settings-nav.tsx apps/client/src/pages/settings/account.tsx; test $? -eq 1 &amp;&amp;
      grep -q "bg-\[oklch(95%_0.02_220)\] font-semibold text-apex-accent" apps/client/src/pages/settings/settings-nav.tsx &amp;&amp;
      grep -q "Wkrótce" apps/client/src/pages/settings/settings-nav.tsx &amp;&amp;
      grep -q "max-w-\[720px\]" apps/client/src/pages/settings/account.tsx &amp;&amp;
      cd apps/client &amp;&amp; bunx tsc -b --noEmit
    </automated>
  </verify>
  <done>
    Three files exist with named exports. `settings-nav.tsx` contains verbatim active-state class string and Polish `Wkrótce` tooltip copy. `account.tsx` stub renders `Konto` h2 inside `max-w-[720px]` container. No `export default` anywhere. `tsc -b --noEmit` passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Mount nested `/settings` route in `main.tsx`</name>
  <files>apps/client/src/main.tsx</files>
  <read_first>
    - apps/client/src/main.tsx (whole file — lines 1-74; locate the existing Placeholder route on line 43)
    - .planning/phases/01-settings-shell-konto/01-PATTERNS.md §7 (modify main.tsx)
    - .planning/phases/01-settings-shell-konto/01-CONTEXT.md §"Integration Points" (D-02, D-03)
  </read_first>
  <action>
    Single surgical edit (D-01, D-02). Replace the existing single-element entry:

    `{ path: 'settings', element: <Placeholder title="Settings" /> },`

    with a nested route mounted under the existing `ProtectedRoute → AppLayout` parent (per D-01):

    ```
    {
      path: 'settings',
      element: <SettingsLayout />,
      children: [
        { index: true, element: <Navigate to="account" replace /> },
        { path: 'account', element: <AccountPage /> },
      ],
    },
    ```

    The index `<Navigate to="account" replace />` implements D-02: deep-link/refresh on `/settings` lands on `/settings/account`.

    Add two imports at the top alongside the existing `@/pages/...` imports (Biome organizeImports will sort alphabetically — write them in any order):
    - `import { AccountPage } from '@/pages/settings/account';`
    - `import { SettingsLayout } from '@/pages/settings/settings-layout';`

    Do NOT remove the `Placeholder` function definition (lines 59-65) — the `/support` route on line 44 still uses it. `Navigate` is already imported on line 16; reuse it. Do NOT reorder other routes. Do NOT touch the `AuthLayout` / `ProtectedRoute` wrappers — `/settings/*` is automatically gated by being a child of `<ProtectedRoute />` (D-01).
  </action>
  <verify>
    <automated>
      cd /Users/kodari/projects/games &amp;&amp;
      grep -q "import { SettingsLayout } from '@/pages/settings/settings-layout'" apps/client/src/main.tsx &amp;&amp;
      grep -q "import { AccountPage } from '@/pages/settings/account'" apps/client/src/main.tsx &amp;&amp;
      grep -q "element: <SettingsLayout />" apps/client/src/main.tsx &amp;&amp;
      grep -q 'element: <Navigate to="account" replace />' apps/client/src/main.tsx &amp;&amp;
      grep -q "{ path: 'support', element: <Placeholder" apps/client/src/main.tsx &amp;&amp;
      grep -c "Placeholder title=\"Settings\"" apps/client/src/main.tsx | grep -q '^0$' &amp;&amp;
      cd apps/client &amp;&amp; bunx tsc -b --noEmit &amp;&amp; bunx vite build --mode development
    </automated>
  </verify>
  <done>
    `main.tsx` mounts `/settings` as a nested route with index `<Navigate to="account" replace />` and a `/settings/account` child. `Placeholder` function is preserved (still used by `/support`). TypeScript compiles and Vite build succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → React router | URL path crosses here; resolved against `createBrowserRouter` config |
| `/settings/*` → `ProtectedRoute` | Auth session check happens upstream of `SettingsLayout` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | Unauthenticated user accessing `/settings/*` | mitigate | `/settings` is a child of `<ProtectedRoute />` (`main.tsx:30`); guard already redirects to `/login` if no session. Verified in Plan 03 task 3. |
| T-01-02 | Tampering | URL manipulation (`/settings/foo` deep link) | accept | react-router falls through to the existing `/` `<Navigate to="/games">` index — non-existent settings sub-paths are not security-sensitive in Phase 1. Future sections opt-in by adding their own children. |
| T-01-03 | Information disclosure | Tooltip leaking future feature names (`Integracje`, `Dane`, `Wygląd`) | accept | Disabled items intentionally communicate roadmap shape per CONTEXT.md D-06; no PII, no secrets. |
| T-01-04 | Denial of service | Bundle bloat from shadcn primitives | accept | Three primitives (`alert-dialog`, `card`, `label`) are tree-shakable Radix wrappers; project already includes `tooltip`, `sheet` etc. No measurable user impact. |

ASVS L1 categories not applicable to this plan: authentication (Plan 02), session management (Plan 03), input validation (Plan 02). Broader hardening (CSRF, per-user rate-limit, SameSite=Strict) is owned by Phase 3 — NOT pulled forward.
</threat_model>

<verification>
- `cd apps/client && bunx tsc -b --noEmit` passes (no type errors introduced).
- `cd apps/client && bunx vite build --mode development` succeeds (router tree resolves at build time).
- Manual smoke (single-line `curl` not applicable for SPA): after `bun run dev`, navigate to `http://localhost:5173/settings` while logged in → URL replaces to `/settings/account`, side-nav visible with `Konto` active link and three `Wkrótce`-tooltipped disabled items.
- Grep-gate (file present + invariants):
  ```
  test -f apps/client/src/components/ui/alert-dialog.tsx
  test -f apps/client/src/components/ui/card.tsx
  test -f apps/client/src/components/ui/label.tsx
  grep -q "destructive:.*bg-destructive" apps/client/src/components/ui/button.tsx
  grep -q "user: svg" apps/client/src/components/icons.tsx
  grep -q "plug: svg" apps/client/src/components/icons.tsx
  grep -q "database: svg" apps/client/src/components/icons.tsx
  grep -q "palette: svg" apps/client/src/components/icons.tsx
  ```
</verification>

<success_criteria>
ROADMAP success criteria addressed by this plan:
- **SC-1** (settings shell visible, spójna z AppLayout): satisfied end-to-end (route mounted, layout renders nav + outlet, FE-07 parity via verbatim class copy).
- **SC-5** (ProtectedRoute guarding `/settings/*`): structural prerequisite satisfied (route nested under existing `<ProtectedRoute />`). Behavioral verification deferred to Plan 03 task 3.

REQUIREMENTS addressed:
- **SET-01**: Side-nav + content area structure live, extensible (Phase 2 will add `/settings/integrations` child).
- **FE-07**: shadcn primitives + `apex-*` tokens + verbatim active-state classes from `sidebar.tsx`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-settings-shell-konto/01-01-SUMMARY.md` capturing:
- Generated shadcn files (paths + export names verified post-install)
- `Button` variant added (snippet of new entry in `buttonVariants`)
- Icon keys added (4 new keys, brief glyph description)
- Files created with line counts
- Verification outputs (tsc + vite build)
- Any deviations from the plan (icon glyph aesthetics are at executor's reasonable discretion)
</output>
</output>
</output>
