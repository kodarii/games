---
phase: 04-frontend-stability
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/src/components/ui/dropdown-menu.tsx
  - apps/client/src/components/icons.tsx
  - apps/client/src/components/game-view/game-view-header.tsx
  - apps/client/src/components/game-view/game-view-actions.tsx
  - apps/client/src/components/game-view/game-view-fields.tsx
  - apps/client/src/pages/game-view.tsx
autonomous: false
requirements: [FE-03, FE-04, FE-05]
requirements_addressed: [FE-03, FE-04, FE-05]
sc_mappings: [3, 4, 5]
mode: mvp
tags: [react, radix, dropdown, shadcn, decomposition, icons, game-view, refactor]

must_haves:
  truths:
    - "`apps/client/src/components/ui/dropdown-menu.tsx` istnieje (shadcn wrapper Radix DropdownMenu) i jest aliased pod `@/components/ui/dropdown-menu` (workspace path quirk z STATE.md naprawiony: brak folderu `apps/client/@/`)."
    - "Action dropdown w `game-view.tsx` używa `@radix-ui/react-dropdown-menu` przez shadcn wrapper — brak hand-rolled click-outside (`useEffect.*mousedown` ZNIKNIĘTE z source); keyboard nav (Tab/Arrow/Escape) działa za darmo z Radix (FE-03)."
    - "`game-view.tsx` rozdzielone na trzy NOWE komponenty w `components/game-view/`: `game-view-header.tsx`, `game-view-actions.tsx`, `game-view-fields.tsx`; każdy <250 LOC; `game-view.tsx` orchestrator <250 LOC (FE-04)."
    - "Lewy panel (cover/badges/format/UploadCoverButton) jest absorbed do `<GameViewFields>` per RESEARCH §Pattern 5 'Risk' decision — nie spawn'ujemy 4. komponentu. SC4 wording '3 komponenty' zachowany."
    - "Brak inline `<svg>` w `apps/client/src/pages/game-view.tsx` (FE-05) — `grep -c '<svg' apps/client/src/pages/game-view.tsx` == 0. Inline SVGs migrated do `icons.tsx`: `Icon.disc`, `Icon.download`, `Icon.edit`. `Icon.trash` REUSED (already exists)."
    - "Nowe ikony w `icons.tsx` używają istniejącego `svg(children, vb)` factory (lines 11-25), 16x16 viewBox, stroke-width 1.4 — match'ujące konwencję `Icon.trash` (lines 451-479) i `Icon.coffee` (103-120)."
    - "Zachowane: `useGameDraft(game)` w `GameViewBody`; URL state (jeśli istnieje); mutation behavior (`useUpdateGameMutation`, `useDeleteGameMutation`, `useMoveToCollectionMutation`); edit-mode toggle; breadcrumb; `notesRef` autosize effect (przeniesiony do `<GameViewFields>` razem z textarea)."
    - "Manualny happy-path smoke przechodzi: open game (`/games/:id`) → switch to edit (`?edit=1` lub button) → zmień field → save → success; dropdown Edit/Delete: keyboard nav Tab → Enter, Arrow up/down, Escape zamyka; click-outside zamyka."
    - "OUT OF SCOPE: pre-existing `alert('Failed to delete: ...')` w `game-view.tsx:285` — preserve as-is, NIE zastępujemy toast'em w tym planie (defer to v2)."
  artifacts:
    - path: "apps/client/src/components/ui/dropdown-menu.tsx"
      provides: "shadcn wrapper Radix DropdownMenu (Root, Trigger, Content, Item, Separator, etc.)"
      contains: "DropdownMenuPrimitive"
    - path: "apps/client/src/components/icons.tsx"
      provides: "Extended icon registry with disc, download, edit (16x16 stroke-1.4)"
      contains: "disc: svg"
    - path: "apps/client/src/components/game-view/game-view-header.tsx"
      provides: "Header bar: SidebarTrigger + breadcrumb + edit-mode CTA buttons"
      exports: ["GameViewHeader"]
      max_lines: 250
    - path: "apps/client/src/components/game-view/game-view-actions.tsx"
      provides: "Wishlist 'Move' button + Radix-based ActionsDropdown (Edit/Delete)"
      exports: ["GameViewActions"]
      max_lines: 250
    - path: "apps/client/src/components/game-view/game-view-fields.tsx"
      provides: "Left panel (cover/badges/format/UploadCoverButton) + right panel (<dl> grid with FieldItem + Notes textarea)"
      exports: ["GameViewFields"]
      max_lines: 250
    - path: "apps/client/src/pages/game-view.tsx"
      provides: "GameViewPage (query + error gate) + GameViewBody (orchestrator state + mutations + layout shell delegating to 3 children)"
      max_lines: 250
  key_links:
    - from: "apps/client/src/components/game-view/game-view-actions.tsx"
      to: "@/components/ui/dropdown-menu"
      via: "named import (DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator)"
      pattern: "from '@/components/ui/dropdown-menu'"
    - from: "apps/client/src/pages/game-view.tsx (GameViewBody)"
      to: "apps/client/src/components/game-view/game-view-header.tsx"
      via: "composition (named import + render)"
      pattern: "<GameViewHeader"
    - from: "apps/client/src/pages/game-view.tsx (GameViewBody)"
      to: "apps/client/src/components/game-view/game-view-actions.tsx"
      via: "composition"
      pattern: "<GameViewActions"
    - from: "apps/client/src/pages/game-view.tsx (GameViewBody)"
      to: "apps/client/src/components/game-view/game-view-fields.tsx"
      via: "composition"
      pattern: "<GameViewFields"
    - from: "apps/client/src/components/game-view/game-view-actions.tsx"
      to: "Icon.edit + Icon.trash"
      via: "named import from @/components/icons"
      pattern: "Icon\\.(edit|trash)"
    - from: "apps/client/src/components/game-view/game-view-fields.tsx"
      to: "Icon.disc + Icon.download"
      via: "named import from @/components/icons"
      pattern: "Icon\\.(disc|download)"

---

<objective>
Trzy splecione zmiany w game-view subsystem: (a) instalacja shadcn `dropdown-menu` primitive + migracja hand-rolled `ActionsDropdown` na Radix; (b) rozbicie `pages/game-view.tsx` (669 LOC) na orchestrator + trzy komponenty w `components/game-view/` (każdy <250 LOC); (c) ekstrakcja czterech inline `<svg>` do `Icon` registry — trzy nowe entries (`disc`, `download`, `edit`) + reuse istniejącego `Icon.trash`.

Purpose: Phase 4 SC3 (FE-03 — Radix dropdown z keyboard nav za darmo) + SC4 (FE-04 — dekompozycja monolitu na nazwane komponenty) + SC5 (FE-05 — brak inline SVG w game-view).

Output: 1 nowy plik (shadcn dropdown-menu wrapper, ~80 LOC, Biome-ignored), 3 nowe pliki komponentów (game-view-header/actions/fields), 1 rozszerzony plik (icons.tsx + 3 entries), 1 znacząco zrefaktorowany plik (game-view.tsx, 669 → ~150 LOC).

Plan nie-autonomous: zawiera checkpoint manualnego smoke testu (dropdown keyboard nav nie da się zweryfikować w bun:test source-pin).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-stability/04-RESEARCH.md
@.planning/phases/04-frontend-stability/04-PATTERNS.md

@apps/client/src/pages/game-view.tsx
@apps/client/src/components/icons.tsx
@apps/client/src/components/ui/alert-dialog.tsx
@apps/client/src/pages/settings/settings-page.tsx
@apps/client/src/hooks/use-game-draft.ts
@apps/client/components.json
@apps/client/package.json

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase + RESEARCH §Pattern 4-6 + PATTERNS.md. -->

From apps/client/package.json (already installed, do NOT add):
- `@radix-ui/react-dropdown-menu`: `^2.1.2` (RESEARCH §Standard Stack confirmed via npm view: latest 2.1.16, caret resolves within 2.x).
- `class-variance-authority`, `clsx`, `tailwind-merge` — used by `cn()` in `@/lib/utils`.

From apps/client/src/components/ui/alert-dialog.tsx — PRIMARY ANALOG (PATTERNS.md `### dropdown-menu.tsx`):
- Lines 1-12: `import * as React from "react"; import * as AlertDialogPrimitive from "@radix-ui/react-alert-dialog"; import { cn } from "@/lib/utils"; const AlertDialog = AlertDialogPrimitive.Root; ...`
- Lines 13-26: `React.forwardRef` wrapper for sub-component with `className` merged via `cn()`.
- Lines 127-139: explicit named-export list at bottom (no `export default`).
- **Style drift expected:** shadcn CLI emits double quotes + missing trailing commas — `components/ui/**` is in `biome.json` ignore list (CLAUDE.md confirms). Do NOT manually reformat.

From STATE.md "Plan 01-01 shadcn CLI workspace quirk":
- `npx shadcn@latest add <primitive>` writes files to `apps/client/@/components/ui/<primitive>.tsx` (literal `@` directory at workspace root), NOT to `apps/client/src/components/ui/`.
- Required cleanup: `mv apps/client/@/components/ui/dropdown-menu.tsx apps/client/src/components/ui/dropdown-menu.tsx && rm -rf apps/client/@`.
- Verify the final path resolves: `import { DropdownMenu } from '@/components/ui/dropdown-menu'` (where `@/` alias points to `apps/client/src/`).

From Radix DropdownMenu API (verified via Context7 `/websites/radix-ui_primitives`):
- Root → Trigger → Portal → Content → Item / Separator / Sub.
- `<DropdownMenuTrigger asChild>` delegates to child element (preserves existing button styling).
- `<DropdownMenuItem onSelect={...}>` is the canonical action callback (NOT `onClick`).
- `<DropdownMenuContent align="end" sideOffset={4} collisionPadding={8}>` is standard for top-bar dropdowns.
- Keyboard nav, click-outside, `role="menu"`, focus trap, Escape — all built-in (RESEARCH §Pattern 4 + §Don't Hand-Roll).

From RESEARCH §Pattern 4 (lines 354-388) — AUTHORITATIVE dropdown usage in `<GameViewActions>`:
```tsx
<DropdownMenu>
  <DropdownMenuTrigger asChild>
    <button type="button" aria-label="Actions" className="...current trigger classes...">
      <Icon.more size={15} className="text-apex-ink-6" />
    </button>
  </DropdownMenuTrigger>
  <DropdownMenuContent align="end" className="min-w-[160px]">
    <DropdownMenuItem onSelect={onEdit}>
      <Icon.edit size={13} className="mr-2" />
      Edit game
    </DropdownMenuItem>
    <DropdownMenuSeparator />
    <DropdownMenuItem onSelect={onDelete} className="text-[#e63946] focus:bg-[#fff5f5] focus:text-[#e63946]">
      <Icon.trash size={13} className="mr-2" />
      Delete game
    </DropdownMenuItem>
  </DropdownMenuContent>
</DropdownMenu>
```

From apps/client/src/pages/game-view.tsx (current, 669 LOC) — slicing map (RESEARCH §Pattern 5 + PATTERNS.md):
- Lines 35-73: `FormatChip` — leaf; goes INTO `game-view-fields.tsx` as local helper.
- Lines 75-169: `ActionsDropdown` — hand-rolled, ENTIRE replace with Radix in `game-view-actions.tsx`.
- Lines 85-92: hand-rolled click-outside `useEffect(addEventListener('mousedown'))` — DELETE (Radix handles).
- Lines 171-204: `FieldItem` — leaf; goes INTO `game-view-fields.tsx`.
- Lines 206-212: `SectionLabel` — leaf; goes INTO `game-view-fields.tsx`.
- Lines 214-229: `GameViewPage` — keep AS-IS in `game-view.tsx` (query + error gate).
- Lines 231-668: `GameViewBody` — SHRINK to ~150 LOC orchestrator; extracted into 3 children.
- Lines 306-357: header bar JSX → `game-view-header.tsx`.
- Lines 342-352: wishlist "Move to collection" Button + lines 75-169 ActionsDropdown → `game-view-actions.tsx`.
- Lines 361-651: left panel (cover/badges/format/UploadCoverButton) + right panel (`<dl>` grid + Notes textarea) → `game-view-fields.tsx`.
- Lines 250-255: `notesRef` autosize effect → MOVE INTO `game-view-fields.tsx` (co-located with textarea).
- Line 285: pre-existing `alert('Failed to delete: ...')` → PRESERVE AS-IS (out of scope; defer to v2).

From apps/client/src/components/icons.tsx (current state):
- Lines 11-25: `svg(children, vb = '0 0 16 16')` factory — USE THIS for new entries.
- Lines 451-479: `trash: svg(...)` — REUSE (FE-03 dropdown delete icon).
- Lines 103-120: `coffee: svg(...)` — style precedent for double-stroke icons (use for `disc`).
- Convention: 16x16 viewBox, stroke-width 1.4, `currentColor` stroke, `fill="none"` (RESEARCH §Pattern 6 + PATTERNS.md `### icons.tsx`).

From apps/client/src/pages/game-view.tsx inline SVGs (verbatim, with line refs):
- Line 39-42 (disc icon, FormatChip): `<svg width="10" height="10" viewBox="0 0 24 24" fill="none">` + two concentric circles `<circle cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="1.8" /><circle cx="12" cy="12" r="4" stroke="currentColor" strokeWidth="1.8" />` (or similar — read source verbatim).
- Line 44-68 (download/cloud icon, FormatChip): polyline + line + path — adapt to 16x16 stroke-1.4.
- Line 117-130 (pencil/edit icon, dropdown Edit item): pencil over square — adapt.
- Line 142-162 (trash icon, dropdown Delete item): `Icon.trash` exists; REUSE, don't duplicate.

Source viewBox in current code is `0 0 24 24` stroke 1.8 (24x24) — MUST normalize to `0 0 16 16` stroke 1.4 (16x16) for registry consistency. This is "anti-pattern" flagged in PATTERNS.md `### icons.tsx` and RESEARCH §Anti-Patterns.

From RESEARCH §Pattern 5 'Risk' decision:
- Cover image's `style={{ background: color-mix(...) }}` (line 364) tightly coupled to `liveCoverColor`.
- **DECISION:** put left-panel inside `<GameViewFields>` (cover IS a field — editable via `CoverColorPicker` + `UploadCoverButton`). Three-component target preserved per SC4 wording.

From apps/client/src/pages/settings/settings-page.tsx:17-45 — orchestrator pattern to mirror for `GameViewBody`:
- Owns query + global state (session, useState for editMode/dialogs).
- Renders composition of named sub-components (`<AccountSection>`, `<SecuritySection>`, etc.).
- Field-level state lives in leaves.

From apps/client/src/hooks/use-game-draft.ts (used by current game-view.tsx):
- Returns `{ draft, set, reset, toPayload }`.
- `set` is `<K extends keyof GameDraft>(key: K, value: GameDraft[K]) => void`.
- Stable callbacks (useCallback inside) — safe to prop-drill into children (RESEARCH §Pitfall 8: accept re-renders; do not premature memoize).

From apps/client/components.json:
- Style: `new-york`. Base color: `neutral`. shadcn primitives go to `components/ui/`.
- Aliases: `@/components`, `@/lib/utils`, `@/hooks`, etc.

From CLAUDE.md UI-language rule + Brand:
- Existing copy w `game-view.tsx` jest po angielsku (game-view.tsx is mature; preserve copy exactly during refactor — do NOT polonize during decomposition).
- Code/comments: English.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Zainstaluj shadcn dropdown-menu primitive (+ workspace path cleanup)</name>
  <files>apps/client/src/components/ui/dropdown-menu.tsx</files>
  <read_first>
    - apps/client/components.json (potwierdź style: new-york, base-color: neutral)
    - apps/client/src/components/ui/alert-dialog.tsx (cały — primary analog dla shadcn wrapper)
    - .planning/STATE.md (sekcja "Plan 01-01 shadcn CLI workspace quirk" — `mv` + `rm -rf` cleanup)
    - .planning/phases/04-frontend-stability/04-RESEARCH.md §Standard Stack lines 52-63 (CLI commands) + §Pitfall 5 (path quirk)
    - .planning/phases/04-frontend-stability/04-PATTERNS.md `### dropdown-menu.tsx` (style drift note: Biome ignores `components/ui/**`)
  </read_first>
  <action>
    Single primitive install. Wykonaj DOKŁADNIE poniższe kroki, NIE skróty:

    1. **Pre-flight check** — upewnij się że plik docelowy NIE istnieje:
       ```bash
       test ! -f apps/client/src/components/ui/dropdown-menu.tsx
       ```
       (Jeśli istnieje — STOP, sprawdź dlaczego, nie nadpisuj.)

    2. **Pre-flight check 2** — upewnij się że `apps/client/@/` NIE istnieje (śmieci po poprzednim CLI):
       ```bash
       test ! -d apps/client/@
       ```

    3. **Uruchom shadcn CLI z folderu `apps/client/`** (NIE z repo root — RESEARCH §Pitfall 5 + STATE.md):
       ```bash
       cd apps/client &amp;&amp; npx shadcn@latest add dropdown-menu
       ```
       (CLI może zapytać o config — `components.json` istnieje w `apps/client/`, więc CLI ma context. Jeśli zapyta o overwrite — wybierz NO; nie powinno bo plik nie istnieje.)

    4. **Workspace path cleanup** (RESEARCH §Pitfall 5 + STATE.md Plan 01-01 decision):
       ```bash
       cd /Users/kodari/projects/games &amp;&amp;
       if [ -f apps/client/@/components/ui/dropdown-menu.tsx ]; then
         mv apps/client/@/components/ui/dropdown-menu.tsx apps/client/src/components/ui/dropdown-menu.tsx &amp;&amp;
         rm -rf apps/client/@
       fi
       ```
       (Jeśli CLI w nowszej wersji już writes do `src/components/ui/` bezpośrednio — `mv` no-op'em jest pominięty przez `if`. Pozwalamy obu trybom przejść.)

    5. **Verify final state:**
       - Plik istnieje pod `apps/client/src/components/ui/dropdown-menu.tsx`.
       - Folder `apps/client/@/` NIE istnieje (lub jest pusty i można go usunąć).
       - Plik zawiera `DropdownMenuPrimitive` import z `@radix-ui/react-dropdown-menu` (oznacza że CLI poprawnie wygenerował shadcn wrapper).
       - Plik exportuje (named, NIE default): `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuSeparator` (minimum — może być więcej: Sub, Portal, Group, etc.).

    6. **TypeScript pass:**
       ```bash
       cd apps/client &amp;&amp; bunx tsc -b --noEmit
       ```

    **Zakazy:**
    - NIE pisz pliku ręcznie zamiast użycia CLI — shadcn CLI emituje canonical version dopasowaną do `components.json` style + base color. Ręczne pisanie jest off-spec.
    - NIE reformatuj wygenerowanego pliku (Biome ma `components/ui/**` w ignore — RESEARCH §Pattern 4 / PATTERNS.md style drift note). Double quotes + missing trailing commas są EXPECTED.
    - NIE dodawaj `@radix-ui/react-dropdown-menu` do `package.json` — jest tam już (`^2.1.2` per RESEARCH §Standard Stack). Jeśli CLI próbuje go bumpować — odrzuć (we'd be drifting from project pin).
    - NIE wymyślaj alternatywnego importu path (`@/components/ui/dropdown-menu` jest finalny path).

    **Pitfall awareness:**
    - Jeśli `npx shadcn@latest` failuje (network or CLI version mismatch) — fallback per RESEARCH §Environment Availability: można copy `dropdown-menu.tsx` manualnie z shadcn docs (~80 LOC of forwardRef wrappers using `@radix-ui/react-dropdown-menu`). Reference: `apps/client/src/components/ui/alert-dialog.tsx` — identyczna struktura (Root/Trigger/Content forwardRef wrappers).
  </action>
  <verify>
    <automated>
      cd /Users/kodari/projects/games &amp;&amp;
      test -f apps/client/src/components/ui/dropdown-menu.tsx &amp;&amp;
      test ! -d apps/client/@ &amp;&amp;
      grep -q "DropdownMenuPrimitive" apps/client/src/components/ui/dropdown-menu.tsx &amp;&amp;
      grep -q "@radix-ui/react-dropdown-menu" apps/client/src/components/ui/dropdown-menu.tsx &amp;&amp;
      grep -q "DropdownMenu" apps/client/src/components/ui/dropdown-menu.tsx &amp;&amp;
      grep -q "DropdownMenuTrigger" apps/client/src/components/ui/dropdown-menu.tsx &amp;&amp;
      grep -q "DropdownMenuContent" apps/client/src/components/ui/dropdown-menu.tsx &amp;&amp;
      grep -q "DropdownMenuItem" apps/client/src/components/ui/dropdown-menu.tsx &amp;&amp;
      grep -q "DropdownMenuSeparator" apps/client/src/components/ui/dropdown-menu.tsx &amp;&amp;
      ! grep -q "export default" apps/client/src/components/ui/dropdown-menu.tsx &amp;&amp;
      cd apps/client &amp;&amp; bunx tsc -b --noEmit
    </automated>
  </verify>
  <done>
    `apps/client/src/components/ui/dropdown-menu.tsx` istnieje. Eksportuje minimum: DropdownMenu, DropdownMenuTrigger, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator (named only). Folder `apps/client/@/` nie istnieje. TypeScript pass. `@radix-ui/react-dropdown-menu` w package.json niezmieniony.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rozszerz icons.tsx o disc, download, edit (16x16, stroke-1.4)</name>
  <files>apps/client/src/components/icons.tsx</files>
  <read_first>
    - apps/client/src/components/icons.tsx (cały — z naciskiem na lines 11-25 factory, 103-120 `coffee` style precedent, 451-479 `trash` style precedent)
    - apps/client/src/pages/game-view.tsx (lines 35-73 dla disc/download source SVG, lines 117-162 dla edit/trash source SVG)
    - .planning/phases/04-frontend-stability/04-RESEARCH.md §Pattern 6 (lines 433-447 source SVG inventory) + §Open Question 4 (hand-roll vs lucide — decision: hand-roll)
    - .planning/phases/04-frontend-stability/04-PATTERNS.md `### icons.tsx` (convention: 16x16, stroke-1.4, currentColor, fill="none")
  </read_first>
  <action>
    Dodaj **trzy** nowe entries do `Icon` registry. Każdy używa istniejącego `svg(children, vb)` factory (linia 11-25). Zachowuje konwencje: 16x16 viewBox (default), `stroke="currentColor"`, `stroke-width="1.4"`, `fill="none"`.

    1. **Read current `game-view.tsx` SVG paths verbatim** — przed konwersją obejrzyj LITERALNIE które elementy SVG są w source (linie 39-42, 44-68, 117-130). Nie zgaduj path data — kopiuj literal `d=...` strings i adaptuj viewBox/stroke-width.

    2. **`Icon.disc`** — dwa koncentryczne okręgi (CD/dysk). Source: `game-view.tsx:39-42` (24x24 stroke-1.8). Cel:
       ```tsx
       disc: svg(
         <>
           <circle cx="8" cy="8" r="6.3" stroke="currentColor" strokeWidth="1.4" fill="none" />
           <circle cx="8" cy="8" r="2.2" stroke="currentColor" strokeWidth="1.4" fill="none" />
         </>,
       ),
       ```
       (Jeśli source ma inne radii — adaptuj proporcjonalnie z 24x24 do 16x16: scale factor 16/24 ≈ 0.667. Originaly r=10 → 6.67 ≈ 6.3; r=4 → 2.67 ≈ 2.2. Środek (12,12) → (8,8). Patrz `coffee` (icons.tsx:103-120) jako wzór dwu-elementowej ikony stroke-based.)

    3. **`Icon.download`** — cloud-down / arrow-down icon. Source: `game-view.tsx:44-68`. Cel: download arrow (line + arrowhead) ALBO cloud-down jeśli source jest cloud-shaped. Read source verbatim, decide. Przykład jeśli to download arrow:
       ```tsx
       download: svg(
         <>
           <path d="M8 2v8" stroke="currentColor" strokeWidth="1.4" strokeLinecap="round" />
           <path d="M4.5 7l3.5 3.5 3.5-3.5" stroke="currentColor" strokeWidth="1.4" strokeLinecap="round" strokeLinejoin="round" fill="none" />
           <path d="M3 13h10" stroke="currentColor" strokeWidth="1.4" strokeLinecap="round" />
         </>,
       ),
       ```
       (Adaptacja zależy od literal path data w source. Cel: rozpoznawalna ikona "download/digital", 16x16, stroke-1.4. Patrz `Icon.trash` strukturę: `<>...</>` fragment with multiple `<path>` / `<circle>` / `<line>` elements.)

    4. **`Icon.edit`** — pencil over square. Source: `game-view.tsx:117-130`. Cel:
       ```tsx
       edit: svg(
         <>
           <path d="M2.5 13.5h11" stroke="currentColor" strokeWidth="1.4" strokeLinecap="round" />
           <path d="M11.2 3.5l1.3 1.3-7.5 7.5H3.7v-1.3l7.5-7.5z" stroke="currentColor" strokeWidth="1.4" strokeLinejoin="round" fill="none" />
         </>,
       ),
       ```
       (Klasyczna pencil shape — ząb + linia podstawy. Adaptuj path data z source 24x24 → 16x16.)

    5. **Position w registry** — dodaj alfabetycznie wśród istniejących entries (lub na końcu listy, blisko `Icon.trash` per PATTERNS.md guidance — `trash` jest line 451-479, więc `disc`/`download`/`edit` przed nim alphabetically lub adjacent). NIE refactor existing entries; tylko 3 dodania.

    6. **NIE dodawaj `Icon.trash`** — already exists (line 451-479). REUSE w Task 4.

    7. **NIE używaj lucide-react** dla tych 3 ikon (RESEARCH §Open Question 4: rekomendacja hand-roll matching dominant pattern; lucide-react reserved tylko dla `Heart` która już używa go).

    **Zakazy:**
    - NIE zmieniaj signatury `svg(children, vb)` factory ani istniejących entries.
    - NIE używaj viewBox `0 0 24 24` (dryf z konwencji — registry to 16x16; PATTERNS.md flag).
    - NIE używaj stroke-width 1.8 (dryf — registry to 1.4).
    - NIE używaj `fill="currentColor"` na path'ach które są outlinem (stroke-only — `fill="none"`).
    - NIE wprowadzaj `import` z `lucide-react` na nowe ikony.
    - NIE rób inline `<svg>` w samych komponentach Phase 4 (FE-05 — wszystko przez registry).

    **Sanity check:**
    - Otwórz `apps/client/src/components/icons.tsx` w trybie dev (`bun run dev`) i odwiedź dowolną stronę używającą `Icon.trash` — istniejąca ikona MUSI dalej renderować się normalnie. To regression check, że nie zepsułeś factory.
  </action>
  <verify>
    <automated>
      cd /Users/kodari/projects/games &amp;&amp;
      grep -q "disc:" apps/client/src/components/icons.tsx &amp;&amp;
      grep -q "download:" apps/client/src/components/icons.tsx &amp;&amp;
      grep -q "edit:" apps/client/src/components/icons.tsx &amp;&amp;
      grep -q "trash:" apps/client/src/components/icons.tsx &amp;&amp;
      # Nowe entries używają svg() factory:
      grep -E "disc:\s*svg\(" apps/client/src/components/icons.tsx &amp;&amp;
      grep -E "download:\s*svg\(" apps/client/src/components/icons.tsx &amp;&amp;
      grep -E "edit:\s*svg\(" apps/client/src/components/icons.tsx &amp;&amp;
      # Brak strokeWidth="1.8" (dryf z konwencji):
      ! grep -E 'strokeWidth="1\.8"' apps/client/src/components/icons.tsx &amp;&amp;
      cd apps/client &amp;&amp; bunx tsc -b --noEmit
    </automated>
  </verify>
  <done>
    `icons.tsx` zawiera trzy nowe entries `disc`, `download`, `edit` (każdy używa `svg(...)` factory, 16x16, stroke-1.4, fill="none"). Istniejące `trash` niezmienione. Żaden nowy entry nie używa `strokeWidth="1.8"` (dryf z konwencji). TypeScript pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Utwórz GameViewHeader (siostra-folder split)</name>
  <files>apps/client/src/components/game-view/game-view-header.tsx</files>
  <read_first>
    - apps/client/src/pages/game-view.tsx (lines 306-357 — current header bar JSX: SidebarTrigger, breadcrumb, edit-mode CTAs)
    - apps/client/src/components/layout/sidebar.tsx (potwierdź `SidebarTrigger` named export)
    - apps/client/src/pages/settings/settings-page.tsx (lines 47-62 — AccountSection precedent: props-only sub-component)
    - .planning/phases/04-frontend-stability/04-RESEARCH.md §Pattern 5 (lines 392-431 decomposition map) + §Code Examples → "Decomposed game-view child signature (SC4)" (lines 626-662 — full GameViewHeader signature)
    - .planning/phases/04-frontend-stability/04-PATTERNS.md `### game-view-header.tsx` (props interface from RESEARCH lines 632-642)
  </read_first>
  <action>
    Utwórz `apps/client/src/components/game-view/game-view-header.tsx`. Konwencje: kebab-case file, named exports only, single quotes, semicolons, trailing commas (Biome).

    1. **Imports** (minimum):
       ```tsx
       import { SidebarTrigger } from '@/components/ui/sidebar';
       ```
       Dodaj inne tylko jeśli używane w JSX (np. `cn` z `@/lib/utils` jeśli klasy są dynamiczne — sprawdź obecny source).

    2. **Props interface — `GameViewHeaderProps`** (verbatim z RESEARCH lines 632-642, w angielsku, kebab-case-aware nazwa typu):
       ```tsx
       export interface GameViewHeaderProps {
         backPath: string;
         backLabel: string;
         liveTitle: string;
         editMode: boolean;
         isSaving: boolean;
         onCancel: () => void;
         onSave: () => void;
         onNavigate: (to: string) => void;
       }
       ```

    3. **Komponent** — extract literal JSX z `game-view.tsx:306-357` do `<GameViewHeader>`. Substytucje literalów na props:
       - Hard-coded `backPath` na `props.backPath` (np. obecnie `/games` → `props.backPath`).
       - Hard-coded `backLabel` (np. obecnie `'Games'` → `props.backLabel`).
       - `liveTitle` z bieżącego stanu draft → `props.liveTitle`.
       - `editMode` boolean → `props.editMode`.
       - `isSaving` z `useUpdateGameMutation` → `props.isSaving`.
       - `onClick` na Cancel button → `props.onCancel`.
       - `onClick` na Save button → `props.onSave`.
       - `onClick` na breadcrumb back → `props.onNavigate(props.backPath)`.

    4. **Signature** (verbatim z RESEARCH lines 643-662, z TSDoc nad funkcją):
       ```tsx
       /**
        * Top bar of GameViewPage: SidebarTrigger + breadcrumb (backLabel · liveTitle) +
        * edit-mode CTA cluster (Cancel / Save).
        *
        * Stateless leaf: receives data and callbacks; renders.
        */
       export function GameViewHeader({
         backPath,
         backLabel,
         liveTitle,
         editMode,
         isSaving,
         onCancel,
         onSave,
         onNavigate,
       }: GameViewHeaderProps) {
         return (
           <div className="flex h-[52px] shrink-0 items-center gap-3 border-b border-apex-line-3 bg-white px-4 lg:px-5">
             <SidebarTrigger className="shrink-0 text-apex-ink-3 hover:text-apex-ink" />
             <nav className="flex min-w-0 items-center gap-[6px] text-[13px]">
               <button
                 type="button"
                 onClick={() => onNavigate(backPath)}
                 className="shrink-0 font-medium text-apex-accent hover:underline"
               >
                 {backLabel}
               </button>
               <span className="shrink-0 text-apex-line-1 select-none">·</span>
               <span className="truncate text-apex-ink-3">{liveTitle}</span>
             </nav>
             {editMode && (
               <div className="ml-auto flex shrink-0 items-center gap-2">
                 <button
                   type="button"
                   onClick={onCancel}
                   className="...current Cancel className from game-view.tsx..."
                 >
                   Cancel
                 </button>
                 <button
                   type="button"
                   onClick={onSave}
                   disabled={isSaving}
                   className="...current Save className from game-view.tsx..."
                 >
                   {isSaving ? 'Saving…' : 'Save'}
                 </button>
               </div>
             )}
           </div>
         );
       }
       ```
       **WAŻNE:** className strings dla Cancel/Save buttons muszą być SKOPIOWANE LITERALNIE z `game-view.tsx:306-357` — nie wymyślaj nowych. Verbatim copy zachowuje wygląd 1:1.

    5. **LOC budget:** target ~80 LOC (RESEARCH §Pattern 5). Hard cap: 250 LOC (FE-04). Jeśli przekraczasz 250 — re-check, czy nie wciągnąłeś więcej niż lines 306-357 z source'a.

    **Zakazy:**
    - NIE inline `<svg>` w header'ze — jeśli ikony są używane (np. inside `SidebarTrigger`), to są już w `Icon` registry.
    - NIE wprowadzaj wewnętrznego state (np. `useState`) w GameViewHeader — leaf stateless (RESEARCH §Pattern 5 + PATTERNS.md `### game-view-header.tsx`).
    - NIE używaj mutations bezpośrednio — Save/Cancel handlers przychodzą jako props.
    - NIE zmieniaj copy (`Cancel`, `Save`, `Saving…`) — current strings preserve.
    - NIE refactor `SidebarTrigger` — używaj jako jest.
    - NIE dodawaj `export default` (named only — CLAUDE.md).
  </action>
  <verify>
    <automated>
      cd /Users/kodari/projects/games &amp;&amp;
      test -f apps/client/src/components/game-view/game-view-header.tsx &amp;&amp;
      grep -q "export function GameViewHeader" apps/client/src/components/game-view/game-view-header.tsx &amp;&amp;
      grep -q "export interface GameViewHeaderProps" apps/client/src/components/game-view/game-view-header.tsx &amp;&amp;
      grep -q "SidebarTrigger" apps/client/src/components/game-view/game-view-header.tsx &amp;&amp;
      ! grep -q "useState\|useEffect\|useMutation" apps/client/src/components/game-view/game-view-header.tsx &amp;&amp;
      ! grep -q "&lt;svg" apps/client/src/components/game-view/game-view-header.tsx &amp;&amp;
      ! grep -q "export default" apps/client/src/components/game-view/game-view-header.tsx &amp;&amp;
      [ "$(wc -l &lt; apps/client/src/components/game-view/game-view-header.tsx)" -lt 250 ] &amp;&amp;
      cd apps/client &amp;&amp; bunx tsc -b --noEmit
    </automated>
  </verify>
  <done>
    Plik istnieje, `<250 LOC, named export GameViewHeader + GameViewHeaderProps`. Brak `useState`/`useEffect`/`useMutation` (stateless leaf). Brak inline `<svg>`. TypeScript pass.
  </done>
</task>

<task type="auto">
  <name>Task 4: Utwórz GameViewActions z Radix DropdownMenu (replace hand-rolled)</name>
  <files>apps/client/src/components/game-view/game-view-actions.tsx</files>
  <read_first>
    - apps/client/src/pages/game-view.tsx (lines 75-169 — ActionsDropdown — całość; lines 342-352 — wishlist "Move to collection" button)
    - apps/client/src/components/ui/dropdown-menu.tsx (świeżo utworzony w Task 1 — verify exports match expectations)
    - apps/client/src/components/icons.tsx (potwierdź `Icon.edit`, `Icon.trash`, `Icon.more` exist — `Icon.edit` świeżo dodany w Task 2; `Icon.trash` already exists; `Icon.more` w current registry)
    - apps/client/src/pages/settings/settings-page.tsx (lines 192-229 PasswordRow — leaf with internal useState precedent)
    - .planning/phases/04-frontend-stability/04-RESEARCH.md §Pattern 4 (lines 354-388 dropdown usage) + §Anti-Patterns line 456 (delete hand-rolled click-outside)
    - .planning/phases/04-frontend-stability/04-PATTERNS.md `### game-view-actions.tsx` (props interface lines 245-253, source extraction guidance)
  </read_first>
  <action>
    Utwórz `apps/client/src/components/game-view/game-view-actions.tsx`. To jest komponent, który łączy wishlist "Move" button + Radix DropdownMenu (Edit/Delete).

    1. **Imports:**
       ```tsx
       import {
         DropdownMenu,
         DropdownMenuContent,
         DropdownMenuItem,
         DropdownMenuSeparator,
         DropdownMenuTrigger,
       } from '@/components/ui/dropdown-menu';
       import { Icon } from '@/components/icons';
       ```
       (Plus inne potrzebne z source: `Button` jeśli używany na "Move" button — sprawdź `game-view.tsx:342-352` literal.)

    2. **Props interface — `GameViewActionsProps`** (z PATTERNS.md lines 245-253):
       ```tsx
       export interface GameViewActionsProps {
         kind: 'owned' | 'wishlist';
         isMovePending: boolean;
         onMove: () => void;
         onEdit: () => void;
         onDelete: () => void;
       }
       ```

    3. **Komponent — verbatim z RESEARCH §Pattern 4 lines 362-388, z props wired:**
       ```tsx
       /**
        * Action cluster on GameViewPage top bar:
        * - Wishlist games: "Move to collection" CTA + dropdown.
        * - Owned games: dropdown only (Edit / Delete).
        *
        * Replaces the hand-rolled ActionsDropdown (game-view.tsx:75-169) with Radix DropdownMenu —
        * keyboard nav (Tab/Arrow/Escape), click-outside, focus return, role="menu" handled by Radix.
        */
       export function GameViewActions({
         kind,
         isMovePending,
         onMove,
         onEdit,
         onDelete,
       }: GameViewActionsProps) {
         return (
           <div className="flex shrink-0 items-center gap-2">
             {kind === 'wishlist' && (
               <button
                 type="button"
                 onClick={onMove}
                 disabled={isMovePending}
                 className="...current Move className from game-view.tsx:342-352..."
               >
                 {isMovePending ? 'Moving…' : 'Move to collection'}
               </button>
             )}
             <DropdownMenu>
               <DropdownMenuTrigger asChild>
                 <button
                   type="button"
                   aria-label="Actions"
                   className="flex h-8 w-8 items-center justify-center rounded-[7px] border border-apex-line-4 bg-white hover:bg-apex-surface-hover"
                 >
                   <Icon.more size={15} className="text-apex-ink-6" />
                 </button>
               </DropdownMenuTrigger>
               <DropdownMenuContent align="end" sideOffset={4} className="min-w-[160px]">
                 <DropdownMenuItem onSelect={onEdit}>
                   <Icon.edit size={13} className="mr-2" />
                   Edit game
                 </DropdownMenuItem>
                 <DropdownMenuSeparator />
                 <DropdownMenuItem
                   onSelect={onDelete}
                   className="text-[#e63946] focus:bg-[#fff5f5] focus:text-[#e63946]"
                 >
                   <Icon.trash size={13} className="mr-2" />
                   Delete game
                 </DropdownMenuItem>
               </DropdownMenuContent>
             </DropdownMenu>
           </div>
         );
       }
       ```

    4. **CRITICAL — anti-pattern do USUNIĘCIA z source:**
       - Hand-rolled click-outside `useEffect(addEventListener('mousedown'))` (game-view.tsx:85-92) — NIE kopiuj. Radix Portal handles outside-click natively (RESEARCH §Anti-Patterns line 456 + §Don't Hand-Roll).
       - Hand-rolled `useState(open)` + `tabIndex` + Escape handler na `<div>` wrapper — NIE kopiuj. Radix handles wszystko.
       - Inline `<svg>` z game-view.tsx:117-130 (pencil/edit) → już migrated do `Icon.edit` w Task 2. Inline `<svg>` z game-view.tsx:142-162 (trash) → REUSE `Icon.trash`.

    5. **Move button copy** — sprawdź literal w `game-view.tsx:342-352` ("Move to collection" wydaje się prawdopodobny, ale verbatim z source). Zachowaj literal copy z `isMovePending` ternary.

    6. **className na Move button** — verbatim copy z `game-view.tsx:342-352`. To może być `<Button>` z `@/components/ui/button` — jeśli tak, użyj `<Button>`.

    7. **`Icon.more`** — sprawdź czy istnieje w current `icons.tsx`. Jeśli tak — używaj. Jeśli nie — fallback na lucide-react `MoreHorizontal` lub adaptuj sentinel (`...`) ikonę. (Sprawdź `read_first` results.)

    **Zakazy:**
    - NIE wracaj do hand-rolled click-outside / Escape / focus — Radix to robi.
    - NIE używaj `onClick` na `DropdownMenuItem` — używaj `onSelect` (canonical Radix API per RESEARCH §Pattern 4).
    - NIE inline `<svg>` — wszystko przez `Icon.*`.
    - NIE wprowadzaj `useState(open)` ani innego ręcznego open/close state — Radix DropdownMenu jest self-managing.
    - NIE zmieniaj copy "Edit game" / "Delete game" — current strings z RESEARCH §Pattern 4 (kopiuj literal z game-view.tsx jeśli się różnią).
    - NIE wprowadzaj `useEffect` w `GameViewActions` — leaf bez side effects.
    - NIE dodawaj `export default`.

    **Pitfall awareness (RESEARCH §Pitfall 3):**
    - Jeśli dropdown w nowym layoucie pozycjonuje się off-screen — header bar może mieć `transform` ancestor; dodaj `<DropdownMenuContent collisionPadding={8} sideOffset={4}>`. Verified safe per RESEARCH §Pitfall 3 ale potwierdź manualnie.
  </action>
  <verify>
    <automated>
      cd /Users/kodari/projects/games &amp;&amp;
      test -f apps/client/src/components/game-view/game-view-actions.tsx &amp;&amp;
      grep -q "export function GameViewActions" apps/client/src/components/game-view/game-view-actions.tsx &amp;&amp;
      grep -q "export interface GameViewActionsProps" apps/client/src/components/game-view/game-view-actions.tsx &amp;&amp;
      grep -q "from '@/components/ui/dropdown-menu'" apps/client/src/components/game-view/game-view-actions.tsx &amp;&amp;
      grep -q "DropdownMenu" apps/client/src/components/game-view/game-view-actions.tsx &amp;&amp;
      grep -q "Icon.edit" apps/client/src/components/game-view/game-view-actions.tsx &amp;&amp;
      grep -q "Icon.trash" apps/client/src/components/game-view/game-view-actions.tsx &amp;&amp;
      grep -q "onSelect={onEdit}" apps/client/src/components/game-view/game-view-actions.tsx &amp;&amp;
      grep -q "onSelect={onDelete}" apps/client/src/components/game-view/game-view-actions.tsx &amp;&amp;
      # NO hand-rolled click-outside:
      ! grep -E "addEventListener\\(['\"]mousedown" apps/client/src/components/game-view/game-view-actions.tsx &amp;&amp;
      ! grep -q "useState" apps/client/src/components/game-view/game-view-actions.tsx &amp;&amp;
      ! grep -q "useEffect" apps/client/src/components/game-view/game-view-actions.tsx &amp;&amp;
      ! grep -q "&lt;svg" apps/client/src/components/game-view/game-view-actions.tsx &amp;&amp;
      [ "$(wc -l &lt; apps/client/src/components/game-view/game-view-actions.tsx)" -lt 250 ] &amp;&amp;
      cd apps/client &amp;&amp; bunx tsc -b --noEmit
    </automated>
  </verify>
  <done>
    Plik istnieje, <250 LOC, `<GameViewActions>` używa Radix DropdownMenu (`DropdownMenuTrigger asChild`, `DropdownMenuItem onSelect`, `DropdownMenuSeparator`). Brak `useState`/`useEffect`/`addEventListener mousedown` (Radix handle'uje). Brak inline `<svg>`. Icon.edit + Icon.trash importowane. TypeScript pass.
  </done>
</task>

<task type="auto">
  <name>Task 5: Utwórz GameViewFields (left panel + right grid + notes textarea)</name>
  <files>apps/client/src/components/game-view/game-view-fields.tsx</files>
  <read_first>
    - apps/client/src/pages/game-view.tsx (cały — z naciskiem na lines 35-73 FormatChip, 171-204 FieldItem, 206-212 SectionLabel, 250-255 notesRef effect, 361-651 left+right panel JSX)
    - apps/client/src/hooks/use-game-draft.ts (cały — return shape `{draft, set, reset, toPayload}`)
    - apps/client/src/components/icons.tsx (potwierdź `Icon.disc`, `Icon.download` świeżo dodane w Task 2)
    - .planning/phases/04-frontend-stability/04-RESEARCH.md §Pattern 5 + §Pattern 5 Risk decision (left panel absorbed do fields) + §Pitfall 8 (memo NIE premature)
    - .planning/phases/04-frontend-stability/04-PATTERNS.md `### game-view-fields.tsx` (props interface lines 293-304)
  </read_first>
  <action>
    Największy z trzech splits — left panel (cover/badges/format/UploadCoverButton) + right panel (`<dl>` grid z `FieldItem` + Notes textarea + `notesRef` autosize effect). Target ~200 LOC, hard cap 250 LOC.

    1. **Imports** (minimum — adjust based on what current source uses):
       ```tsx
       import { useEffect, useRef, type ReactNode } from 'react';
       import { Icon } from '@/components/icons';
       import { cn } from '@/lib/utils';
       // Plus current dependencies: CoverColorPicker, UploadCoverButton, AddPlatformDialog,
       // GameDraft type, Game type, Platform type — all from current game-view.tsx imports.
       ```

    2. **Props interface — `GameViewFieldsProps`** (z PATTERNS.md lines 295-304):
       ```tsx
       export interface GameViewFieldsProps {
         game: Game;
         draft: GameDraft;
         set: <K extends keyof GameDraft>(key: K, value: GameDraft[K]) => void;
         editMode: boolean;
         platforms: Platform[];
         platformsLoading: boolean;
         onAddPlatform: () => void;
       }
       ```
       (Adjust dokładną sygnaturę `set` aby pasowała do tego co zwraca `useGameDraft` — read source.)

    3. **Co-locate lokalne sub-componenty** w tym samym pliku (RESEARCH §Pattern 5: "FieldItem, SectionLabel, FormatChip jako local sub-components OR extracts them too"). Recommendation: trzymaj w tym samym pliku, są tiny:
       - `FormatChip` — verbatim z `game-view.tsx:35-73`, ale ZAMIEŃ inline `<svg>` na `Icon.disc` / `Icon.download` (Task 2 entries).
       - `FieldItem` — verbatim z `game-view.tsx:171-204`.
       - `SectionLabel` — verbatim z `game-view.tsx:206-212`.

    4. **Główny komponent** — extract literal JSX z `game-view.tsx:361-651` do `<GameViewFields>`. Substytucje:
       - Local state references (`liveCoverColor`, `draft`, `set`, `editMode`, etc.) → z props.
       - Mutation calls → przeniesione do `GameViewBody` w Task 6; tutaj tylko render.
       - `notesRef` useRef + useEffect autosize (lines 250-255) → MOVE INTO `<GameViewFields>` (co-located z textarea per RESEARCH §Pattern 5 invariant).

    5. **Notes textarea + autosize effect:**
       ```tsx
       const notesRef = useRef<HTMLTextAreaElement>(null);
       useEffect(() => {
         const el = notesRef.current;
         if (!el) return;
         el.style.height = 'auto';
         el.style.height = `${el.scrollHeight}px`;
       }, [draft.notes, editMode]);
       ```
       (Literal verbatim z `game-view.tsx:250-255` — sprawdź exact deps array w source.)

    6. **FormatChip refactor** — eliminate inline `<svg>` blocks:
       ```tsx
       function FormatChip({ format }: { format: string | null }) {
         if (!format) return null;
         if (format === 'physical') {
           return (
             <span className="...current chip classes...">
               <Icon.disc size={10} className="..." />
               Physical
             </span>
           );
         }
         if (format === 'digital') {
           return (
             <span className="...current chip classes...">
               <Icon.download size={10} className="..." />
               Digital
             </span>
           );
         }
         return null;
       }
       ```
       (Literal copy/spec z current FormatChip — replace tylko `<svg>...</svg>` blocks na `<Icon.disc>` / `<Icon.download>`.)

    7. **Left panel preservation** — cover image, badges, format chip, UploadCoverButton, CoverColorPicker. Verbatim copy z `game-view.tsx:361-440` (approx). Zachowaj:
       - `style={{ background: color-mix(...) }}` na cover background (RESEARCH §Pattern 5 Risk — left panel absorbed).
       - All className strings literal.
       - `CoverColorPicker` + `UploadCoverButton` props wired przez `set` z props.

    8. **Right panel preservation** — `<dl>` grid z `FieldItem`'ami + `SectionLabel`'ami + notes textarea. Verbatim copy z `game-view.tsx:440-651`.

    9. **LOC budget:** target ~200 LOC. Hard cap 250 (FE-04). Jeśli przekraczasz — sprawdź: czy nie wciągnąłeś state/effects, które należą do `GameViewBody`?

    **Zakazy:**
    - NIE inline `<svg>` w `FormatChip` ani innym sub-komponencie — wszystko przez `Icon.*` (FE-05).
    - NIE wywołuj `useUpdateGameMutation`/`useDeleteGameMutation`/`useMoveToCollectionMutation` w `<GameViewFields>` — te są w `GameViewBody`. Tutaj tylko render + lokalny notes autosize.
    - NIE wywołuj `useGameDraft(game)` w `<GameViewFields>` — orchestrator (`GameViewBody`) wywołuje raz; przepływa props.
    - NIE memoizuj z `React.memo`/`useMemo` premature (RESEARCH §Pitfall 8 — accept re-renders, optymalizuj tylko jeśli measure'd performance issue).
    - NIE zmieniaj kopi UI (labels, placeholder texts).
    - NIE refactoruj `CoverColorPicker` ani `UploadCoverButton` — używaj jak są.
    - NIE dodawaj `export default`.
  </action>
  <verify>
    <automated>
      cd /Users/kodari/projects/games &amp;&amp;
      test -f apps/client/src/components/game-view/game-view-fields.tsx &amp;&amp;
      grep -q "export function GameViewFields" apps/client/src/components/game-view/game-view-fields.tsx &amp;&amp;
      grep -q "export interface GameViewFieldsProps" apps/client/src/components/game-view/game-view-fields.tsx &amp;&amp;
      grep -q "Icon.disc\|Icon.download" apps/client/src/components/game-view/game-view-fields.tsx &amp;&amp;
      ! grep -q "&lt;svg" apps/client/src/components/game-view/game-view-fields.tsx &amp;&amp;
      ! grep -q "useUpdateGameMutation\|useDeleteGameMutation\|useMoveToCollectionMutation" apps/client/src/components/game-view/game-view-fields.tsx &amp;&amp;
      ! grep -q "useGameDraft(" apps/client/src/components/game-view/game-view-fields.tsx &amp;&amp;
      [ "$(wc -l &lt; apps/client/src/components/game-view/game-view-fields.tsx)" -lt 250 ] &amp;&amp;
      cd apps/client &amp;&amp; bunx tsc -b --noEmit
    </automated>
  </verify>
  <done>
    Plik istnieje, <250 LOC, `<GameViewFields>` zawiera left panel (cover/badges/format) + right panel (`<dl>` grid + notes textarea) + autosize effect on notes textarea. `FormatChip`/`FieldItem`/`SectionLabel` jako local helpers. Brak inline `<svg>`. Brak mutations (te w `GameViewBody`). Brak `useGameDraft` call (przepływa z parenta). TypeScript pass.
  </done>
</task>

<task type="auto">
  <name>Task 6: Slim down game-view.tsx do orchestratora; usuń hand-rolled dropdown</name>
  <files>apps/client/src/pages/game-view.tsx</files>
  <read_first>
    - apps/client/src/pages/game-view.tsx (cały — to jest plik który aggressively shrinking)
    - apps/client/src/components/game-view/game-view-header.tsx (Task 3 — props interface)
    - apps/client/src/components/game-view/game-view-actions.tsx (Task 4 — props interface)
    - apps/client/src/components/game-view/game-view-fields.tsx (Task 5 — props interface)
    - apps/client/src/pages/settings/settings-page.tsx (lines 17-45 — orchestrator pattern: query + state + composition)
    - .planning/phases/04-frontend-stability/04-RESEARCH.md §Pattern 5 (lines 392-431 split map) + §Anti-Patterns "alert(...)" preservation
  </read_first>
  <action>
    Zrefaktoruj `apps/client/src/pages/game-view.tsx` z 669 LOC do <250 LOC orchestrator. Cel: `GameViewPage` (query + error gate, ~30 LOC) + `GameViewBody` (state + mutations + composition of 3 children, ~150 LOC).

    1. **KEEP AS-IS (line refs current source):**
       - Lines 214-229 `GameViewPage` — query + error gate. Bez zmian semantycznych.
       - Wszystkie use-* hooks na początku `GameViewBody`: `useGameDraft(game)`, `useState(editMode, ...)`, `useState(addPlatformOpen, ...)`, `useState(deleteDialogOpen, ...)`, `useUpdateGameMutation`, `useDeleteGameMutation`, `useMoveToCollectionMutation`, `useNavigate`, query for platforms list.
       - Logic for `handleSave`, `handleCancel`, `handleMove`, `handleDelete` — pozostaje w `GameViewBody`. Te są callbacks przekazywane do children.
       - `<DeleteConfirmDialog>` i `<AddPlatformDialog>` — render w `GameViewBody` na poziomie wrappera (POZA children).
       - Layout shell (outer `<div className="flex flex-1 flex-col h-full">` lub similar — verbatim z current source).
       - Pre-existing `alert('Failed to delete: ${err.message}')` (current line 285) — PRESERVE LITERAL. NIE zastępuj toast'em (out of scope per additional_context; defer to v2).

    2. **REMOVE / EXTRACT:**
       - `FormatChip` definition (lines 35-73) — DELETE (moved to `<GameViewFields>` in Task 5).
       - `ActionsDropdown` definition (lines 75-169) — DELETE (replaced by `<GameViewActions>` in Task 4).
       - Hand-rolled click-outside `useEffect(addEventListener('mousedown'))` (lines 85-92) — DELETE (Radix handles).
       - `FieldItem` definition (lines 171-204) — DELETE (moved to `<GameViewFields>`).
       - `SectionLabel` definition (lines 206-212) — DELETE (moved to `<GameViewFields>`).
       - `notesRef` autosize effect (lines 250-255) — DELETE (moved to `<GameViewFields>`).
       - Inline `<svg>` blocks (lines 39, 44, 117, 142) — already gone (extracted via Task 2 to Icon registry, used by Tasks 4+5).
       - Header bar JSX (lines 306-357) — replaced by `<GameViewHeader>`.
       - Body JSX (lines 361-651) — replaced by `<GameViewFields>` + `<GameViewActions>`.

    3. **ADD — composition of 3 children w `GameViewBody`:**
       ```tsx
       import { GameViewHeader } from '@/components/game-view/game-view-header';
       import { GameViewActions } from '@/components/game-view/game-view-actions';
       import { GameViewFields } from '@/components/game-view/game-view-fields';
       ```

       `GameViewBody` JSX shape:
       ```tsx
       return (
         <div className="...current outer layout classes...">
           <GameViewHeader
             backPath={game.kind === 'wishlist' ? '/wishlist' : '/games'}
             backLabel={game.kind === 'wishlist' ? 'Wishlist' : 'Games'}
             liveTitle={draft.title || game.title}
             editMode={editMode}
             isSaving={updateMutation.isPending}
             onCancel={handleCancel}
             onSave={handleSave}
             onNavigate={(to) => navigate(to)}
           />
           <div className="...current body wrapper classes...">
             {/* Actions row — może być inside header bar lub jako osobna linia */}
             <div className="ml-auto">
               <GameViewActions
                 kind={game.kind}
                 isMovePending={moveMutation.isPending}
                 onMove={handleMove}
                 onEdit={() => setEditMode(true)}
                 onDelete={() => setDeleteDialogOpen(true)}
               />
             </div>
             <GameViewFields
               game={game}
               draft={draft}
               set={set}
               editMode={editMode}
               platforms={platforms ?? []}
               platformsLoading={platformsLoading}
               onAddPlatform={() => setAddPlatformOpen(true)}
             />
           </div>
           <DeleteConfirmDialog
             open={deleteDialogOpen}
             onOpenChange={setDeleteDialogOpen}
             onConfirm={handleDelete}
           />
           <AddPlatformDialog
             open={addPlatformOpen}
             onOpenChange={setAddPlatformOpen}
             onCreated={(newPlatform) => { set('platformId', newPlatform.id); setAddPlatformOpen(false); }}
           />
         </div>
       );
       ```
       (Literal placement of `<GameViewActions>` zależy od current layout — możliwe że `<GameViewActions>` jest inside header lub jako sticky element. Sprawdź `game-view.tsx:306-357` i `:342-352` żeby zdecydować. Jedna z opcji: actions inside header right side. Druga: jako oddzielny element przed body. Wybierz opcję która wymaga MINIMUM redesign — visual parity celem.)

    4. **LOC budget:** target `GameViewBody` ~150 LOC + `GameViewPage` ~30 LOC = ~180 LOC dla całego pliku. Hard cap 250 (FE-04 SC4). Po refactorze potwierdź `wc -l apps/client/src/pages/game-view.tsx < 250`.

    5. **Visual parity** — żaden user-facing element NIE zmienia się wizualnie. To pure refactor. Class strings, copy, layout, mutation behavior, URL state — wszystko zachowane.

    6. **GREP gate dla SC5 (FE-05):**
       ```bash
       grep -c '<svg' apps/client/src/pages/game-view.tsx
       ```
       MUSI być 0 (literalne wymaganie FE-05). Po extract'ach z Task 2 nie powinno być żadnego inline svg.

    **Zakazy:**
    - NIE zmieniaj zachowania mutations — `useUpdateGameMutation`, `useDeleteGameMutation`, `useMoveToCollectionMutation` wywoływane jak dotychczas, error handling preserved.
    - NIE zastępuj `alert('Failed to delete: ...')` — out of scope (defer v2).
    - NIE redesign'uj layout — visual parity 1:1.
    - NIE memoizuj children z `React.memo` (RESEARCH §Pitfall 8).
    - NIE renderuj `<GameViewHeader>` POZA `<GameViewBody>` — keep `GameViewPage` jako thin wrapper around `GameViewBody`.
    - NIE pozostaw definicji `FormatChip`/`ActionsDropdown`/`FieldItem`/`SectionLabel` w game-view.tsx — przeniesione (would be dead code).
    - NIE pozostaw `useEffect.*mousedown` w source (auto-fail FE-03 source-pin).
    - NIE inline `<svg>` (auto-fail FE-05 source-pin).
    - NIE refactor `useGameDraft` ani jego semantics (RESEARCH §Anti-Patterns line 457: "DO NOT change semantics of the draft hook or toPayload").
  </action>
  <verify>
    <automated>
      cd /Users/kodari/projects/games &amp;&amp;
      # SC4 — file size guard:
      [ "$(wc -l &lt; apps/client/src/pages/game-view.tsx)" -lt 250 ] &amp;&amp;
      # SC5 — no inline SVG (literal FE-05 requirement):
      [ "$(grep -c '&lt;svg' apps/client/src/pages/game-view.tsx)" = "0" ] &amp;&amp;
      # SC3 — no hand-rolled mousedown click-outside:
      ! grep -E "addEventListener\\(['\"]mousedown" apps/client/src/pages/game-view.tsx &amp;&amp;
      # SC4 — composes 3 children:
      grep -q "&lt;GameViewHeader" apps/client/src/pages/game-view.tsx &amp;&amp;
      grep -q "&lt;GameViewActions" apps/client/src/pages/game-view.tsx &amp;&amp;
      grep -q "&lt;GameViewFields" apps/client/src/pages/game-view.tsx &amp;&amp;
      # SC4 — children imports point to game-view/ subfolder:
      grep -q "from '@/components/game-view/game-view-header'" apps/client/src/pages/game-view.tsx &amp;&amp;
      grep -q "from '@/components/game-view/game-view-actions'" apps/client/src/pages/game-view.tsx &amp;&amp;
      grep -q "from '@/components/game-view/game-view-fields'" apps/client/src/pages/game-view.tsx &amp;&amp;
      # Mutations preserved:
      grep -q "useUpdateGameMutation\|useDeleteGameMutation\|useMoveToCollectionMutation" apps/client/src/pages/game-view.tsx &amp;&amp;
      # useGameDraft preserved:
      grep -q "useGameDraft" apps/client/src/pages/game-view.tsx &amp;&amp;
      # All 3 new child files exist and are <250 LOC:
      [ "$(wc -l &lt; apps/client/src/components/game-view/game-view-header.tsx)" -lt 250 ] &amp;&amp;
      [ "$(wc -l &lt; apps/client/src/components/game-view/game-view-actions.tsx)" -lt 250 ] &amp;&amp;
      [ "$(wc -l &lt; apps/client/src/components/game-view/game-view-fields.tsx)" -lt 250 ] &amp;&amp;
      # Full client compile:
      cd apps/client &amp;&amp; bunx tsc -b --noEmit &amp;&amp; bunx vite build --mode development
    </automated>
  </verify>
  <done>
    `game-view.tsx` < 250 LOC. Zero inline `<svg>` (FE-05 grep-gate = 0). Zero `addEventListener('mousedown')` (FE-03). Importuje wszystkie trzy children z `@/components/game-view/`. Cztery sub-komponenty (`FormatChip`, `ActionsDropdown`, `FieldItem`, `SectionLabel`) usunięte z source (przeniesione do children). Mutations + `useGameDraft` preserved. `bunx tsc -b --noEmit` + `bunx vite build` pass. Wszystkie 3 nowe pliki w `components/game-view/` też <250 LOC.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Pełna decomposicja `game-view.tsx` (669 → ~150 LOC orchestrator + 3 nowe komponenty <250 LOC każdy), Radix DropdownMenu zastępujący hand-rolled dropdown, wszystkie inline `<svg>` w `game-view.tsx` migrated do `Icon` registry. Zachowane visual parity i mutation behavior.
  </what-built>
  <how-to-verify>
    Manualny happy-path smoke test (krytyczny — brak jsdom; cała pewność jakości tutaj):

    **A. Owned game flow:**
    1. `bun run dev`, otwórz `http://localhost:5173/games`, kliknij dowolną grę → otwiera `/games/:id` (view mode).
    2. Visual: header bar z back-breadcrumb (`Games · {tytuł}`), action button "..." w prawym górnym rogu, lewy panel z cover image (background color-mixed), prawy panel z `<dl>` grid pól (year, hours played, format, status, genre, developer, platform, purchased at, price, notes).
    3. Klik action button "..." → dropdown otwiera się. Items: "Edit game" (z `Icon.edit`), separator, "Delete game" (czerwony z `Icon.trash`).
    4. **Keyboard nav (Radix):**
       - Naciśnij `Esc` → dropdown zamyka się.
       - Klik trigger ponownie → dropdown otwarty.
       - Naciśnij `↓` (Arrow down) → focus na "Edit game".
       - `↓` → focus na "Delete game".
       - `↑` → focus wraca na "Edit game".
       - `Tab` → focus wychodzi z dropdown (zamyka).
       - Klik trigger, `Enter` na focused item → akcja wykona się (Edit toggle / Delete dialog).
    5. **Click outside:** Klik trigger, klik POZA dropdown → zamyka się (Radix Portal collision).
    6. **Role attribute (a11y check):** Open DevTools → Inspect dropdown content → element musi mieć `role="menu"` (Radix native).
    7. **Edit mode:** Klik "Edit game" → switching to edit. Header pokazuje Cancel + Save buttons. Pola w right panel są edytowalne (inputs/selects). Notes textarea autosize'uje się.
    8. **Save:** Zmień hours_played, klik Save. Network: `PATCH /api/games/:id`. UI wraca do view mode z nową wartością. Toast (jeśli sonner wired w mutations) potwierdza.
    9. **Delete:** Klik "..." → "Delete game" → AlertDialog (jeśli istnieje) lub bezpośredni delete (current behavior); po sukcesie navigate back do `/games`.

    **B. Wishlist game flow:**
    10. Otwórz `/wishlist`, klik wishlist game → `/wishlist/:id`.
    11. Visual: header back-breadcrumb (`Wishlist · {tytuł}`), action area zawiera "Move to collection" button + "..." dropdown. (Owned games NIE mają "Move to collection".)
    12. Klik "Move to collection" → przenosi do owned; navigate to `/games/:id`.
    13. Klik "..." → ten sam Edit/Delete dropdown.

    **C. Visual parity check (vs przed refactorem):**
    14. Cover image background `color-mix` working — color preview pasuje do `liveCoverColor`.
    15. FormatChip pokazuje `<Icon.disc>` dla physical i `<Icon.download>` dla digital — ikony 16x16 stroke-1.4 (nie 10x10 jak były inline; size prop steruje).
    16. Edit pencil w dropdown to `<Icon.edit>` 13x13. Trash w delete item to `<Icon.trash>` 13x13.
    17. Brak żadnych visual regressions — strona wygląda IDENTYCZNIE jak przed refactorem (bo to pure refactor).

    **D. Grep gates (potwierdzenie automatyczne):**
    18. `grep -c '<svg' apps/client/src/pages/game-view.tsx` → `0` (FE-05).
    19. `grep -c 'addEventListener.*mousedown' apps/client/src/pages/game-view.tsx` → `0` (FE-03).
    20. `wc -l apps/client/src/pages/game-view.tsx apps/client/src/components/game-view/*.tsx` → wszystkie <250 (FE-04).

    **E. Build:**
    21. `cd apps/client && bunx vite build --mode production` pass (bez warnings o missing imports).
    22. `bun test` w repo root — all tests green (regression: nothing broken).

    **Known acceptable behavior (NOT a regression):**
    - `alert('Failed to delete: ${err.message}')` przy błędzie delete — pre-existing, out of scope (defer v2).
    - StrictMode w dev może wywoływać effects double — to expected.
  </how-to-verify>
  <resume-signal>Wpisz "approved" jeśli wszystkie 22 punkty zaliczone. W przeciwnym razie opisz który scenariusz failuje (visual regression, brak keyboard nav, missing role="menu", inline svg, etc.).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Radix DropdownMenu Portal → DOM root | Radix renderuje content w `document.body` portal; collision-detection oblicza pozycję na podstawie trigger bounding-box |
| Hand-rolled DOM listeners (REMOVED) | Wcześniej: `useEffect(addEventListener('mousedown'))` jako custom click-outside; po refactorze: Radix Portal handle'uje natively, brak custom DOM listener |
| GameViewBody props → children | `set`, `onEdit`, `onDelete`, `onMove`, `onCancel`, `onSave`, `onNavigate` callback flow w dół; children nie wywołują mutations sami |
| GameView fields → notes textarea | `notesRef` autosize effect lokalny do `GameViewFields`; nie wymyka się do parenta |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering / XSS | Radix DropdownMenu renderuje user-supplied tekst w `DropdownMenuItem` | n/a | Wszystkie copy w `<GameViewActions>` to hard-coded strings ("Edit game", "Delete game"). Brak user input echoed. |
| T-04-15 | Information disclosure | Hand-rolled click-outside przepadał event-listener bez cleanup → memory leak / event-spam | mitigate | DELETE current `useEffect(addEventListener)` w `game-view.tsx:85-92`; Radix handle'uje natively z proper cleanup. Pin: `! grep -E "addEventListener.*mousedown" game-view.tsx` w verify. |
| T-04-16 | Denial of service | Radix Portal collision math mispositions w transform ancestor (RESEARCH §Pitfall 3) | mitigate | Verified safe per RESEARCH §Pitfall 3 (header bar bez transformów). Fallback: `<DropdownMenuContent collisionPadding={8} sideOffset={4}>` — dodane w Task 4 dla safety. |
| T-04-17 | Repudiation | Pre-existing `alert('Failed to delete: ...')` echoes server error message | accept | Pre-existing behavior; out of scope dla Phase 4 (defer v2). Risk: `err.message` może zawierać stack trace fragments — single-user app, akceptowalne. |
| T-04-18 | Elevation of privilege | Bypass `<ProtectedRoute>` na `/games/:id` przez direct fetch | n/a | Route still gated by `<ProtectedRoute>` w `main.tsx`. Refactor nie touch'uje route structure. |
| T-04-19 | Information disclosure | Notes textarea autosize effect leaki user input do innych komponentów | n/a | `notesRef` jest lokalny do `GameViewFields`, useRef nigdy nie expose'owany do parenta. Pin: ref nie jest forwarded. |
| T-04-20 | Spoofing | shadcn CLI ścieg ścieżki `apps/client/@/` zostawia śmieci, które mogą być importowane przez pomyłkę | mitigate | Task 1 verify: `test ! -d apps/client/@` — failuje jeśli śmieci pozostały. STATE.md decision z Plan 01-01 powtórzona. |

ASVS L1 mapping: V14 (Configuration management) — handle'owanie shadcn workspace quirk dokumentowane w STATE.md i powtarzane. V5 (Input validation) — pure refactor; brak nowych input surfaces.
</threat_model>

<verification>
- **Automated (grep gates per success criterion):**
  ```
  # FE-03 (SC3) — no hand-rolled click-outside, Radix dropdown wired:
  ! grep -E "addEventListener\\(['\"]mousedown" apps/client/src/pages/game-view.tsx
  ! grep -E "addEventListener\\(['\"]mousedown" apps/client/src/components/game-view/*.tsx
  grep -q "from '@/components/ui/dropdown-menu'" apps/client/src/components/game-view/game-view-actions.tsx

  # FE-04 (SC4) — 3 children files, each <250 LOC, game-view.tsx <250 LOC:
  test -f apps/client/src/components/game-view/game-view-header.tsx
  test -f apps/client/src/components/game-view/game-view-actions.tsx
  test -f apps/client/src/components/game-view/game-view-fields.tsx
  for f in apps/client/src/components/game-view/game-view-header.tsx \
           apps/client/src/components/game-view/game-view-actions.tsx \
           apps/client/src/components/game-view/game-view-fields.tsx \
           apps/client/src/pages/game-view.tsx; do
    n=$(wc -l < "$f"); [ "$n" -lt 250 ] || { echo "FAIL: $f = $n LOC"; exit 1; }
  done

  # FE-05 (SC5) — zero inline svg in game-view.tsx (literal requirement):
  [ "$(grep -c '<svg' apps/client/src/pages/game-view.tsx)" = "0" ]

  # New icons exist:
  grep -q "disc:" apps/client/src/components/icons.tsx
  grep -q "download:" apps/client/src/components/icons.tsx
  grep -q "edit:" apps/client/src/components/icons.tsx

  # shadcn primitive installed cleanly:
  test -f apps/client/src/components/ui/dropdown-menu.tsx
  test ! -d apps/client/@
  ```
- **TypeScript:** `cd apps/client && bunx tsc -b --noEmit` passes.
- **Build:** `cd apps/client && bunx vite build --mode production` passes.
- **Test suite:** `bun test` (z repo root) — wszystkie istniejące testy green (regression check).
- **Manualny happy-path smoke (Task 7 checkpoint):** 22 punkty z `<how-to-verify>` zaliczonych — keyboard nav, role="menu", click-outside, edit-save flow, wishlist move flow, visual parity.
- **Brak nowych devDependencies w `package.json`** (`@radix-ui/react-dropdown-menu` był już; jeśli shadcn CLI dodał coś — odrzuć / cofnij).
</verification>

<success_criteria>
ROADMAP success criteria addressed:
- **SC-3 (FE-03)**: Action dropdown w `game-view.tsx` używa `@radix-ui/react-dropdown-menu` — keyboard nav (Tab/Arrow/Escape) działa, `role="menu"` obecny w DOM, brak hand-rolled click-outside. ✓ Task 1 (primitive), Task 4 (wrap), Task 6 (delete hand-rolled). Verify: grep `! addEventListener mousedown`, manual keyboard nav check.
- **SC-4 (FE-04)**: `game-view.tsx` rozdzielony na `game-view-header.tsx`, `game-view-actions.tsx`, `game-view-fields.tsx` (każdy <250 linii); existing URL state i mutation behavior zachowany — happy-path manualny test view+edit przechodzi. ✓ Tasks 3, 4, 5 (create children), Task 6 (shrink orchestrator). Verify: `wc -l` < 250 on all 4 files, manual smoke.
- **SC-5 (FE-05)**: Inline SVG w `game-view.tsx` przeniesione do `@/components/icons.tsx` jako `<Icon.x />`; brak inline `<svg>` w `game-view.tsx`. ✓ Task 2 (3 new icon entries), Tasks 4+5 (consume), Task 6 (delete inline svg). Verify: `grep -c '<svg' game-view.tsx` = 0.

REQUIREMENTS addressed:
- **FE-03**: Hand-rolled dropdown w `game-view.tsx` zastąpiony przez `@radix-ui/react-dropdown-menu` (keyboard nav, Escape, role="menu" za darmo). ✓
- **FE-04**: `game-view.tsx` (669 linii) podzielony na komponenty: `game-view-header`, `game-view-actions`, `game-view-fields` (każdy <250 linii); zachowane existing API i URL state. ✓
- **FE-05**: Inline SVG-i w `game-view.tsx` przeniesione do `@/components/icons.tsx` jako `<Icon.name />`. ✓

Out of Scope (explicit deferrals):
- Pre-existing `alert('Failed to delete: ${err.message}')` w `game-view.tsx:285` — preserve-existing-behavior, defer v2 (per additional_context + RESEARCH §Anti-Patterns line 516). Replace with toast belongs to "polish v2" plan.
- Generalizacja split do innych pages (settings-page, games-page) — phase scope strictly `game-view`.
- Performance memoization z `React.memo` — RESEARCH §Pitfall 8: accept re-renders; optymalizuj tylko jeśli measured.
- `Icon.more` registry entry (jeśli nie istnieje już) — out of scope; jeśli current `icons.tsx` nie ma `more`, użyj `Icon.more` fallback (lub adaptuj — sprawdź w Task 4 read_first).
- Replacement `<DeleteConfirmDialog>` na shadcn `<AlertDialog>` — already exists (Phase 1 work); no change.
- Migration completely off CoverColorPicker / UploadCoverButton — out of scope.
- Polonizacja copy `game-view.tsx` (`Edit game`, `Delete game`, `Cancel`, `Save`, `Saving…`) — current copy preserved (UI lang policy: deferred to dedicated polonization phase).
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-stability/04-03-SUMMARY.md` recording:
- LOC counts before / after:
  - `apps/client/src/pages/game-view.tsx` (target 669 → <250)
  - `apps/client/src/components/game-view/game-view-header.tsx` (target <250)
  - `apps/client/src/components/game-view/game-view-actions.tsx` (target <250)
  - `apps/client/src/components/game-view/game-view-fields.tsx` (target <250)
- Czy shadcn CLI workspace path quirk wystąpił (czy `mv apps/client/@/...` był potrzebny), czy CLI w nowej wersji writes do `src/components/ui/` bezpośrednio
- Lista 3 nowych ikon w `icons.tsx` (`disc`, `download`, `edit`) + ich source viewBox/strokeWidth (potwierdzenie 16x16 / 1.4)
- Czy keyboard nav w dropdown weryfikowany (Tab/Arrow/Escape) + czy `role="menu"` widoczny w DevTools Elements
- Czy `alert('Failed to delete...')` zachowany jako pre-existing (potwierdzenie out-of-scope)
- Czy `useGameDraft(game)` zachował identyczną semantykę (`{draft, set, reset, toPayload}`)
- Czy `notesRef` autosize effect przeniesiony do `<GameViewFields>` razem z textarea (co-located)
- Any unexpected behavioral regression caught during manual smoke (22-step checkpoint) — fix or flag
</output>
</content>
</invoke>