---
phase: 04-frontend-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/src/hooks/use-credentials-form.ts
  - apps/client/src/pages/login.tsx
  - apps/client/src/pages/register.tsx
  - apps/client/src/pages/__tests__/login.test.tsx
  - apps/client/src/pages/__tests__/register.test.tsx
autonomous: true
requirements: [FE-02, FE-06]
requirements_addressed: [FE-02, FE-06]
sc_mappings: [2, 6]
mode: mvp
tags: [react, hooks, forms, formdata, uncontrolled, better-auth, regression, bun-test, source-pin]

must_haves:
  truths:
    - "Plik `apps/client/src/hooks/use-credentials-form.ts` istnieje i exportuje named `useCredentialsForm` + dwa interfaces (`UseCredentialsFormArgs`, `UseCredentialsFormReturn`). Hook jest auth-agnostic — NIE wywołuje `signIn`/`signUp`/`refetchSession` sam (RESEARCH §Pattern 3 + Critical Invariants line 317-321)."
    - "Hook zwraca `{ handleSubmit, isPending, error, fieldErrors, resetErrors }` (kształt z RESEARCH §Pattern 3 lines 258-264). `handleSubmit` jest `(e: FormEvent<HTMLFormElement>) => Promise<void>`."
    - "Hook NIE injectuje `value=`/`onChange=` w inputy — caller renderuje bare `<Input name=\"...\" />` (MEMORY: `feedback_react_autofill_uncontrolled` + RESEARCH §Pitfall 7 / §Pattern 3 invariant)."
    - "`login.tsx` i `register.tsx` używają `useCredentialsForm`; obie zawierają `await refetchSession()` PRZED `navigate(...)` (MEMORY: `feedback_better_auth_session_refetch`); brak duplikacji `new FormData(form)` bloków między oboma plikami (FE-02 wording w REQUIREMENTS.md)."
    - "Sync capture `form = e.currentTarget` i `data = new FormData(form)` PRZED jakimkolwiek `await` (RESEARCH §Pitfall 4) — całość w hooku, więc obie strony korzystają z tego samego safe captura."
    - "Validators login.tsx: brak (better-auth waliduje serwerowo). Validators register.tsx: `name` required, `password` min 8 znaków, `confirmPassword === password` (zachowane z current `register.tsx:21-57` per PATTERNS.md)."
    - "Error code mapping zachowany: `INVALID_EMAIL_OR_PASSWORD` → `'Invalid email or password.'` (login); `USER_ALREADY_EXISTS` → `fieldErrors.email` (register). Wartości zwracane przez `onSubmit` callback, nie przez hook."
    - "Source-pin testy `pages/__tests__/login.test.tsx` i `register.test.tsx` failują jeśli: (a) brakuje `await refetchSession()`, (b) `refetchSession` jest PO `navigate`, (c) `<Input>` ma `value={`, (d) brakuje `useCredentialsForm` importu, (e) brakuje `signIn.email`/`signUp.email` z `@/lib/auth-client`."
    - "`bun test apps/client/src/pages/__tests__/login.test.tsx apps/client/src/pages/__tests__/register.test.tsx` zielony."
    - "`useCredentialsForm` NIE jest generalizowane do `useForm` ani używane przez Settings password form — out of scope FE-02 (RESEARCH §Open Question 2 + §Anti-Patterns line 452)."
  artifacts:
    - path: "apps/client/src/hooks/use-credentials-form.ts"
      provides: "Uncontrolled+FormData form driver hook (auth-agnostic)"
      exports: ["useCredentialsForm", "UseCredentialsFormArgs", "UseCredentialsFormReturn", "CredentialField"]
      contains: "new FormData"
      min_lines: 50
    - path: "apps/client/src/pages/login.tsx"
      provides: "Login page using useCredentialsForm + better-auth signIn.email + refetchSession-before-navigate"
      contains: "useCredentialsForm"
    - path: "apps/client/src/pages/register.tsx"
      provides: "Register page using useCredentialsForm + better-auth signUp.email + refetchSession-before-navigate"
      contains: "useCredentialsForm"
    - path: "apps/client/src/pages/__tests__/login.test.tsx"
      provides: "FE-06 source-pin convention test for LoginPage (refetchSession-before-navigate + uncontrolled inputs)"
      contains: "describe('LoginPage source-pin convention — FE-06'"
    - path: "apps/client/src/pages/__tests__/register.test.tsx"
      provides: "FE-06 source-pin convention test for RegisterPage (refetchSession-before-navigate + uncontrolled inputs)"
      contains: "describe('RegisterPage source-pin convention — FE-06'"
  key_links:
    - from: "apps/client/src/pages/login.tsx"
      to: "apps/client/src/hooks/use-credentials-form.ts"
      via: "named import"
      pattern: "import \\{ useCredentialsForm \\} from '@/hooks/use-credentials-form'"
    - from: "apps/client/src/pages/register.tsx"
      to: "apps/client/src/hooks/use-credentials-form.ts"
      via: "named import"
      pattern: "import \\{ useCredentialsForm \\} from '@/hooks/use-credentials-form'"
    - from: "apps/client/src/pages/login.tsx (onSubmit callback body)"
      to: "navigate"
      via: "await refetchSession() PRZED navigate"
      pattern: "await refetchSession\\(\\)[\\s\\S]*?navigate\\("
    - from: "apps/client/src/pages/register.tsx (onSubmit callback body)"
      to: "navigate"
      via: "await refetchSession() PRZED navigate"
      pattern: "await refetchSession\\(\\)[\\s\\S]*?navigate\\("
    - from: "apps/client/src/pages/__tests__/login.test.tsx"
      to: "apps/client/src/pages/login.tsx"
      via: "readFileSync source-pin"
      pattern: "readFileSync\\(resolve\\(__dirname, '\\.\\./login\\.tsx'\\)"
    - from: "apps/client/src/pages/__tests__/register.test.tsx"
      to: "apps/client/src/pages/register.tsx"
      via: "readFileSync source-pin"
      pattern: "readFileSync\\(resolve\\(__dirname, '\\.\\./register\\.tsx'\\)"

---

<objective>
Wyekstrahuj wspólny driver login/register do `useCredentialsForm` (uncontrolled + FormData + pending/error/fieldErrors state) i przepisz obie page'y na hook. Hook pozostaje AUTH-AGNOSTIC — `await refetchSession()` żyje w page-level `onSubmit` callback, NIE w hooku. Następnie dodaj dwa źródłowe testy regresyjne pinujące oba MEMORY bugi: (a) `refetchSession` przed `navigate`, (b) uncontrolled inputs.

Purpose: Phase 4 SC2 (FE-02 — dedup duplikacji formData między login/register) + SC6 (FE-06 — regression tests pinujące dwa bugi z MEMORY: `feedback_better_auth_session_refetch` + `feedback_react_autofill_uncontrolled`). Cel: failujący test, gdy ktokolwiek cofnie którykolwiek fix.

Output: 1 nowy plik hooka (~70 LOC) + refaktor 2 page'y (~60 LOC każda) + 2 nowe pliki testów regresyjnych (bun:test, source-pin via readFileSync — NO jsdom).

TDD note: testy regresyjne są **deliverable per SC6**. Zacznij od nich (RED — failują bo hook jeszcze nie istnieje + pages jeszcze nie refaktorowane). Potem implement hook + refactor pages (GREEN). Refactor (jeśli potrzebny) na końcu.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-stability/04-RESEARCH.md
@.planning/phases/04-frontend-stability/04-PATTERNS.md

@apps/client/src/pages/login.tsx
@apps/client/src/pages/register.tsx
@apps/client/src/pages/settings/account-password-form.tsx
@apps/client/src/hooks/use-add-game-with-metadata.ts
@apps/client/src/hooks/use-game-draft.ts
@apps/client/src/components/auth/protected-route.test.tsx
@apps/client/src/components/add-game-modal.test.tsx
@apps/client/src/lib/auth-client.ts

<interfaces>
<!-- Key contracts the executor needs. -->

From apps/client/src/lib/auth-client.ts (state after Phase 1+2+3):
```ts
export const { signIn, signUp, signOut, useSession, changePassword, revokeSessions } = authClient;
```
- `signIn.email({ email, password })` → returns `{ error?: { code: string, message?: string } }`. Code `'INVALID_EMAIL_OR_PASSWORD'` is the canonical bad-credentials response.
- `signUp.email({ name, email, password })` → returns `{ error?: { code: string } }`. Code `'USER_ALREADY_EXISTS'` is the canonical duplicate-email response.
- `useSession()` → `{ data, isPending, refetch }`. The `refetch` is what login/register MUST await before `navigate(...)`.

From apps/client/src/pages/login.tsx (current, lines 1-89):
- Uses `useState(error)`, `useState(isPending)`, `useState(fieldErrors)` — these MOVE INTO the hook.
- `const form = e.currentTarget; const data = new FormData(form);` — sync capture pattern PRESERVED.
- Error code mapping `INVALID_EMAIL_OR_PASSWORD` PRESERVED in `onSubmit` callback (passed to hook as return value).
- `await refetchSession()` BEFORE `navigate(from, { replace: true })` PRESERVED in `onSubmit` callback.
- `from = (location.state as { from?: string } | null)?.from ?? '/games'` PRESERVED.

From apps/client/src/pages/register.tsx (current, lines 1-144):
- Uses three `useState` for error/isPending/fieldErrors — same MOVE.
- Client-side validators (lines 21-57 per PATTERNS.md):
  - `name` required (non-empty trimmed)
  - `password` min 8 znaków
  - `password === confirmPassword`
  → These become `validate` callbacks on `CredentialField[]` entries.
- `USER_ALREADY_EXISTS` → `fieldErrors.email` PRESERVED in `onSubmit` callback return value.
- `await refetchSession()` BEFORE `navigate('/games', { replace: true })` PRESERVED.

From apps/client/src/pages/settings/account-password-form.tsx (lines 37-70) — canonical sync-capture pattern to lift into hook:
```tsx
const onSubmit = async (e: React.FormEvent<HTMLFormElement>) => {
  e.preventDefault();
  const form = e.currentTarget;           // SYNC
  const data = new FormData(form);        // SYNC
  const currentPassword = String(data.get('currentPassword') ?? '');
  const newPassword = String(data.get('newPassword') ?? '');
  // ... validate before any await ...
  setIsPending(true);
  // ... await better-auth ...
};
```
This is the EXACT shape — `form = e.currentTarget` MUST be captured before any `await` (RESEARCH §Pitfall 4: React synthetic event pooling).

From apps/client/src/hooks/use-add-game-with-metadata.ts (lines 11-31, 54-58) — hook signature pattern to mirror:
- Two exported interfaces: `UseAddGameWithMetadataOptions` (args) + `UseAddGameWithMetadataResult` (return).
- Named export `export function useAddGameWithMetadata(opts): UseAddGameWithMetadataResult`.
- `useCallback` for stable callbacks (RESEARCH §Pitfall 6: be aware that `useCallback([args])` re-creates per render when caller passes object literal — accept the recreation for submit-cadence hooks).
- TSDoc block above the function explaining behavioral invariants (precedens: `use-game-draft.ts:23-35`).

From apps/client/src/components/auth/protected-route.test.tsx (1-22) — canonical source-pin test shape:
```tsx
import { describe, expect, test } from 'bun:test';
import { readFileSync } from 'node:fs';
import { resolve } from 'node:path';

describe('ProtectedRoute regression — SET-05', () => {
  const source = readFileSync(resolve(__dirname, 'protected-route.tsx'), 'utf-8');

  test('imports useSession from @/lib/auth-client', () => {
    expect(source).toMatch(/from '@\/lib\/auth-client'/);
    expect(source).toMatch(/useSession/);
  });
  // ...
});
```

From apps/client/src/components/add-game-modal.test.tsx (16-22, 24-33) — cross-folder + ordered-pin idioms:
```tsx
const hookSrc = readFileSync(
  resolve(__dirname, '../hooks/use-add-game-with-metadata.ts'),
  'utf-8',
);

// ordered-position pin:
const platformIdx = modalSrc.indexOf('>Platform<');
const titleIdx = modalSrc.indexOf('>Title<');
expect(platformIdx).toBeLessThan(titleIdx);
```
This SAME pattern (using `.search(/pattern/)` for regex-flexible position checks) drives the `refetchSession` < `navigate` assertion (RESEARCH lines 596-603).

From RESEARCH §Pattern 3 (lines 244-314) — AUTHORITATIVE hook implementation. Skopiuj signature i body 1:1; w razie wątpliwości doc'y `protected-route.test.tsx` to fakt na disku.

From RESEARCH §Pattern 3 caller shape (lines 546-583) — AUTHORITATIVE `login.tsx` after refactor.

From CLAUDE.md UI-language rule:
- UI copy w JSX: polski (akceptowalne też angielski jeśli current page już ma angielski — login.tsx ma "Welcome back" / "Sign in" — zachowaj current copy, nie zmieniaj na polski w tym planie).
- Identyfikatory, typy, komentarze, error codes: angielski.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Napisz testy regresyjne dla LoginPage + RegisterPage (RED)</name>
  <files>apps/client/src/pages/__tests__/login.test.tsx, apps/client/src/pages/__tests__/register.test.tsx</files>
  <read_first>
    - apps/client/src/components/auth/protected-route.test.tsx (cały — canonical source-pin shape)
    - apps/client/src/components/add-game-modal.test.tsx (lines 1-50 — cross-folder readFileSync + ordered indexOf)
    - apps/client/src/pages/login.tsx (current — wiedz co aktualnie tam jest, żeby zaprojektować assertions, które przejdą PO refactorze a NIE przejdą jeśli ktoś cofnie którykolwiek fix)
    - apps/client/src/pages/register.tsx (current)
    - .planning/phases/04-frontend-stability/04-RESEARCH.md §Code Examples → "Source-pin regression test (SC6)" (lines 587-624) + §Validation Architecture Phase Requirements (lines 748-760)
    - .planning/phases/04-frontend-stability/04-PATTERNS.md `### apps/client/src/pages/__tests__/login.test.tsx`
    - ~/.claude/projects/-Users-kodari-projects-games/memory/feedback_better_auth_session_refetch.md
    - ~/.claude/projects/-Users-kodari-projects-games/memory/feedback_react_autofill_uncontrolled.md
  </read_first>
  <behavior>
    Test 1 (login.test.tsx → describe 'LoginPage regression — FE-06'):
    - imports useCredentialsForm hook
    - imports signIn from @/lib/auth-client
    - imports useSession from @/lib/auth-client (dla refetch)
    - `await refetchSession()` MUSI być w source PRZED `navigate(`
    - `<Input name="email"` istnieje (uncontrolled — name= obecne)
    - `<Input name="password"` istnieje
    - `<Input name="email" ...>` NIE ma `value={...}`
    - `<Input name="password" ...>` NIE ma `value={...}`
    - źródło NIE używa `useState(...)` do trzymania wartości pól (sanity check; akceptujemy ewentualne `useState` dla innych celów jeśli istnieje — strongest signal jest absence `value={` na Input z name="email"/"password")

    Test 2 (register.test.tsx → describe 'RegisterPage regression — FE-06'):
    - imports useCredentialsForm hook
    - imports signUp from @/lib/auth-client
    - imports useSession z @/lib/auth-client
    - `await refetchSession()` PRZED `navigate(`
    - `<Input name="name"`, `<Input name="email"`, `<Input name="password"`, `<Input name="confirmPassword"` (lub odpowiedniki) wszystkie obecne
    - żaden z powyższych nie ma `value={`
  </behavior>
  <action>
    Utwórz dwa pliki `bun:test` source-pin. Konwencje (z `protected-route.test.tsx` + `add-game-modal.test.tsx`):
    - Imports z `bun:test`: `describe, expect, test`.
    - `readFileSync(resolve(__dirname, '../login.tsx'), 'utf-8')` — `__tests__/` jest siostrą `pages/`, więc `../` cofa do `pages/`.
    - Single top-level `const source = readFileSync(...)`.
    - Każdy `test()` to jedna assercja (lub mała grupa logicznie powiązanych assertji).

    **`apps/client/src/pages/__tests__/login.test.tsx`** — pełny szkielet (verbatim z RESEARCH §Code Examples lines 587-624, z drobnymi rozszerzeniami):

    ```tsx
    import { describe, expect, test } from 'bun:test';
    import { readFileSync } from 'node:fs';
    import { resolve } from 'node:path';

    /**
     * Source-pin convention test (NOT behavioral).
     * Fails when the file no longer contains the required tokens — does NOT verify
     * runtime behavior. Pins two MEMORY-derived invariants in source form:
     *   - feedback_better_auth_session_refetch: `await refetchSession()` must appear before `navigate(`
     *   - feedback_react_autofill_uncontrolled: credential inputs must be uncontrolled (no `value={...}` prop)
     * Manual UAT (DevTools Network panel: sign-in → get-session → redirect) is the
     * behavioral source of truth. See grill C3 + enterprise F.1 for rationale.
     */
    describe('LoginPage source-pin convention — FE-06', () => {
      const source = readFileSync(resolve(__dirname, '../login.tsx'), 'utf-8');

      test('imports useCredentialsForm hook (FE-02 dedup)', () => {
        expect(source).toMatch(/from '@\/hooks\/use-credentials-form'/);
        expect(source).toMatch(/useCredentialsForm/);
      });

      test('imports signIn from @/lib/auth-client (better-auth integration intact)', () => {
        expect(source).toMatch(/from '@\/lib\/auth-client'/);
        expect(source).toMatch(/signIn\.email/);
      });

      test('refetchSession is awaited BEFORE navigate (MEMORY: feedback_better_auth_session_refetch)', () => {
        const refetchIdx = source.search(/await\s+refetchSession\s*\(\s*\)/);
        const navigateIdx = source.search(/navigate\s*\(/);
        expect(refetchIdx).toBeGreaterThan(-1);
        expect(navigateIdx).toBeGreaterThan(-1);
        expect(refetchIdx).toBeLessThan(navigateIdx);
      });

      test('form uses uncontrolled inputs — bare <Input name="..."> (MEMORY: feedback_react_autofill_uncontrolled)', () => {
        expect(source).toMatch(/<Input[^>]*name="email"/);
        expect(source).toMatch(/<Input[^>]*name="password"/);
      });

      test('no controlled-input value= prop on email/password inputs (autofill must work)', () => {
        // The strongest signal: no `value={...}` attribute on the credential inputs.
        expect(source).not.toMatch(/<Input[^>]*name="email"[^>]*value=\{/);
        expect(source).not.toMatch(/<Input[^>]*name="password"[^>]*value=\{/);
      });
    });
    ```

    **`apps/client/src/pages/__tests__/register.test.tsx`** — analogicznie, swap `signIn.email` → `signUp.email`, dodaj fields `name`, `email`, `password`, `confirmPassword`:

    ```tsx
    import { describe, expect, test } from 'bun:test';
    import { readFileSync } from 'node:fs';
    import { resolve } from 'node:path';

    /**
     * Source-pin convention test (NOT behavioral). See LoginPage rationale.
     */
    describe('RegisterPage source-pin convention — FE-06', () => {
      const source = readFileSync(resolve(__dirname, '../register.tsx'), 'utf-8');

      test('imports useCredentialsForm hook (FE-02 dedup)', () => {
        expect(source).toMatch(/from '@\/hooks\/use-credentials-form'/);
        expect(source).toMatch(/useCredentialsForm/);
      });

      test('imports signUp from @/lib/auth-client', () => {
        expect(source).toMatch(/from '@\/lib\/auth-client'/);
        expect(source).toMatch(/signUp\.email/);
      });

      test('refetchSession is awaited BEFORE navigate (MEMORY: feedback_better_auth_session_refetch)', () => {
        const refetchIdx = source.search(/await\s+refetchSession\s*\(\s*\)/);
        const navigateIdx = source.search(/navigate\s*\(/);
        expect(refetchIdx).toBeGreaterThan(-1);
        expect(navigateIdx).toBeGreaterThan(-1);
        expect(refetchIdx).toBeLessThan(navigateIdx);
      });

      test('form uses uncontrolled inputs (FormData driver) — MEMORY: feedback_react_autofill_uncontrolled', () => {
        expect(source).toMatch(/<Input[^>]*name="name"/);
        expect(source).toMatch(/<Input[^>]*name="email"/);
        expect(source).toMatch(/<Input[^>]*name="password"/);
        expect(source).toMatch(/<Input[^>]*name="confirmPassword"/);
      });

      test('no controlled-input value= prop on credential inputs', () => {
        expect(source).not.toMatch(/<Input[^>]*name="email"[^>]*value=\{/);
        expect(source).not.toMatch(/<Input[^>]*name="password"[^>]*value=\{/);
        expect(source).not.toMatch(/<Input[^>]*name="confirmPassword"[^>]*value=\{/);
        expect(source).not.toMatch(/<Input[^>]*name="name"[^>]*value=\{/);
      });
    });
    ```

    **TDD note (RED):** Testy mają failować przy uruchomieniu W TYM TASKU — bo `useCredentialsForm` jeszcze nie istnieje i source login/register go nie importuje. To jest oczekiwane. Failujący stan zostanie naprawiony w Task 2 + 3 (GREEN).

    **Zakazy:**
    - NIE dodawaj `vitest`, `@testing-library/react`, `jsdom`, `happy-dom` ani innych devDependencies (RESEARCH §Pitfall 7 + §Project Constraints #2).
    - NIE rób `render(<LoginPage />)` — brak jsdom; source-pin to całe rzemiosło tutaj.
    - NIE pin'uj kompletnego literal copy "Sign in" / "Welcome back" — current login.tsx ma angielski copy, ale to OUT OF SCOPE (FE-06 dotyczy fixów MEMORY, nie copy).
  </action>
  <verify>
    <automated>
      cd /Users/kodari/projects/games &amp;&amp;
      test -f apps/client/src/pages/__tests__/login.test.tsx &amp;&amp;
      test -f apps/client/src/pages/__tests__/register.test.tsx &amp;&amp;
      grep -q "describe('LoginPage source-pin convention — FE-06'" apps/client/src/pages/__tests__/login.test.tsx &amp;&amp;
      grep -q "describe('RegisterPage source-pin convention — FE-06'" apps/client/src/pages/__tests__/register.test.tsx &amp;&amp;
      grep -q "readFileSync(resolve(__dirname, '../login.tsx')" apps/client/src/pages/__tests__/login.test.tsx &amp;&amp;
      grep -q "readFileSync(resolve(__dirname, '../register.tsx')" apps/client/src/pages/__tests__/register.test.tsx &amp;&amp;
      grep -q "refetchIdx" apps/client/src/pages/__tests__/login.test.tsx &amp;&amp;
      grep -q "navigateIdx" apps/client/src/pages/__tests__/login.test.tsx &amp;&amp;
      # RED enforcement (grill I1 + enterprise B.5):
      # Tests MUST fail here — hook nie istnieje, page'y nie importują. Jeśli passują, znaczy że
      # testy nic nie pinują albo hook już wcześniej istnieje. Either way — STOP, fix Task 1 najpierw.
      ! (cd apps/client &amp;&amp; bun test src/pages/__tests__/login.test.tsx src/pages/__tests__/register.test.tsx 2>&amp;1 | grep -qE '(0 fail|^pass)')
    </automated>
  </verify>
  <done>
    Dwa pliki testów istnieją z poprawnym source-pin shape (`bun:test` + `readFileSync` z `__dirname + '../'`). Każdy test failuje w obecnym stanie (hook nie istnieje, login/register go nie używają) — to jest expected RED phase. NIE dodano żadnych devDeps.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementuj useCredentialsForm hook (GREEN część 1)</name>
  <files>apps/client/src/hooks/use-credentials-form.ts</files>
  <read_first>
    - apps/client/src/pages/settings/account-password-form.tsx (lines 37-70 — sync capture + FormData + e.currentTarget BEFORE await pattern)
    - apps/client/src/hooks/use-add-game-with-metadata.ts (lines 11-58 — hook signature with Args/Result interfaces, useCallback, TSDoc block)
    - apps/client/src/hooks/use-game-draft.ts (lines 13-59 — useCallback for stable callbacks, TSDoc block precedent)
    - .planning/phases/04-frontend-stability/04-RESEARCH.md §Pattern 3 (lines 236-321 — AUTHORITATIVE implementation)
    - .planning/phases/04-frontend-stability/04-RESEARCH.md §Pitfall 4 + §Pitfall 6 (sync capture, useCallback deps)
    - .planning/phases/04-frontend-stability/04-PATTERNS.md `### use-credentials-form.ts`
  </read_first>
  <behavior>
    Hook spec:
    - Input: `{ fields: readonly CredentialField[], onSubmit: (values: T) => Promise<{error?,fieldErrors?} | void> }`
    - Output: `{ handleSubmit, isPending, error, fieldErrors, resetErrors }`
    - `handleSubmit`:
      - prevents default
      - sync captures `form = e.currentTarget` and `data = new FormData(form)` BEFORE any await
      - builds `values: Record<string,string>` from `data.get(name)`, trimmed, coerced via `String(... ?? '')`
      - runs per-field `validate(value, allValues)` callbacks (jeśli zdefiniowane); pierwszy zwrot non-null/non-undefined string trafia do fieldErrors[name]
      - jeśli jakiekolwiek validators failują → `setFieldErrors(errs)` i RETURN bez wywoływania onSubmit
      - jeśli wszystko OK → `resetErrors()`, `setIsPending(true)`, `await args.onSubmit(values)`, `setIsPending(false)`
      - jeśli result zawiera `error` → `setError(result.error)`
      - jeśli result zawiera `fieldErrors` → `setFieldErrors(result.fieldErrors)`
    - `resetErrors`: czyści error + fieldErrors
    - HOOK NIE wywołuje better-auth (auth-agnostic) — caller w `onSubmit` callback wywołuje `signIn`/`signUp` + `refetchSession` + `navigate`
  </behavior>
  <action>
    Utwórz `apps/client/src/hooks/use-credentials-form.ts`. Konwencje: kebab-case, named exports only, single quotes, semicolons, trailing commas (Biome).

    **Kod (verbatim z RESEARCH §Pattern 3 lines 244-315, z dodanym TSDoc):**

    ```ts
    import { type FormEvent, useCallback, useState } from 'react';

    /**
     * Field descriptor for `useCredentialsForm`.
     *
     * @property name      - The form-input `name` attribute used as the key in `new FormData(form)` lookups.
     * @property validate  - Optional client-side validator. Return a non-empty string to surface as `fieldErrors[name]`,
     *                       or null/undefined to pass. Runs BEFORE `args.onSubmit` is invoked. Receives the current
     *                       field value plus the full values map (for cross-field invariants like `password === confirmPassword`).
     */
    export interface CredentialField {
      name: string;
      validate?: (value: string, all: Record<string, string>) => string | null | undefined;
    }

    export interface UseCredentialsFormArgs<T extends Record<string, string>> {
      fields: readonly CredentialField[];
      onSubmit: (
        values: T,
      ) => Promise<{ error?: string; fieldErrors?: Partial<Record<keyof T, string>> } | void>;
    }

    export interface UseCredentialsFormReturn<T> {
      handleSubmit: (e: FormEvent<HTMLFormElement>) => Promise<void>;
      isPending: boolean;
      error: string | null;
      fieldErrors: Partial<Record<keyof T, string>>;
      resetErrors: () => void;
    }

    /**
     * Uncontrolled-form driver for credential pages (login + register).
     *
     * Invariants pinned by FE-06 source-pin convention tests:
     *   1. Form values come from `new FormData(form)` — NEVER from React state (browser autofill rule).
     *      The hook does NOT inject `value=` / `onChange=` props onto inputs; the caller renders bare
     *      `<Input name="..." />`.
     *   2. `e.currentTarget` is captured SYNCHRONOUSLY before any `await` (React synthetic event pooling).
     *   3. The hook is AUTH-AGNOSTIC.
     *
     * Rationale for invariant 3 (auth-agnostic): `await refetchSession()` lives in the caller's
     * `onSubmit` callback, not in this hook. This is a DELIBERATE choice — pulling refetch into the
     * hook would semantically work, but it would prevent the FE-06 source-pin test from observing
     * the invariant at the page-source layer (grill C3 → enterprise B.5). The MEMORY rule
     * `feedback_better_auth_session_refetch` is about *what the page does after sign-in*, not
     * about the hook's internals — so the pin lives where the invariant lives. If you ever refactor
     * this to call refetch inside the hook, you MUST simultaneously rewrite the source-pin tests
     * to inspect the hook source instead (and accept that the pin no longer pins the page semantics).
     */
    export function useCredentialsForm<T extends Record<string, string>>(
      args: UseCredentialsFormArgs<T>,
    ): UseCredentialsFormReturn<T> {
      const [isPending, setIsPending] = useState(false);
      const [error, setError] = useState<string | null>(null);
      const [fieldErrors, setFieldErrors] = useState<Partial<Record<keyof T, string>>>({});

      const resetErrors = useCallback(() => {
        setError(null);
        setFieldErrors({});
      }, []);

      const handleSubmit = useCallback(
        async (e: FormEvent<HTMLFormElement>): Promise<void> => {
          e.preventDefault();
          const form = e.currentTarget;            // SYNC capture before any await (RESEARCH §Pitfall 4)
          const data = new FormData(form);         // SYNC

          const values = {} as Record<string, string>;
          for (const f of args.fields) {
            values[f.name] = String(data.get(f.name) ?? '').trim();
          }

          const errs: Record<string, string> = {};
          for (const f of args.fields) {
            if (!f.validate) continue;
            const msg = f.validate(values[f.name], values);
            if (msg) errs[f.name] = msg;
          }
          if (Object.keys(errs).length > 0) {
            setFieldErrors(errs as Partial<Record<keyof T, string>>);
            return;
          }

          resetErrors();
          setIsPending(true);
          const result = await args.onSubmit(values as T);
          setIsPending(false);

          if (result?.error) setError(result.error);
          if (result?.fieldErrors) setFieldErrors(result.fieldErrors);
        },
        [args, resetErrors],
      );

      return { handleSubmit, isPending, error, fieldErrors, resetErrors };
    }
    ```

    **Zakazy / awareness:**
    - NIE pisz `value=`/`onChange=` w hooku (auto-fail FE-06 test) — caller renderuje bare `<Input name="..." />`.
    - NIE wywołuj `signIn`/`signUp`/`refetchSession`/`navigate` w hooku — hook auth-agnostic. Caller robi to w `onSubmit` callback.
    - NIE generalizuj do `useForm` (RESEARCH §Anti-Patterns 452 + §Open Question 2) — scope to login + register only.
    - `useCallback([args])` recreates per render (RESEARCH §Pitfall 6) — accept dla submit cadence; nie próbuj memoizować pojedyncze props.
    - `e.currentTarget` MUSI być captured PRZED `await args.onSubmit` (RESEARCH §Pitfall 4) — już jest w kodzie powyżej; nie reorder.
  </action>
  <verify>
    <automated>
      cd /Users/kodari/projects/games &amp;&amp;
      test -f apps/client/src/hooks/use-credentials-form.ts &amp;&amp;
      grep -q "export function useCredentialsForm" apps/client/src/hooks/use-credentials-form.ts &amp;&amp;
      grep -q "export interface UseCredentialsFormArgs" apps/client/src/hooks/use-credentials-form.ts &amp;&amp;
      grep -q "export interface UseCredentialsFormReturn" apps/client/src/hooks/use-credentials-form.ts &amp;&amp;
      grep -q "export interface CredentialField" apps/client/src/hooks/use-credentials-form.ts &amp;&amp;
      grep -q "new FormData" apps/client/src/hooks/use-credentials-form.ts &amp;&amp;
      grep -q "e.currentTarget" apps/client/src/hooks/use-credentials-form.ts &amp;&amp;
      ! grep -q "signIn\|signUp\|refetchSession\|navigate(" apps/client/src/hooks/use-credentials-form.ts &amp;&amp;
      ! grep -q "value=\\{" apps/client/src/hooks/use-credentials-form.ts &amp;&amp;
      ! grep -q "export default" apps/client/src/hooks/use-credentials-form.ts &amp;&amp;
      cd apps/client &amp;&amp; bunx tsc -b --noEmit
    </automated>
  </verify>
  <done>
    Hook plik istnieje. Named exports: `useCredentialsForm`, `UseCredentialsFormArgs`, `UseCredentialsFormReturn`, `CredentialField`. Auth-agnostic (brak `signIn`/`signUp`/`refetchSession`/`navigate`). Brak `value=` w hooku. TypeScript pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Refactor login.tsx i register.tsx na useCredentialsForm (GREEN część 2) + uruchom testy</name>
  <files>apps/client/src/pages/login.tsx, apps/client/src/pages/register.tsx</files>
  <read_first>
    - apps/client/src/pages/login.tsx (current state)
    - apps/client/src/pages/register.tsx (current state)
    - apps/client/src/hooks/use-credentials-form.ts (świeżo utworzony w Task 2)
    - .planning/phases/04-frontend-stability/04-RESEARCH.md §Code Examples → "useCredentialsForm wiring in login.tsx (SC2)" (lines 546-583)
    - .planning/phases/04-frontend-stability/04-PATTERNS.md `### apps/client/src/pages/login.tsx & register.tsx (MOD)`
    - apps/client/src/components/ui/input.tsx (potwierdź props — `name`, `type`, `required`, `autoComplete` przejdą jako standard HTML attrs)
  </read_first>
  <action>
    Dwa refactory. Każdy zachowuje całe behavior current page'a (error code mapping, route fallback, autoComplete attrs), tylko ekstrahuje `useState(error/isPending/fieldErrors) + handleSubmit` do hooka.

    **(a) `apps/client/src/pages/login.tsx`** — target shape (verbatim z RESEARCH §Code Examples lines 548-582, z drobnymi adjustments dla current copy):

    1. Imports:
       ```tsx
       import { useNavigate, useLocation } from 'react-router-dom';
       import { signIn, useSession } from '@/lib/auth-client';
       import { useCredentialsForm } from '@/hooks/use-credentials-form';
       import { Button } from '@/components/ui/button';
       import { Input } from '@/components/ui/input';
       // ... whatever else current login.tsx imports (Label, layout helpers) ...
       ```
    2. Body komponentu `LoginPage`:
       ```tsx
       export function LoginPage() {
         const navigate = useNavigate();
         const location = useLocation();
         const { refetch: refetchSession } = useSession();

         const { handleSubmit, isPending, error, fieldErrors } = useCredentialsForm({
           fields: [{ name: 'email' }, { name: 'password' }],
           onSubmit: async ({ email, password }) => {
             const { error: signInError } = await signIn.email({ email, password });
             if (signInError) {
               return {
                 error:
                   signInError.code === 'INVALID_EMAIL_OR_PASSWORD'
                     ? 'Invalid email or password.'
                     : 'Something went wrong. Try again.',
               };
             }
             await refetchSession();                                              // MEMORY rule pinned
             const from = (location.state as { from?: string } | null)?.from ?? '/games';
             navigate(from, { replace: true });
           },
         });

         return (
           <div>
             {/* Zachowaj obecny copy + layout — "Welcome back" itp., NIE polonizuj w tym planie */}
             <h1 className="text-2xl font-semibold text-apex-ink">Welcome back</h1>
             <p className="mt-2 text-sm text-apex-muted">Sign in to your Apex account.</p>
             {error && <div className="mt-6 ...">{error}</div>}
             <form onSubmit={handleSubmit} className="mt-8 space-y-4">
               <Input name="email" type="email" required autoComplete="email" />
               {fieldErrors.email && <p className="text-xs text-[#e63946]">{fieldErrors.email}</p>}
               <Input name="password" type="password" required autoComplete="current-password" />
               {fieldErrors.password && <p className="text-xs text-[#e63946]">{fieldErrors.password}</p>}
               <Button type="submit" disabled={isPending}>{isPending ? 'Signing in…' : 'Sign in'}</Button>
             </form>
           </div>
         );
       }
       ```
    3. **MUST preserve (PATTERNS.md `### login.tsx`):**
       - Sync capture (już w hooku — caller nie touch'uje).
       - `await refetchSession()` PRZED `navigate` (literalnie w `onSubmit` callback, lines `await refetchSession();` THEN `navigate(...)`).
       - Error code mapping `INVALID_EMAIL_OR_PASSWORD` w `onSubmit` callback (zwraca `{ error }`).
       - `from` extraction z `location.state`.
       - `autoComplete` attrs ("email" + "current-password").
       - `required` na inputach.
    4. **MUST remove:**
       - `useState(error, ...)`, `useState(isPending, ...)`, `useState(fieldErrors, ...)` — przenoszą się do hooka.
       - Ręczne `e.preventDefault(); const form = e.currentTarget; const data = new FormData(form); ...` w komponencie — hook to robi.
       - Wszystkie `setError`/`setIsPending`/`setFieldErrors` w komponencie — hook to robi.

    **(b) `apps/client/src/pages/register.tsx`** — analogiczna struktura:

    ```tsx
    export function RegisterPage() {
      const navigate = useNavigate();
      const { refetch: refetchSession } = useSession();

      const { handleSubmit, isPending, error, fieldErrors } = useCredentialsForm<{
        name: string;
        email: string;
        password: string;
        confirmPassword: string;
      }>({
        fields: [
          {
            name: 'name',
            validate: (v) => (v.length === 0 ? 'Name is required.' : null),
          },
          { name: 'email' },
          {
            name: 'password',
            validate: (v) => (v.length < 8 ? 'Password must be at least 8 characters.' : null),
          },
          {
            name: 'confirmPassword',
            validate: (v, all) =>
              v !== all.password ? 'Passwords do not match.' : null,
          },
        ],
        onSubmit: async ({ name, email, password }) => {
          const { error: signUpError } = await signUp.email({ name, email, password });
          if (signUpError) {
            if (signUpError.code === 'USER_ALREADY_EXISTS') {
              return { fieldErrors: { email: 'An account with this email already exists.' } };
            }
            return { error: 'Something went wrong. Try again.' };
          }
          await refetchSession();                                                 // MEMORY rule pinned
          navigate('/games', { replace: true });
        },
      });

      return (
        <div>
          {/* Zachowaj current Polish/English copy — nie zmieniaj w tym planie */}
          <h1>...</h1>
          {error && <div>{error}</div>}
          <form onSubmit={handleSubmit} className="space-y-4">
            <Input name="name" type="text" required autoComplete="name" />
            {fieldErrors.name && <p>{fieldErrors.name}</p>}
            <Input name="email" type="email" required autoComplete="email" />
            {fieldErrors.email && <p>{fieldErrors.email}</p>}
            <Input name="password" type="password" required autoComplete="new-password" />
            {fieldErrors.password && <p>{fieldErrors.password}</p>}
            <Input name="confirmPassword" type="password" required autoComplete="new-password" />
            {fieldErrors.confirmPassword && <p>{fieldErrors.confirmPassword}</p>}
            <Button type="submit" disabled={isPending}>...</Button>
          </form>
        </div>
      );
    }
    ```

    **MUST preserve (PATTERNS.md `### register.tsx`):**
    - Client-side validators: `name` non-empty, `password` min 8, `password === confirmPassword`. (Przeniesione do `validate` callbacks na `CredentialField[]`.)
    - `USER_ALREADY_EXISTS` → `fieldErrors.email` mapping w `onSubmit` callback.
    - `await refetchSession()` PRZED `navigate('/games', { replace: true })`.
    - `autoComplete` attrs (`name`, `email`, `new-password` × 2).
    - Wszystkie current copy strings (NIE polonizuj/anglicyzuj w tym planie).

    **(c) Po obu refactorach: uruchom testy** — powinno być GREEN:
    ```
    bun test apps/client/src/pages/__tests__/login.test.tsx apps/client/src/pages/__tests__/register.test.tsx
    ```

    **Zakazy:**
    - NIE dodawaj `value=` / `onChange=` do Input'ów (auto-fail FE-06 test + MEMORY rule).
    - NIE rób `await refetchSession()` PO `navigate(...)` (auto-fail FE-06 test + MEMORY rule).
    - NIE rób `await refetchSession()` w hooku — auth-agnostic; pinujemy w page source.
    - NIE zmieniaj copy UI (out of scope tego planu).
    - NIE usuwaj `autoComplete` attrs — krytyczne dla autofill (MEMORY).
    - NIE łącz validatorów w jeden mega-validator — per-field `validate` callbacks dają lepsze fieldErrors localization.
  </action>
  <verify>
    <automated>
      cd /Users/kodari/projects/games &amp;&amp;
      grep -q "useCredentialsForm" apps/client/src/pages/login.tsx &amp;&amp;
      grep -q "useCredentialsForm" apps/client/src/pages/register.tsx &amp;&amp;
      grep -q "signIn.email" apps/client/src/pages/login.tsx &amp;&amp;
      grep -q "signUp.email" apps/client/src/pages/register.tsx &amp;&amp;
      grep -q "await refetchSession" apps/client/src/pages/login.tsx &amp;&amp;
      grep -q "await refetchSession" apps/client/src/pages/register.tsx &amp;&amp;
      # No controlled inputs:
      ! grep -E '&lt;Input[^&gt;]*name="email"[^&gt;]*value=\\{' apps/client/src/pages/login.tsx &amp;&amp;
      ! grep -E '&lt;Input[^&gt;]*name="password"[^&gt;]*value=\\{' apps/client/src/pages/login.tsx &amp;&amp;
      ! grep -E '&lt;Input[^&gt;]*name="email"[^&gt;]*value=\\{' apps/client/src/pages/register.tsx &amp;&amp;
      ! grep -E '&lt;Input[^&gt;]*name="password"[^&gt;]*value=\\{' apps/client/src/pages/register.tsx &amp;&amp;
      # No duplicated FormData blocks in pages (FE-02 dedup):
      [ "$(grep -c 'new FormData' apps/client/src/pages/login.tsx)" = "0" ] &amp;&amp;
      [ "$(grep -c 'new FormData' apps/client/src/pages/register.tsx)" = "0" ] &amp;&amp;
      cd apps/client &amp;&amp; bunx tsc -b --noEmit &amp;&amp;
      cd /Users/kodari/projects/games &amp;&amp;
      bun test apps/client/src/pages/__tests__/login.test.tsx apps/client/src/pages/__tests__/register.test.tsx
    </automated>
  </verify>
  <done>
    Obie page'y używają `useCredentialsForm`, importują `signIn.email`/`signUp.email` z `@/lib/auth-client`, mają `await refetchSession()` PRZED `navigate(...)` w `onSubmit` callback. Żaden `<Input>` nie ma `value={`. Brak `new FormData(` w samych page'ach (przeniesione do hooka — FE-02 dedup). `bun test` na obu test files: zielony (RED → GREEN cykl zamknięty).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    `useCredentialsForm` hook + zrefaktorowane `login.tsx` / `register.tsx` używające hooka + dwa testy regresyjne FE-06 pinujące oba MEMORY bugi. Cały flow zachowany; tylko ekstrakcja state + handler do hooka.
  </what-built>
  <how-to-verify>
    Manualny smoke test (test stack nie ma jsdom — behavioral coverage tylko manualnie):

    1. `bun run dev` z repo root, otwórz `http://localhost:5173/login` w przeglądarce z zapisanymi credentialami (Chrome/Safari/Firefox z password manager).
    2. **Autofill test (MEMORY: feedback_react_autofill_uncontrolled):**
       - Przy załadowaniu strony password manager powinien zaproponować autofill `email`/`password`.
       - Klik "Use saved" w password manager → oba pola się wypełniają BEZ konieczności kliknięcia w input.
       - Klik "Sign in" → submit działa bez ręcznego interaction z polami. (Jeśli autofill wymaga ręcznego kliknięcia w input żeby się pokazał — to regression bug; failuj plan.)
    3. **refetchSession-before-navigate test (MEMORY: feedback_better_auth_session_refetch):**
       - Z DevTools Network panel filtruj na requests.
       - Submit login z poprawnymi credentials.
       - Oczekiwana sekwencja: `POST /api/auth/sign-in/email` (200) → `GET /api/auth/get-session` (refetch) → redirect do `/games` (route remount, `ProtectedRoute` nie bounce'uje na `/login`).
       - Sprawdź URL bar: musi zmienić się do `/games` (lub do `from` ze state). Jeśli widzisz krótki flash `/login` → `/games` → `/login` → bug, failuj.
    4. **Error path test:**
       - Wpisz złe hasło → submit → komunikat "Invalid email or password." widoczny; przycisk powraca z disabled.
       - Wpisz dobre hasło po złym → submit → komunikat znika, redirect.
    5. **Register flow:**
       - Otwórz `/register`. Spróbuj submit z `password` 6 znaków → fieldError "Password must be at least 8 characters." (validator z hooka).
       - Spróbuj `password` ≠ `confirmPassword` → fieldError "Passwords do not match.".
       - Submit z poprawnymi danymi → analogicznie do logina: sign-up → get-session → navigate `/games`.
       - Submit z duplikat email → `fieldErrors.email` "An account with this email already exists.".
    6. **FE-02 dedup spot-check (manualne):**
       - `grep -c 'new FormData' apps/client/src/pages/login.tsx apps/client/src/pages/register.tsx` → 0 0 (FormData tylko w hooku).
       - `wc -l apps/client/src/pages/login.tsx apps/client/src/pages/register.tsx` → obie pomniejszone vs przed refactor.

    7. **Test run (potwierdzenie automatyczne):**
       ```
       bun test apps/client/src/pages/__tests__/login.test.tsx apps/client/src/pages/__tests__/register.test.tsx
       ```
       Oczekiwany wynik: wszystkie test cases passed (5 w login.test.tsx, 5 w register.test.tsx).
  </how-to-verify>
  <resume-signal>Wpisz "approved" jeśli wszystkie 6 scenariuszy działają + testy zielone. W przeciwnym razie opisz który scenariusz failuje.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser autofill → form values | Browser password manager wstrzykuje credentials do uncontrolled inputs; React state by nie zobaczył ich dopóki user nie zinteraguje — stąd `new FormData(form)` (a nie React state) jest correct |
| useCredentialsForm hook → onSubmit callback | Hook delivers `values: T` (string map z trimmed FormData); caller wykonuje better-auth call + refetchSession + navigate |
| better-auth signIn/signUp → page | API zwraca `{ error?: { code } }`; page mapuje code → user-facing message |
| useSession refetch → ProtectedRoute | Po `refetchSession()` `useSession` zwraca świeży session state; bez tego ProtectedRoute widzi stary stan (null) i robi bounce do /login (MEMORY bug) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Authentication bypass | Hook akceptuje wartości bez serwerowej walidacji | accept | better-auth waliduje serwerowo (`signIn.email`/`signUp.email`); client validators są UX-only (RESEARCH §Security Domain — Applicable ASVS V5). Zero risk: client nie może bypass'ować serwera. |
| T-04-08 | Information disclosure | Password leaki do logów/console | mitigate | Hook NIE loguje `values`. `console.error` w komponentach (jeśli istnieje) MUSI być sanitized; review na PR. Strukturalny `console.error(values)` jest forbidden — pin w action: "submit handler never logs password". |
| T-04-09 | Repudiation | Stale session po sign-in (MEMORY bug) | mitigate | `await refetchSession()` przed `navigate(...)` pinowany przez FE-06 source-pin test. Test failuje jeśli ktokolwiek cofnie kolejność. |
| T-04-10 | Tampering | User wstrzykuje `value=` przez React DevTools | accept | Even with React DevTools, browser submit wysyła DOM value (FormData), nie React-state. Auto-mitigated przez uncontrolled-inputs choice. |
| T-04-11 | Authentication (V2) — autofill failure → weak password reuse | UX rather than direct attack | mitigate | Uncontrolled-inputs rule preserves password-manager autofill — pinowany przez FE-06 test (no `value=`). Failure mode: user retorts to remembered weaker passwords; mitigated by ensuring autofill works. |
| T-04-12 | XSS w error message | `signInError.code` echoed do UI | mitigate | Page MAPUJE `signInError.code` na static string literals ("Invalid email or password.", "Something went wrong. Try again.", "An account with this email already exists.") — nigdy NIE renderuje raw `signInError.message`. Pin w `<action>` Task 3. |
| T-04-13 | Stored credentials leak via test source | Testy źródłowo zawierają hasła? | n/a | Testy nie zawierają żadnych credentials — czytają source komponentów regex'em. Zero risk. |
| T-04-22 | CSRF | Cross-origin POST do `/api/auth/sign-in/email` / `sign-up/email` z złośliwej strony | mitigate | Phase 3 (SEC-02, commit `3cd709c`) zamontował CSRF Origin guard na `/api/*` (`apps/api/src/index.ts:64` — middleware sprawdza `Origin` + `Sec-Fetch-Site` na każdym non-GET requeście). Better-auth endpointy są pod `/api/auth/*`, więc są pokryte tym samym guardem. SameSite=Strict cookie attribute (Phase 3 SEC-03) jest druga warstwa. useCredentialsForm fetch'em (`signIn.email`/`signUp.email` z better-auth client) trafia w guard od środka aplikacji — same-origin, więc przejdzie. Brak HMAC token w form body nie jest potrzebny — origin guard + SameSite=Strict to obecny baseline. |
| T-04-23 | Rate-limit confusion | Lokalny `useCredentialsForm` submit submituje z prędkością klika — czy uderza w rate-limit | n/a | Auth endpointy mają WŁASNY rate-limit better-auth (`/sign-in/email`: 5/60s, configured w `apps/api/src/infrastructure/auth/auth.ts`). Mutation rate-limit (SEC-05, 60/min per-user) NIE obejmuje auth endpointów — sprawdzone w `apps/api/src/routes/middleware/mutation-rate-limit.ts` (mountuje się POZA `/api/auth/*`). useCredentialsForm submit nie zmienia rate-limit footprint. |

ASVS L1 mapping:
- V2.1.1 (Authentication, "Use first-class controls"): delegujemy do better-auth (already integrated).
- V3.2.2 (Session, "Token bound to verified events"): `await refetchSession()` po sign-in zapewnia że client-side useSession state pasuje do server-side session cookie state.
- V5.1.1 (Input validation): client validators w hook są UX-only; serwer waliduje (better-auth).
</threat_model>

<verification>
- `cd apps/client && bunx tsc -b --noEmit` passes.
- `bun test apps/client/src/pages/__tests__/login.test.tsx apps/client/src/pages/__tests__/register.test.tsx` passes (10 test cases łącznie).
- `bun test apps/client/src/pages/__tests__/` (cały folder) passes — żaden inny test (jeśli istnieje) nie złamany.
- Pełny suite: `bun test` (z repo root) passes — żaden API test nie złamany.
- Grep invariants (repo root):
  ```
  grep -q "export function useCredentialsForm" apps/client/src/hooks/use-credentials-form.ts
  grep -q "new FormData" apps/client/src/hooks/use-credentials-form.ts
  ! grep -q "signIn\|signUp\|refetchSession\|navigate(" apps/client/src/hooks/use-credentials-form.ts
  grep -q "useCredentialsForm" apps/client/src/pages/login.tsx
  grep -q "useCredentialsForm" apps/client/src/pages/register.tsx
  grep -q "await refetchSession" apps/client/src/pages/login.tsx
  grep -q "await refetchSession" apps/client/src/pages/register.tsx
  [ "$(grep -c 'new FormData' apps/client/src/pages/login.tsx)" = "0" ]
  [ "$(grep -c 'new FormData' apps/client/src/pages/register.tsx)" = "0" ]
  ```
- Manualny smoke (Task 4 checkpoint): 6 scenariuszy z `<how-to-verify>` zaliczonych.
- Brak nowych devDependencies — `package.json` unchanged.
</verification>

<success_criteria>
ROADMAP success criteria addressed:
- **SC-2 (FE-02)**: `apps/client/src/pages/login.tsx` i `register.tsx` używają wspólnego hooka `useCredentialsForm({ fields, onSubmit })` (uncontrolled + FormData + pending/error state); brak duplikacji `new FormData(form)` w obu plikach. ✓ Hook utworzony w Task 2; pages zrefaktorowane w Task 3; pin'y w verify (`grep -c 'new FormData'` = 0 w obu pages).
- **SC-6 (FE-06)**: Test (`login.test.tsx`/`register.test.tsx`) wymusza obecność `await refetchSession()` przed `navigate` i że formularz operuje na uncontrolled inputs (FormData driver) — failuje, jeśli ktoś cofnie którykolwiek z dwóch fixów z MEMORY. ✓ Testy utworzone w Task 1 (RED) i zielone w Task 3 (GREEN); ordering pin via `source.search().toBeLessThan()`; uncontrolled pin via `.not.toMatch(/value=\{/)`.

REQUIREMENTS addressed:
- **FE-02**: `useCredentialsForm({ fields, onSubmit })` hook ekstrahuje wspólny pattern login/register (uncontrolled + FormData + pending/error state); oba page'y używają go zamiast ręcznej duplikacji. ✓
- **FE-06**: Regression testy dla login/register zapinające oba niedawne bugi: (a) refetchSession przed navigate, (b) uncontrolled+FormData zamiast controlled useState. ✓

Out of Scope (explicit deferrals):
- Generalizacja `useCredentialsForm` do `useForm` (RESEARCH §Open Question 2 + §Anti-Patterns 452) — scope strictly login + register.
- Refactor Settings password form na hook — separate scope; current pattern already shipped (Plan 01-02).
- Behavioral E2E test login/register (jsdom + RTL) — RESEARCH §Pitfall 7 + §Project Constraints #2 zabrania nowych test deps; source-pin to całe FE-06.
- Polonizacja copy login/register (current ma "Welcome back" / "Sign in" — angielski); UI lang policy decyduje properly w innej phase.
- **Sign-out flow w `apps/client/src/components/layout/sidebar.tsx:114-118`** — NIE używa `await refetchSession()` przed `navigate('/login')`. Działa dziś tylko dlatego że `/login` jest public route (ProtectedRoute nie bouncuje nawet ze stale session). Świadomie out-of-scope (FE-06 wymaga pinów dla login.tsx/register.tsx, NIE dla sign-out). Eskalowane z enterprise C — track jako follow-up jeśli kiedyś sign-out będzie przenosił na ProtectedRoute. Sidebar.tsx jest pinem zachowania, NIE invariantem.
- Toast notifications for sign-in success — current page nie ma; out of scope.
- `useCredentialsForm` consumer poza login/register — generalizacja deferred (RESEARCH §Open Question 2).
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-stability/04-02-SUMMARY.md` recording:
- Final LOC counts: `use-credentials-form.ts`, `login.tsx` (before vs after), `register.tsx` (before vs after)
- Test counts: liczba `test()` cases w `login.test.tsx` + `register.test.tsx`
- Czy wszystkie 10 testów zielone na finalnym `bun test`
- Manualny smoke result: czy autofill works, czy refetchSession-before-navigate sequence widoczna w DevTools Network
- Lista zachowanych behaviorów: error code mapping (`INVALID_EMAIL_OR_PASSWORD`, `USER_ALREADY_EXISTS`), client validators (name/password/confirmPassword), `from` z location.state, `autoComplete` attrs
- Czy ktoś wprowadził mimowolnie `value={` lub `onChange={` na Input'y (sanity check post-refactor)
</output>
</content>
</invoke>