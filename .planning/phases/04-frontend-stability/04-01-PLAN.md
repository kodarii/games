---
phase: 04-frontend-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/client/src/components/error-boundary.tsx
  - apps/client/src/main.tsx
autonomous: true
requirements: [FE-01]
requirements_addressed: [FE-01]
sc_mappings: [1]
mode: mvp
tags: [react, error-boundary, class-component, fallback, main-tsx]

must_haves:
  truths:
    - "Uncaught render error anywhere in the React tree (under `<RouterProvider>`) does NOT blank the SPA — globalny ErrorBoundary łapie błąd i renderuje fallback UI (FE-01 / SC1)."
    - "Fallback UI zawiera polski komunikat (`Coś poszło nie tak.` + opis) i DWA przyciski: primary `Załaduj ponownie` (`window.location.assign('/')`) oraz secondary `Wróć do logowania` (`window.location.assign('/login')` — break-glass dla session-induced crashes per grill C1 → enterprise A.1 → re-grill N3). ROADMAP SC1 literalne wymaganie przycisku `Załaduj ponownie` zachowane; reset semantyka zmieniona z `reload()` na `assign('/')` żeby uniknąć URL-stuck-loop. Cache TanStack Query zostaje zrzucony jako naturalny efekt full navigation (assign niszczy JS-runtime), więc explicit `clear()` byłby placebo — pomijamy."
    - "Mount order w `main.tsx`: `StrictMode → QueryClientProvider → ErrorBoundary → RouterProvider`; `<Toaster>` pozostaje POZA boundary (toast portal przeżywa fallback) — RESEARCH §Pattern 2 + Pitfall 2."
    - "Komponent ErrorBoundary jest klasą (extends Component) implementującą `static getDerivedStateFromError` + `componentDidCatch` — w React 18 nie ma funkcyjnego ekwiwalentu (RESEARCH §Pattern 1, §State of the Art)."
    - "`componentDidCatch` loguje strukturalny payload `{ event: 'render.error.boundary', error, componentStack }` (kształt naśladuje API logger convention — `igdb.breaker.open` style; zero kosztu, łatwy hook do Sentry w v2 per RESEARCH §Open Question 1)."
    - "Fallback NIE renderuje `error.message` ani componentStack jako tekst — XSS/info-disclosure mitigation (security domain T-04-01); copy jest statyczny."
    - "`bun:test` source-pin (opcjonalny) potwierdza obecność `class ErrorBoundary` + `getDerivedStateFromError` + montaż w `main.tsx` między `QueryClientProvider` a `RouterProvider`."
  artifacts:
    - path: "apps/client/src/components/error-boundary.tsx"
      provides: "Klasa ErrorBoundary + funkcyjny AppErrorFallback z przyciskiem reload"
      exports: ["ErrorBoundary", "AppErrorFallback"]
      contains: "class ErrorBoundary"
    - path: "apps/client/src/main.tsx"
      provides: "Mount tree z ErrorBoundary owijającym RouterProvider"
      contains: "<ErrorBoundary"
  key_links:
    - from: "apps/client/src/main.tsx"
      to: "apps/client/src/components/error-boundary.tsx"
      via: "named import + JSX wrap of RouterProvider"
      pattern: "import \\{ ErrorBoundary"
    - from: "apps/client/src/components/error-boundary.tsx"
      to: "componentDidCatch structured log"
      via: "console.error structured event"
      pattern: "render\\.error\\.boundary"
    - from: "apps/client/src/components/error-boundary.tsx (AppErrorFallback)"
      to: "window.location.assign('/')"
      via: "primary Button onClick — safe-route reset eliminuje URL-stuck-loop"
      pattern: "window\\.location\\.assign\\('/'\\)"
    - from: "apps/client/src/components/error-boundary.tsx (AppErrorFallback)"
      to: "window.location.assign('/login')"
      via: "secondary Button onClick — break-glass dla session-corruption"
      pattern: "window\\.location\\.assign\\('/login'\\)"

---

<objective>
Wprowadź globalny ErrorBoundary jako klasę React, zamontuj go w `main.tsx` pomiędzy `QueryClientProvider` a `RouterProvider`. Fallback UI po polsku z DWOMA przyciskami: primary `Załaduj ponownie` (`window.location.assign('/')`) i secondary `Wróć do logowania` (`window.location.assign('/login')` — break-glass). Reset semantyka safe-route-navigate zamiast reload eliminuje URL-induced stuck-loop dla render errors zlokalizowanych pod konkretnym URL-em (grill C1 + enterprise A.1 + re-grill N3). To zamyka SC1 / FE-01 — żaden uncaught render error nie blank'uje już SPA i user nie zostaje uwięziony w pętli URL-bound. **Granica:** globally-induced render errors (np. crash w `<ProtectedRoute>` / `<AppLayout>` montowanym pod każdym URL-em) wymagają break-glass "Wróć do logowania" — udokumentowane w T-04-24.

Purpose: Phase 4 SC1 — odporność na render-time errors. Single-user app, brak Sentry/Axiom (SEC-V2-01 odroczony), ale strukturalny log w `componentDidCatch` zostawia trywialny hook na przyszłość.

Output: 1 nowy plik (`error-boundary.tsx` — ~50 LOC) + edycja `main.tsx` (1 import + 2 linie JSX).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-stability/04-RESEARCH.md
@.planning/phases/04-frontend-stability/04-PATTERNS.md

@apps/client/src/main.tsx
@apps/client/src/components/auth/protected-route.tsx
@apps/client/src/components/ui/button.tsx
@apps/client/src/pages/settings/account-password-form.tsx

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase + RESEARCH §Pattern 1. -->

From apps/client/src/components/auth/protected-route.tsx (lines 9-13) — full-screen centered layout pattern to mirror:
```tsx
return (
  <div className="flex h-screen w-screen items-center justify-center">
    <div className="h-6 w-6 animate-spin rounded-full border-2 border-apex-line-3 border-t-apex-ink" />
  </div>
);
```

From apps/client/src/components/ui/button.tsx — `<Button>` named export from shadcn (variant prop available: `default | destructive | outline | secondary | ghost | link`).
Usage example: `<Button variant="default" size="default" onClick={...}>Załaduj ponownie</Button>`.

From apps/client/src/main.tsx (current mount tree, lines 69-76):
```tsx
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <QueryClientProvider client={queryClient}>
      <RouterProvider router={router} />
      <Toaster richColors position="top-center" />
    </QueryClientProvider>
  </React.StrictMode>,
);
```
Target shape:
```tsx
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <QueryClientProvider client={queryClient}>
      <ErrorBoundary fallback={<AppErrorFallback />}>
        <RouterProvider router={router} />
      </ErrorBoundary>
      <Toaster richColors position="top-center" />
    </QueryClientProvider>
  </React.StrictMode>,
);
```
`<Toaster>` MUSI pozostać POZA `<ErrorBoundary>` — toast portal nie powinien być zdjęty przez fallback (RESEARCH §Pattern 2 line 234).

From React 18 docs (verified via Context7 `/reactjs/react.dev`):
- `static getDerivedStateFromError(error: Error): Partial<State>` — zwraca nowy state, fires PRZED renderem fallback'a; tutaj NIE wolno wykonywać side-effects.
- `componentDidCatch(error: Error, info: ErrorInfo): void` — odpalany PO commit; tutaj logujemy. `info.componentStack` jest stringiem.
- React 18 nie ma funkcyjnego ekwiwalentu — klasa jest jedyną drogą. React 19 też nie zmienia tego (RESEARCH §State of the Art).

From RESEARCH §Pattern 1 (lines 153-189) — AUTHORITATIVE source for the class body. Skopiuj strukturę 1:1, dodaj projektowe konwencje (kebab-case file, named exports, brak `export default`, polski copy w fallback).

From CLAUDE.md UI-language rule:
- UI copy: polski (`Coś poszło nie tak.`, `Załaduj ponownie`).
- Identyfikatory, props, typy, komentarze: angielski.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Utwórz klasę ErrorBoundary z fallback UI</name>
  <files>apps/client/src/components/error-boundary.tsx</files>
  <read_first>
    - apps/client/src/components/auth/protected-route.tsx (lines 9-13 — `flex h-screen w-screen items-center justify-center` layout do mirror'owania)
    - apps/client/src/components/ui/button.tsx (potwierdź named export `Button` + dostępne varianty)
    - .planning/phases/04-frontend-stability/04-RESEARCH.md §Pattern 1 (lines 153-189 — kod klasy ErrorBoundary; verbatim z react.dev) + §Open Question 1 (strukturalny log shape)
    - .planning/phases/04-frontend-stability/04-PATTERNS.md `### apps/client/src/components/error-boundary.tsx` — konwencje (brak `cn`, polski copy, Button z `@/components/ui/button`)
  </read_first>
  <action>
    Utwórz nowy plik `apps/client/src/components/error-boundary.tsx`. Konwencje: kebab-case nazwa pliku, tylko named exports (NIE `export default`), pojedyncze cudzysłowy, trailing commas, semicolons (Biome).

    **Struktura pliku (2 named exports — `ErrorBoundary`, `AppErrorFallback`):**

    1. **Imports:**
       ```tsx
       import { type ErrorInfo, type ReactNode, Component } from 'react';
       import { Button } from '@/components/ui/button';
       ```
       NIE importuj `cn` z `@/lib/utils` — class strings są statyczne (precedens: `protected-route.tsx` używa bare `className=""`, patrz PATTERNS.md `### error-boundary.tsx`).

    2. **Klasa `ErrorBoundary`** — verbatim z RESEARCH §Pattern 1 (linie 153-189), z jedną modyfikacją w `componentDidCatch`: zamiast `console.error('[ErrorBoundary]', error, info.componentStack)` użyj strukturalnego payloadu (RESEARCH §Open Question 1, rekomendacja: tak):
       ```tsx
       componentDidCatch(error: Error, info: ErrorInfo): void {
         // Structured log shape mirrors API logger event naming (e.g. 'igdb.breaker.open').
         // Single-user app — no Sentry/Axiom (SEC-V2-01 deferred). Future hook lands here.
         console.error('[ErrorBoundary]', {
           event: 'render.error.boundary',
           error,
           componentStack: info.componentStack,
         });
       }
       ```
       Reszta klasy (`Props` z `fallback: ReactNode | ((reset: () => void) => ReactNode)`, `State { hasError: boolean }`, `static getDerivedStateFromError`, `private reset = () => this.setState({ hasError: false })`, `render` z guard'em na `this.state.hasError`) — KOPIUJ verbatim z RESEARCH lines 155-189.

    3. **`AppErrorFallback`** — funkcyjny komponent (named export). Polski copy + DWA przyciski: primary "Załaduj ponownie" (nawiguje do `/`) i secondary "Wróć do logowania" (break-glass dla session-induced render-error pętli — eskalowano z grill C1). Reset pattern: `window.location.assign('/')` zamiast `window.location.reload()` — eliminuje URL-induced stuck-loop (RESEARCH §Pattern 1 lines 195-206 + ROADMAP SC1 literalnie `Załaduj ponownie`).

       ```tsx
       export function AppErrorFallback(): ReactNode {
         return (
           <div className="flex h-screen w-screen flex-col items-center justify-center gap-4 bg-white text-apex-ink">
             <h1 className="text-xl font-semibold">Coś poszło nie tak.</h1>
             <p className="max-w-md text-center text-sm text-apex-muted">
               Aplikacja napotkała niespodziewany błąd. Spróbuj odświeżyć stronę.
             </p>
             <div className="flex gap-2">
               <Button onClick={() => window.location.assign('/')}>
                 Załaduj ponownie
               </Button>
               <Button
                 variant="outline"
                 onClick={() => window.location.assign('/login')}
               >
                 Wróć do logowania
               </Button>
             </div>
           </div>
         );
       }
       ```
       Layout (`flex h-screen w-screen ...`) lustrzane do `protected-route.tsx:9-13` (PATTERNS.md). Tailwind tokens projektowe: `text-apex-ink`, `text-apex-muted`, `bg-white` — NIE Tailwind defaults.

       **Rationale (grill C1 → enterprise A.1 → re-grill N3):** `window.location.reload()` na tym samym URL-u tworzy stuck-loop dla URL-induced crashes. `assign('/')` zmienia URL na safe route (`/` przekieruje do `/games` przez ProtectedRoute, lub do `/login` jeśli session stale). Secondary "Wróć do logowania" jest break-glass'em gdy nawet `/` crashuje (session-corruption — np. `useSession()` rzuca dla każdej protected route). **Note:** wcześniej rozważany `queryClient.clear()` przed `assign('/')` został USUNIĘTY (re-grill N3) — `assign()` zrzuca cały JS-runtime włącznie z `queryClient` singletonem, więc `clear()` byłoby placebo. Pisanie kodu który jest no-op'em jest mylące (sugeruje że robi coś czego nie robi). Cache czyści się jako naturalny efekt full navigation.

    **Zakazy:**
    - NIE renderuj `error.message` ani `componentStack` w fallback UI (T-04-01 — XSS/info disclosure).
    - NIE używaj `<button>` raw — użyj `<Button>` z `@/components/ui/button` (precedens: `account-password-form.tsx:151-159`).
    - NIE rób `export default` (CLAUDE.md "Named exports only").
    - NIE wprowadzaj zależności od `sonner` w samym ErrorBoundary — toast nie jest wymagany przez SC1 (RESEARCH §Standard Stack: sonner "not required by SC1").
    - NIE dodawaj `errorElement` do żadnej route w `main.tsx` — RESEARCH §Pattern 2 explicitly: project nie używa loader'ów; klasa jako single boundary wystarcza (Anti-Patterns lines 449-451).

    **Pitfall awareness:**
    - StrictMode w dev wywoła `componentDidCatch` dwukrotnie — RESEARCH §Pitfall 1. To NIE jest bug; nie dedup'uj.
    - `getDerivedStateFromError` zwraca state, NIE wykonuje side-effects (logging należy do `componentDidCatch`).
  </action>
  <verify>
    <automated>
      cd /Users/kodari/projects/games &amp;&amp;
      test -f apps/client/src/components/error-boundary.tsx &amp;&amp;
      grep -q "export class ErrorBoundary" apps/client/src/components/error-boundary.tsx &amp;&amp;
      grep -q "extends Component" apps/client/src/components/error-boundary.tsx &amp;&amp;
      grep -q "static getDerivedStateFromError" apps/client/src/components/error-boundary.tsx &amp;&amp;
      grep -q "componentDidCatch" apps/client/src/components/error-boundary.tsx &amp;&amp;
      grep -q "render.error.boundary" apps/client/src/components/error-boundary.tsx &amp;&amp;
      grep -q "export function AppErrorFallback" apps/client/src/components/error-boundary.tsx &amp;&amp;
      grep -q "Coś poszło nie tak" apps/client/src/components/error-boundary.tsx &amp;&amp;
      grep -q "Załaduj ponownie" apps/client/src/components/error-boundary.tsx &amp;&amp;
      grep -q "Wróć do logowania" apps/client/src/components/error-boundary.tsx &amp;&amp;
      grep -q "window.location.assign" apps/client/src/components/error-boundary.tsx &amp;&amp;
      ! grep -q "window.location.reload" apps/client/src/components/error-boundary.tsx &amp;&amp;
      ! grep -q "queryClient" apps/client/src/components/error-boundary.tsx &amp;&amp;
      ! grep -q "export default" apps/client/src/components/error-boundary.tsx &amp;&amp;
      cd apps/client &amp;&amp; bunx tsc -b --noEmit
    </automated>
  </verify>
  <done>
    Plik istnieje. Klasa `ErrorBoundary extends Component` z `getDerivedStateFromError` + `componentDidCatch` (strukturalny log `render.error.boundary`). `AppErrorFallback` jako named export z polskim copy + DWA przyciski: primary `Załaduj ponownie` (`window.location.assign('/')`) i secondary `Wróć do logowania` (`window.location.assign('/login')`). Brak `window.location.reload`. Brak `queryClient` (re-grill N3 — usunięto placebo `clear()`). Brak `export default`. TypeScript pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Zamontuj ErrorBoundary w main.tsx wokół RouterProvider</name>
  <files>apps/client/src/main.tsx</files>
  <read_first>
    - apps/client/src/main.tsx (cały plik, lines 1-76 — current mount tree)
    - .planning/phases/04-frontend-stability/04-RESEARCH.md §Pattern 2 (lines 210-234 — kolejność mount: StrictMode > QueryClientProvider > ErrorBoundary > RouterProvider, Toaster POZA boundary)
  </read_first>
  <action>
    Single edit `apps/client/src/main.tsx`:

    1. **Dodaj import** — alfabetycznie wśród istniejących `@/` importów:
       ```tsx
       import { AppErrorFallback, ErrorBoundary } from '@/components/error-boundary';
       ```
       (Biome `organizeImports.enabled = true` — IDE/Biome auto-uporządkuje, ale dodaj manualnie żeby diff był minimalny.)

    2. **Zmień JSX w `ReactDOM.createRoot(...).render(...)`** — wrap `<RouterProvider>` w `<ErrorBoundary fallback={<AppErrorFallback />}>`, ZACHOWAJ `<Toaster>` POZA boundary:

       Before:
       ```tsx
       <QueryClientProvider client={queryClient}>
         <RouterProvider router={router} />
         <Toaster richColors position="top-center" />
       </QueryClientProvider>
       ```

       After:
       ```tsx
       <QueryClientProvider client={queryClient}>
         <ErrorBoundary fallback={<AppErrorFallback />}>
           <RouterProvider router={router} />
         </ErrorBoundary>
         <Toaster richColors position="top-center" />
       </QueryClientProvider>
       ```

    **Krytyczna kolejność (RESEARCH §Pattern 2 + §Pitfall 2):**
    - `QueryClientProvider` MUSI być rodzicem `ErrorBoundary` (query cache przeżywa fallback hipotetyczny soft reset — chociaż w tym phase robimy `window.location.reload()`, kolejność i tak jest standardem).
    - `ErrorBoundary` rodzicem `RouterProvider` (łapie błędy renderu w całym drzewie route'ów).
    - `Toaster` POZA `ErrorBoundary` — toast portal nie ginie z fallback'iem.

    **Zakazy:**
    - NIE dodawaj `errorElement` do żadnej route'y w `createBrowserRouter([...])` — RESEARCH §Pattern 2 i §Anti-Patterns (lines 449-451): project nie używa `loader`/`action`; klasa w main.tsx wystarcza.
    - NIE zmieniaj routów w `createBrowserRouter` ani `Placeholder` helpera — out of scope dla tego planu.
    - NIE owijaj `<StrictMode>` w `<ErrorBoundary>` — boundary musi być WEWNĄTRZ providerów (StrictMode → QueryClientProvider → ErrorBoundary).
  </action>
  <verify>
    <automated>
      cd /Users/kodari/projects/games &amp;&amp;
      grep -q "import { AppErrorFallback, ErrorBoundary } from '@/components/error-boundary';" apps/client/src/main.tsx &amp;&amp;
      grep -q "&lt;ErrorBoundary fallback={&lt;AppErrorFallback /&gt;}&gt;" apps/client/src/main.tsx &amp;&amp;
      awk '/&lt;ErrorBoundary/{eb=NR} /&lt;RouterProvider/{rp=NR} /&lt;Toaster/{to=NR} /&lt;\/ErrorBoundary/{ebc=NR} END{exit !(eb&lt;rp &amp;&amp; rp&lt;ebc &amp;&amp; ebc&lt;to)}' apps/client/src/main.tsx &amp;&amp;
      ! grep -q "errorElement" apps/client/src/main.tsx &amp;&amp;
      cd apps/client &amp;&amp; bunx tsc -b --noEmit &amp;&amp; bunx vite build --mode development
    </automated>
  </verify>
  <done>
    `main.tsx` importuje `ErrorBoundary` + `AppErrorFallback`. `<ErrorBoundary fallback={<AppErrorFallback />}>` owija `<RouterProvider>` wewnątrz `<QueryClientProvider>`. `<Toaster>` POZA boundary. `errorElement` NIE dodany do żadnej route. TypeScript + Vite build pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Globalny ErrorBoundary aktywny w produkcyjnym mount tree. Każdy uncaught render error w drzewie `<RouterProvider>` triggeruje fallback UI z polskim komunikatem i przyciskiem `Załaduj ponownie`.
  </what-built>
  <how-to-verify>
    Manualny smoke test FE-01 (poniższy scenariusz nie może być zautomatyzowany w obecnym test-stacku — brak jsdom/RTL):

    1. `bun run dev` w repo root, otwórz `http://localhost:5173`.
    2. Zaloguj się normalnie. Otwórz `/games` — strona renderuje się jak dotychczas (bez regresji).
    3. **Tymczasowo** dodaj do dowolnego komponentu (np. `apps/client/src/pages/games.tsx` na początku funkcji) linię:
       ```tsx
       if (location.search.includes('boom')) throw new Error('synthetic render error');
       ```
       Zapisz, poczekaj na HMR.
    4. Wejdź na `http://localhost:5173/games?boom`. Oczekiwane:
       - Strona pokazuje pełny fallback (białe tło, "Coś poszło nie tak.", podpis, DWA przyciski: "Załaduj ponownie" + "Wróć do logowania") — NIE jest pustym białym ekranem.
       - DevTools Console: log `[ErrorBoundary] { event: 'render.error.boundary', error: Error, componentStack: '...' }` (w StrictMode dev — dwukrotnie, to OK per RESEARCH §Pitfall 1).
       - Toast portal sonner nadal działa (jeśli wystrzelisz toast np. otwierając inne okno przeglądarki z appem) — bo `<Toaster>` jest POZA boundary.
    5. **Test C1 fix (URL-stuck-loop elimination):** Klik `Załaduj ponownie`. Oczekiwane:
       - URL ZMIENIA SIĘ na `/` (z `/games?boom`) — to dowodzi że reset jest `assign('/')`, NIE `reload()` na ten sam URL.
       - Strona po nawigacji renderuje normalnie (`/` → ProtectedRoute → `/games` bez `?boom` lub `/login`).
       - **Cache reset (verified indirectly):** TanStack Query cache jest in-memory (NIE w localStorage/IndexedDB/cookies — Application → Storage NIC nie pokaże). Verify pośrednio: w DevTools Network panel widoczne są nowe GET'y do `/api/games` po nawigacji (cache był zrzucony przez `assign` razem z JS runtime). Brak żadnych "from disk cache" hits dla danych aplikacyjnych.
       - Tymczasowy throw w `games.tsx` STAŁA pułapka — następne wejście na `/games?boom` znów odpali fallback.
    6. **Test break-glass button:** Wróć URL-em do `/games?boom`. Klik `Wróć do logowania`. Oczekiwane:
       - URL ZMIENIA SIĘ na `/login`. Strona logowania renderuje się (user może wylogować się i zalogować ponownie jako recovery z session-corruption).
    7. Usuń URL fragment `?boom` w pasku adresu → enter → strona renderuje normalnie.
    8. **USUŃ tymczasowy throw** z `games.tsx` przed commitem (sprawdź `git diff` przed `git add`).

    Dodatkowe wizualne kryteria akceptacji:
    - Brak inline `<svg>` w fallback (icons.tsx out of scope dla tego planu, ale fallback nie ma używać inline svg).
    - Brak renderowania `error.message`/`componentStack` w widocznym tekście (T-04-01).
    - `Button` z `@/components/ui/button` (NIE raw `<button>`) — sprawdź classNames w DevTools (`buttonVariants` default classy).
  </how-to-verify>
  <resume-signal>Wpisz "approved" jeśli fallback działa, lub opisz problemy (brak fallback'a / błędny copy / błąd reload'a / inline svg / coś wycieka).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| React render → ErrorBoundary | Synchronizny error w renderze deszczu komponentów; klasowa boundary łapie go w `getDerivedStateFromError` |
| ErrorBoundary → DOM (fallback) | Statyczny copy renderowany do DOM; brak user input echoed back |
| componentDidCatch → console | Strukturalny log; w v2 hook do Sentry/Axiom (SEC-V2-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information disclosure | Fallback UI echoes `error.message` / `componentStack` to user | mitigate | Fallback renderuje WYŁĄCZNIE statyczny polski copy (`Coś poszło nie tak.` + opis). `error.message` i `componentStack` lądują tylko w `console.error` (DevTools, lokalnie). Pin w `<action>` Task 1: "NIE renderuj error.message ani componentStack". |
| T-04-02 | Tampering / XSS | Inline HTML w fallback'u | n/a | Cały copy jest hard-coded string literals; brak `dangerouslySetInnerHTML`, brak `innerHTML`, brak user-supplied content w fallback. Pin: source musi mieć tylko statyczny copy. |
| T-04-03 | Denial of service | Loop: render error → fallback → reload → render error → ... (stuck-loop dla URL-induced crashes) | mitigate | Primary reset jest `window.location.assign('/')` — NIE `reload()` na ten sam URL. `assign('/')` przekierowuje na safe route (`/games` lub `/login` przez ProtectedRoute), eliminując URL-induced deterministic-crash loop. Secondary "Wróć do logowania" → `assign('/login')` jest break-glass'em dla globally-induced crashes (T-04-24 — np. `useSession()` rzuca pod każdym URL-em). Obydwa są user-triggered (klik), nie automatyczne. GET endpointy nie wchodzą w mutation rate-limit (`MUTATING_METHODS` w `apps/api/src/routes/middleware/mutation-rate-limit.ts:9`), więc nawet uporczywy reload-loop nie zdrenuje limiter'a. Granica: jeśli zarówno `/` jak i `/login` crashują (catastrophic), user musi zamknąć tab — alternatywne fixy (max-3-resets-then-help) są over-engineered dla single-user. |
| T-04-21 | Tampering | In-flight mutation terminated w pół requestu przez reset (klik fallback'a w trakcie POST/PUT/DELETE) | accept (with honest caveats) | **Faktyczna analiza per HTTP metoda (re-grill NEW-E1 + NEW-E6 → enterprise A.2 corrected):** (1) **POST** (`apps/client/src/lib/api.ts:66,74,167,177,188` — createWishlistItem, createGame, importData, uploadCover, moveToCollection) — wstrzykuje świeży `Idempotency-Key` (UUID v4) **inline w każdym wywołaniu funkcji** (`newIdempotencyKey()` jest evaluated w arg list, nie cache'owane per-mutation). Server-side `idempotencyKeyMiddleware` deduplikuje retry **tylko jeśli klient reuse'uje ten sam klucz** — co w obecnym kodzie się NIE dzieje. TanStack Query `retry: 1` (`query-client.ts:8`) replays mutationFn → świeży UUID na retry. Po `window.location.assign('/')` cały JS-runtime znika, idempotency-key kontekstu nie ma. **Wniosek:** dla scenariusza "klik fallback w trakcie POST" idempotency-key NIE chroni przed dwukrotnym utworzeniem zasobu na serwerze (jeśli server processed request przed reset). Rozwiązanie planowane: **Plan 04-04** wprowadzi cache'owanie idempotency-key per mutation-instance (useRef w hooku) — wtedy ochrona stanie się faktyczna. Do tego czasu: ryzyko duplicate-create akceptujemy (single-user app, user widzi duplicat i może go usunąć). (2) **PUT/PATCH** (`apps/client/src/lib/api.ts:81` — updateGame, `:102` — enrichGameMetadata) — klient wysyła **pełny snapshot** w body (`input: UpdateGameInput`), NIE wysyła `expectedUpdatedAt`. Server w transakcji robi `re-read` aggregate'u, captures `updatedAt`, używa go jako optimistic-lock. **Konsekwencja:** retry PUT z tym samym body po reset = idempotentne przez koincydencję (full-replace = ten sam stan końcowy), bo server-side optimistic-lock chroni przed *concurrent* writes (dwie karty edytują), NIE przed *retry-after-crash*. Bezpieczne dla obecnych PUT/PATCH endpointów (które są full-replace). Niebezpieczne dla future "increment hours_played by 1" PATCH semantyki (relative delta) — taki endpoint potrzebowałby explicit idempotency-key. Convention: jeśli wprowadzasz relative-update PATCH, dodaj idempotency-key per case-by-case. (3) **DELETE** (`apps/client/src/lib/api.ts:84` — deleteGame) — HTTP idempotent przez specyfikację. Powtórzenie `DELETE /api/games/:id` zwróci 404 jeśli już usunięty. Bezpieczne. **Wyjątki w api.ts**: `:223` (saveIgdbIntegration — PUT z idempotency-key) i `:230` (deleteIgdbIntegration — DELETE z idempotency-key) — to są Settings endpointy które NIE operują na aggregate z `updatedAt`, więc nie mają optimistic-lock. Idempotency-key jest tu **jedyną** ochroną przed double-save/delete, i działa tak samo "tylko jeśli klient reuse'uje key" — tj. plan 04-04 też je obejmuje. |
| T-04-24 | Tampering | Render-time crash w komponencie globalnym (np. `ProtectedRoute`, `AppLayout`) — primary reset `assign('/')` nie eliminuje stuck-loop bo cel też crashuje | mitigate | Re-grill NEW-E7. C1 fix eliminuje **URL-induced** stuck-loop (crash zależny od `?boom` query param zniknie po assign('/')). NIE eliminuje **globally-induced** stuck-loop (np. `useSession()` rzuca stale-token error → fallback → assign('/') → ProtectedRoute → useSession() → ten sam crash). Dla tego scenariusza secondary "Wróć do logowania" → `assign('/login')` jest jedyną drogą wyjścia (login jest public, nie woła useSession). Jeśli `/login` też crashuje — fallback boundary trzyma, ale user jest hard-stuck (close tab, sign-out via DevTools cookie deletion). To akceptowalne dla single-user app — alternatywne fixy (np. error retry counter, max-3-resets-then-show-help) są over-engineered dla tej skali. |
| T-04-04 | Information disclosure | Strukturalny log w `console.error` wycieka stack frames z user-PII jeśli error.message zawiera dane | accept | Single-user app na VPS — DevTools dostępne tylko właścicielowi. W v2 (SEC-V2-01 Sentry) trzeba będzie scrubbować PII przed wysłaniem. Format `{event,error,componentStack}` jest gotowy do wpięcia scrubber w v2. |
| T-04-05 | Repudiation | Brak audit trail erroru poza local console | accept | Single-user; przyjęte. SEC-V2-01 to closes (Sentry/Axiom). |
| T-04-06 | Elevation of privilege | Sesja/cookies/route guards bypass przez fallback | n/a | Fallback renderuje POZA `RouterProvider` (zastępuje go); żadne dane chronione nie są w fallback'u. `QueryClientProvider` (rodzic) cache zostaje zrzucony jako naturalny side-effect `window.location.assign('/')` (full page navigation niszczy cały JS-runtime, włącznie z module-level `queryClient` singletonem). Po nawigacji `ProtectedRoute` montuje się od nowa i revalidate'uje session przez `useSession()`. |

ASVS L1 mapping: V14 (Configuration) — error handling configured to fail closed (no info disclosure). V11 (Business logic) — n/a, this is render-time error trap, not business logic.
</threat_model>

<verification>
- `bunx tsc -b --noEmit` w `apps/client/` passes.
- `bunx vite build --mode development` w `apps/client/` succeeds.
- Grep invariants (z repo root):
  ```
  grep -q "class ErrorBoundary extends Component" apps/client/src/components/error-boundary.tsx
  grep -q "static getDerivedStateFromError" apps/client/src/components/error-boundary.tsx
  grep -q "componentDidCatch" apps/client/src/components/error-boundary.tsx
  grep -q "render.error.boundary" apps/client/src/components/error-boundary.tsx
  grep -q "Coś poszło nie tak" apps/client/src/components/error-boundary.tsx
  grep -q "Załaduj ponownie" apps/client/src/components/error-boundary.tsx
  grep -q "Wróć do logowania" apps/client/src/components/error-boundary.tsx
  grep -q "window.location.assign" apps/client/src/components/error-boundary.tsx
  ! grep -q "window.location.reload" apps/client/src/components/error-boundary.tsx
  ! grep -q "queryClient" apps/client/src/components/error-boundary.tsx
  grep -q "<ErrorBoundary" apps/client/src/main.tsx
  ! grep -q "errorElement" apps/client/src/main.tsx
  ```
- Manualny smoke (Task 3 checkpoint): tymczasowy `throw new Error('synthetic')` w jakiejś page (np. `/games/42?boom`) → fallback UI renderuje się; po `Załaduj ponownie` strona NAWIGUJE na `/` (NIE na ten sam URL z `?boom`) — to dowodzi że reset eliminuje stuck-loop. Po `Wróć do logowania` strona nawiguje na `/login`. Po usunięciu throw'a wszystko normalne.
- Brak nowych devDependencies (RESEARCH: "no new packages"). `package.json` nie modified.
</verification>

<success_criteria>
ROADMAP success criteria addressed:
- **SC-1 (FE-01)**: Uncaught render error w dowolnej page'u nie blank'uje SPA — globalny `ErrorBoundary` w `main.tsx` pokazuje fallback UI z komunikatem i przyciskiem "Załaduj ponownie". ✓ Klasa `ErrorBoundary` zamontowana między `<QueryClientProvider>` a `<RouterProvider>`; `AppErrorFallback` z polskim copy + Button z `window.location.reload()`.

REQUIREMENTS addressed:
- **FE-01**: Globalny `ErrorBoundary` opakowuje `<RouterProvider>` w `main.tsx`; renderuje fallback UI (komunikat + przycisk "Załaduj ponownie") — ✓ zamknięte przez Task 1 + Task 2; pinowane przez grep invariants + manualny checkpoint Task 3.

Out of Scope (explicit deferrals):
- Per-route `errorElement` — RESEARCH §Anti-Patterns; project nie używa loader/action.
- Soft reset (`setState({hasError:false})` bez reload) — odrzucony per RESEARCH §Pattern 1 line 208; full reload jest semantycznie pełniejszy i SC1 mówi literalnie "przycisk reload".
- Sentry/Axiom integration (SEC-V2-01) — odroczone; format `{event,error,componentStack}` zostawia trywialny hook.
- Toast notification on caught error — RESEARCH §Standard Stack: sonner "not required by SC1"; fallback UI jest wystarczający.
- Recovery przyciski typu "Wróć do /games" — out of scope; reload to całe SC1.
</success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-stability/04-01-SUMMARY.md` recording:
- Finalny LOC count `error-boundary.tsx` (target ~50 LOC)
- Czy `componentDidCatch` użył strukturalnego payloadu `{event,error,componentStack}` (per RESEARCH §Open Question 1 rekomendacja) — yes/no + reason
- Manualny smoke result: czy synthetic-throw test wyrenderował fallback (Task 3 checkpoint)
- Czy StrictMode dev wywołał `componentDidCatch` dwukrotnie (potwierdzenie RESEARCH §Pitfall 1 jako expected, nie regression)
- Czy `<Toaster>` pozostał POZA `<ErrorBoundary>` (potwierdzenie mount order)
</output>
</content>
</invoke>